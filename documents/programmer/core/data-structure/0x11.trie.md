---
title: Trie - “前缀树”
icon: hashtag

index: true

---

<!-- more -->

## concept

`Trie`（前缀树）是一种按字符路径组织字符串集合的树形结构，适合处理前缀匹配、自动补全、词典查询和敏感词过滤等场景。

## operations

- `insert`：逐字符插入单词，并在结尾节点标记完整单词。
- `search`：逐字符查找，最后确认结尾节点是否为完整单词。
- `startsWith`：只检查前缀路径是否存在，不要求结尾节点是完整单词。
