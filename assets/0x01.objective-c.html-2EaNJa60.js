import{_ as e}from"./plugin-vue_export-helper-x3n3nnut.js";import{r as o,o as c,c as t,a as i,b as n,e as p,d as l,f as a}from"./app-XiL9qv-c.js";const d={},r=a(`<h2 id="设计理念" tabindex="-1"><a class="header-anchor" href="#设计理念" aria-hidden="true">#</a> 设计理念</h2><p><code>OC</code> 底层是使用 <code>C/C++</code> 实现。定一个继承 <code>NSObject</code> 的类，这个类底层使用 <code>C</code> 语言中的 <code>struct</code> 结构体来实现的。</p><h3 id="❓oc、java、python-运行效率孰高" tabindex="-1"><a class="header-anchor" href="#❓oc、java、python-运行效率孰高" aria-hidden="true">#</a> ❓<code>OC、Java、Python</code> 运行效率孰高？</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>Objective-C</code>: 编译型语言，运行效率通常优于解释型语言。底层是基于 <code>C</code>，但是由于动态派发的特性会一定程度上影响效率。</p></li><li><p><code>Java</code>: 编译型语言，运行环境是 <code>JVM</code>(<code>Java</code> 虚拟机)，通过 <code>JIT</code>(Just In Time compiler)技术，<code>Java</code> 能将字节码实时地编译成机器码运行，因此运行效率<strong>较高</strong>，而且在大数据和 <code>Web</code> 服务领域表现优秀。</p></li><li><p><code>Python</code>: 是解释型语言，其运行效率<strong>较低</strong>，无法与编译型语言相比。但是 <code>Python</code> 的易读性、易写性以及丰富的第三方库，使它在科学计算、人工智能等领域得到了广泛的应用。</p></li></ul></details><h3 id="❓oc-的动态性" tabindex="-1"><a class="header-anchor" href="#❓oc-的动态性" aria-hidden="true">#</a> ❓<code>OC</code> 的动态性</h3><details class="hint-container details"><summary>💡</summary><p>首先理解两个“动态语言与静态语言”</p><p><code>OC</code> 的 <code>runtime</code> 文档中介绍中就详细写明了，<code>runtime</code> 的设计是为了将 <code>OC</code> 中的更多的处理延迟到编译后。</p><ul><li>动态类型 (Dynamic typing) : 编译时并不确定对象类型，在运行时才判断对象的类型。<code>OC</code> 中一切对象都可以用 <code>id</code> 指针指向。</li><li>动态绑定 (Dynamic binding) : 方法的调用也是运行时才决定，而并非编译时。</li><li>动态加载 (Dynamic loading) : 运行时可以动态加载其它模块或框架</li></ul></details><h3 id="❓oc-是如何保证系统升级后的-abi-稳定性的" tabindex="-1"><a class="header-anchor" href="#❓oc-是如何保证系统升级后的-abi-稳定性的" aria-hidden="true">#</a> ❓<code>OC</code> 是如何保证系统升级后的 <code>ABI</code> 稳定性的？</h3><details class="hint-container details"><summary>详情</summary><ul><li><p>向后兼容（<code>Backward Compatibility</code>）：新版本的系统库在设计和更新时，会尽量保证向下兼容。</p></li><li><p>动态性（<code>Dynamic Nature</code>）：作为一种动态语言，<code>OC</code> 大量使用动态分发和运行时解析。这意味着很多事情在运行时才确定，例如方法的调用、对象的生成等。这种机制在维护 <code>ABI</code> 稳定性时有很大的灵活性，可以在运行时解析和处理一些可能的问题。</p></li><li><p>利用类和分类（Class and Category）：使用类和分类来模块化代码，使得代码逻辑更容易管理，也更利于维护 <code>ABI</code> 稳定性。在新增或者修改功能时，可以通过添加新的类或者分类，而不是修改旧的代码，这样就能保证旧的代码逻辑和 <code>ABI</code> 不被破坏。</p></li><li><p>接口过期标注(<code>API Deprecation</code>)：系统库在引入新的接口时，会把不推荐使用的旧接口标记为 <code>deprecated</code>，但不会立即删除，这样就可以保证依赖旧接口的代码在一段时间内可以正常运行。</p><p>以上这些都只能尽可能地保证 ABI 的稳定性，但并不能绝对地保证。因为系统升级往往会引入新的功能、新的架构，或者修复旧的 <code>bug</code>，这些都可能对 <code>ABI</code> 产生影响。所以，作为开发者在编写代码时，也需要注意兼容性，避免过度依赖可能会变动的接口和行为。</p></li></ul></details><h3 id="❓id-vs-self-vs-super" tabindex="-1"><a class="header-anchor" href="#❓id-vs-self-vs-super" aria-hidden="true">#</a> ❓<code>id</code> vs <code>self</code> vs <code>super</code></h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>id</code>：一种数据类型，也叫做动态对象类型，所有的 <code>Objective-C</code> 对象都可以用 <code>id</code> 类型来接收。<code>id</code> 类型会忽略类型信息，可以给它发送任何消息，如果在运行时对象能够响应这个消息，则代码可以正常运行，否则会在运行时报错。</p></li><li><p><code>self</code>: 每个对象方法的隐含参数，它指向调用当前方法的对象。<code>self</code> 的数据类型也是 <code>id</code>，但它通常用来引用对象自己。可以用 <code>self</code> 来访问属性或者调用方法。</p></li><li><p><code>super</code>: 并不是一个指针，是一个编译器标识符，代表当前对象的父类。当你向 <code>super</code> 发送消息时，编译器会沿着方法的继承链向上寻找，直到找到对应的方法实现为止。这个机制可用于在子类中调用父类的同名方法，以实现代码的复用。</p></li></ul></details><h3 id="❓id-vs-nsobject" tabindex="-1"><a class="header-anchor" href="#❓id-vs-nsobject" aria-hidden="true">#</a> ❓<code>id</code> vs <code>NSObject</code></h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>id</code>: 一个动态类型的对象指针，它可以指向任何类型的 <code>Objective-C</code> 对象。<code>id</code> 类型的对象可以接收任何消息而不会在编译期报错，运行期若无法响应则会引发异常。使用 <code>id</code> 类型可以编写出非常灵活的代码，但是也需要面对潜在的运行期错误。</p></li><li><p><code>NSObject</code>: 是所有 <code>Objective-C</code> 类的基类，定义了一些所有对象都能响应的基本方法，例如 -<code>init</code>、<code>retain</code>、<code>release</code> 等。如果一个对象类型声明为 <code>NSObject *</code>，只能给它发送 <code>NSObject</code> 类中定义的方法。如果试图发送其他消息，会得到一个编译错误。</p></li></ul></details><h3 id="❓id-vs-instancetype" tabindex="-1"><a class="header-anchor" href="#❓id-vs-instancetype" aria-hidden="true">#</a> ❓<code>id</code> vs <code>instancetype</code></h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>id</code>：一个动态类型的对象指针，可以指向任何类型的 <code>Objective-C</code> 对象。使用 <code>id</code> 类型的变量可以调用任何方法，如果该对象的类实现了该方法，那么就会在运行时调用。缺点是编译器无法进行类型检查和自动补全。</p></li><li><p><code>instancetype</code>：一种特殊的类型，主要用于表示对象的实例，用在方法的返回类型中。与 <code>id</code> 类型不同，方法使用 <code>instancetype</code> 作为返回类型，那么编译器会认为该方法返回一个当前类的实例。这个特性可以帮助编译器进行更准确的类型检查，同时还能提供自动补全的支持。</p></li></ul></details><h3 id="❓id-为什么不能用点语法" tabindex="-1"><a class="header-anchor" href="#❓id-为什么不能用点语法" aria-hidden="true">#</a> ❓<code>id</code> 为什么不能用点语法？</h3><details class="hint-container details"><summary>💡</summary><blockquote><p>点语法是一种简化属性访问的语法。底层实际上是编译器做了一个转换操作，把点语法转换成相应的 <code>getter</code>（对于获取属性）或者 <code>setter</code>（对于设置属性）方法的调用。</p></blockquote><ul><li><p>明确类型的对象，编译器可以知道对象的类定义，知道该对象所拥有的属性和方法，所以在编译期可以正确地做出转换。</p></li><li><p><code>id</code> 类型的对象，编译器无法知道它的实际类型以及拥有的属性和方法，所以无法进行点语法的转换。</p></li></ul></details><h3 id="❓nil-vs-nil-vs-null-vs-nsnull" tabindex="-1"><a class="header-anchor" href="#❓nil-vs-nil-vs-null-vs-nsnull" aria-hidden="true">#</a> ❓<code>nil</code> vs <code>Nil</code> vs <code>NULL</code> vs <code>NSNull</code></h3><details class="hint-container details"><summary>💡</summary><ul><li><code>nil</code> : 一种特殊的指针，指向为空的 <code>OC</code> 对象</li><li><code>Nil</code> :</li><li><code>NULL</code> : 也是表示空指针，一般代表 <code>C</code> 指针为空</li><li><code>NSNull</code> :</li></ul></details><h3 id="❓-load-与-initialize-区别" tabindex="-1"><a class="header-anchor" href="#❓-load-与-initialize-区别" aria-hidden="true">#</a> ❓<code>+load</code> 与 <code>+initialize</code> 区别</h3><details class="hint-container details"><summary>💡</summary><p>这两个都是类方法，可以从下面几个方面去对比两者的区别</p><ul><li><p>调用时机</p><ul><li><code>+load</code> : 这是在运行时加载时，进行触发调用。</li><li><code>+initialize</code> : 会在类第一次使用时，进行触发调用。</li></ul></li><li><p>调用次数</p><ul><li><code>+load</code> : 只会在运行时加载时，调用一次。</li><li><code>+initialize</code> : 第一次调用时，调用一次。</li></ul></li><li><p>调用父类</p><ul><li><code>+load</code> : 不会调用父类的 <code>+load</code></li><li><code>+initialize</code> : 如果当前类没有</li></ul></li><li><p>手动调用</p><ul><li><code>+load</code> : 不能手动调用，运行时进行调用的。手动调用会触发异常。</li><li><code>+initialize</code> : 可以手动调用。</li></ul></li></ul></details><blockquote><p>❓<code>+load</code> 的调用顺序</p></blockquote><details class="hint-container details"><summary>💡</summary><ul><li><code>+load</code> 方法调用顺序与编译连接顺序有关。</li><li>不同类之间的<code>+load</code>方法的调用顺序不能确定，取决于编译连接顺序。</li><li>如果父类、子类和分类都实现了 <code>+load</code> 方法，<code>+load</code> 方法的调用顺序是：先调用父类的<code>+load</code>，再调用子类的<code>+load</code>，最后调用分类的<code>+load</code>。</li></ul></details><h3 id="❓designated-initializer-如何添加" tabindex="-1"><a class="header-anchor" href="#❓designated-initializer-如何添加" aria-hidden="true">#</a> ❓<code>Designated Initializer</code> 如何添加？</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>NS_DESIGNATED_INITIALIZER</code> 是一个宏，用于向编译器标明这个是<code> Designated Initializer</code>。主要目的就是明确指定初始化方法，并帮助我们遵守初始化方法的基本规则：所有的初始化方法最终都应该通过调用同一类中的一个指定初始化方法来完成初始化。</p></blockquote><p><code>NS_DESIGNATED_INITIALIZER</code> 这个宏不是必须的。但加上这个宏可以明确指出哪个是指定初始化方法，有助于提高代码的可读性。另外，如果你在使用 <code>Xcode</code> 开发，并且启用了一些警告选项，没有使用 <code>NS_DESIGNATED_INITIALIZER</code> 宏来标记指定初始化方法可能会导致 <code>Xcode</code> 发出警告信息。</p><p><code>NS_DESIGNATED_INITIALIZER</code> 这个宏的工作原理是，如果你的类中的某个初始化方法使用了这个宏，但是这个方法并没有调用同类中的其他使用了这个宏的初始化方法，或者调用了父类中使用了这个宏的初始化方法，那么编译器就会发出一个警告。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token operator">-</span> <span class="token punctuation">(</span>instancetype<span class="token punctuation">)</span>initWithMyParameter<span class="token punctuation">:</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>myParameter NS_DESIGNATED_INITIALIZER<span class="token punctuation">;</span>
<span class="token keyword">@end</span>
<span class="token keyword">@implementation</span> MyClass
<span class="token operator">-</span> <span class="token punctuation">(</span>instancetype<span class="token punctuation">)</span>initWithMyParameter<span class="token punctuation">:</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>myParameter <span class="token punctuation">{</span>
    <span class="token comment">// 先调用父类的 init</span>
    <span class="token keyword">self</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token keyword">super</span> init<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// 初始化操作...</span>
    <span class="token punctuation">}</span>
    <span class="token keyword">return</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓类簇的优缺点" tabindex="-1"><a class="header-anchor" href="#❓类簇的优缺点" aria-hidden="true">#</a> ❓类簇的优缺点？</h3><details class="hint-container details"><summary>💡</summary><blockquote><p>类簇（<code>Class Cluster</code>）是一种设计模式，在 <code>Objective-C</code> 的类库中有广泛的运用，例如<code>NSString</code>，<code>NSArray</code>，<code>NSDictionary</code>等都是类簇的例子。类簇将一些实现细节封装在内部，对外只提供一个简单的公共接口。</p></blockquote><ul><li><p>优点：</p><ul><li><p>封装：类簇将相关的子类放在同一个父类中，使得框架更加简洁，使用者无需熟悉大量类就能够做很多事情。</p></li><li><p>隐藏实现细节：对外只暴露抽象的接口，隐藏了具体实现的细节。这也保护了代码的稳定性，即使在实现过程中改变了相关的类或算法，对外接口的不变就保证了代码的稳定性。</p></li><li><p>代码的分层更加清晰：类簇模式将数据和操作数据的方法绑定在同一个对象上，使得类的分层关系更清楚。</p></li></ul></li><li><p>缺点：</p><ul><li><p>可扩展性差：由于类簇的具体实现不对外暴露，所以如果需要定制某些特殊的行为，扩展性就会变得很差。</p></li><li><p>创建子类困难：由于大部分方法在私有子类中实现，如果要创建类簇的子类并覆盖某些方法，可能需要实现大量的代码才能获得预期的行为。</p></li><li><p>维护难度大：由于类簇中包含了大量的私有子类，如果要修改或扩展代码，就需要对所有的子类进行处理，这会增加维护难度。</p></li></ul></li></ul></details><hr><h2 id="nsstring" tabindex="-1"><a class="header-anchor" href="#nsstring" aria-hidden="true">#</a> NSString</h2><h3 id="❓oc-中的三种字符串" tabindex="-1"><a class="header-anchor" href="#❓oc-中的三种字符串" aria-hidden="true">#</a> ❓<code>OC</code> 中的三种字符串？</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>__NSCFConstantString</code> ：在编译时被创建的常量字符串对象，如<code>NSString *str = @&quot;Hello, World!&quot;</code>。这些对象会在编译时就被创建，并且在程序的整个生命周期中都不会被销毁。</p></li><li><p><code>__NSTaggedPointerString</code> ：使用 <code>Tagged Pointer</code> 技术来提高内存使用效率，通常用于存储较小的字符串数据。</p></li><li><p><code>__NSCFString</code> ：本质上是一个 <code>Core Foundation</code> 字符串的引用（<code>CFStringRef</code>），这种类型的字符串通常会在运行时程序动态创建的对象字符串。</p></li></ul></details><blockquote><p><code>Tagged Pointer</code> 了解吗？</p></blockquote><details class="hint-container details"><summary>💡</summary><blockquote><p><code>iOS 7</code> 之后 Apple 为了优化引入的一项技术。从字面意思上来看<code>Tagged Pointer</code>就是带有标签的指针。</p></blockquote><p>一般来说，在 64-bit 架构下，一个指针的值需要占用 8 byte 的内存空间；然而对于一些非常小的数据，比如 NSNumber、NSDate、NSString 等，使用 8 byte 存储对象指针就显得非常浪费。为了解决这个问题，苹果引入了 <code>Tagged Pointer</code> 技术，通过标签直接将小的数据对象嵌入到指针中，这样对于小的数据对象，就无需额外的内存来存储了，能够显著提高内存的利用率。需要注意，一个对象是否使用 <code>Tagged Pointer</code> 是由系统决定的，不能人为控制。</p><p>具体到 <code>NSString</code> 这种场景中，如果字符串的内容非常短，那么它的实现方式可能就是 <code>Tagged Pointer</code>，直接将字符串内容存在指针中，提高了内存利用率。</p></details><h3 id="❓nsstring-如何计算字符的个数" tabindex="-1"><a class="header-anchor" href="#❓nsstring-如何计算字符的个数" aria-hidden="true">#</a> ❓<code>NSString</code> 如何计算字符的个数？</h3><details class="hint-container details"><summary>💡</summary><p>可以使用 <code>NSString</code> 的 <code>length</code> 属性来获取字符串长度，但这实际上返回的是字符串中 <code>Unicode</code> 编码的字符数量，并不是通常意义上的字符数量。例如，一个中文字符和一个 <code>emoji</code> 表情，可能都会被算作多个 <code>Unicode</code> 字符。</p><p>可以使用 <code>NSString</code> 的 <code>enumerateSubstringsInRange:options:usingBlock:</code> 方法，用 <code>NSStringEnumerationByComposedCharacterSequences</code> 选项来统计字符串的字符数量。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__block NSUInteger characterCount <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token punctuation">[</span>myString enumerateSubstringsInRange<span class="token punctuation">:</span><span class="token function">NSMakeRange</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> myString<span class="token punctuation">.</span>length<span class="token punctuation">)</span>
                           options<span class="token punctuation">:</span>NSStringEnumerationByComposedCharacterSequences
                        usingBlock<span class="token punctuation">:</span><span class="token operator">^</span><span class="token punctuation">(</span>NSString <span class="token operator">*</span>substring<span class="token punctuation">,</span> NSRange substringRange<span class="token punctuation">,</span> NSRange enclosingRange<span class="token punctuation">,</span> BOOL <span class="token operator">*</span>stop<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    characterCount<span class="token operator">++</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><hr><h2 id="nsarray" tabindex="-1"><a class="header-anchor" href="#nsarray" aria-hidden="true">#</a> NSArray</h2><h3 id="❓-nsmutablearray-alloc-init-和-nsmutablearray-array-有什么区别" tabindex="-1"><a class="header-anchor" href="#❓-nsmutablearray-alloc-init-和-nsmutablearray-array-有什么区别" aria-hidden="true">#</a> ❓<code>[[NSMutableArray alloc] init]</code> 和 <code>[NSMutableArray array]</code> 有什么区别?</h3><details class="hint-container details"><summary>详情</summary><ul><li><p>相同点：都是用来创建一个可变数组的。</p></li><li><p>不同点</p><ul><li>代码简洁度：<code>[NSMutableArray array]</code> 比 <code>[[NSMutableArray alloc] init]</code> 代码更简洁，更易于阅读和理解。</li><li>返回的对象所有权：<code>[[NSMutableArray alloc] init]</code> 返回的是一个引用计数为 1 的对象，而 <code>[NSMutableArray array]</code> 返回的是一个自动释放的对象（<code>autoreleased object</code>）。因此，如果在方法内创建了数组，然后需要返回这个数组，那么 <code>[NSMutableArray array]</code> 是一个更好的选择，因为它返回的对象会在方法结束后自动释放，不需要手动释放。如果是在方法内部创建数组，且数组在方法结束后就不再需要，那么 <code>[[NSMutableArray alloc] init]</code> 是一个更好的选择，因为它的引用计数为1，当不再需要它时，可以通过调用 <code>release</code> 方法将其释放。</li></ul></li></ul></details><h3 id="❓nsmutablearray-是怎么实现的-做增删操作的时候内存空间是怎么改变的-可以用别的方法实现吗" tabindex="-1"><a class="header-anchor" href="#❓nsmutablearray-是怎么实现的-做增删操作的时候内存空间是怎么改变的-可以用别的方法实现吗" aria-hidden="true">#</a> ❓<code>NSMutableArray</code> 是怎么实现的，做增删操作的时候内存空间是怎么改变的，可以用别的方法实现吗？</h3><details class="hint-container details"><summary>💡</summary><p><code>NSMutableArray</code> 的实现是由 Apple 的 Foundation 框架完成的，它是一个动态数组，可以增加和删除元素。具体的内部实现细节 Apple 没有公开，但我们可以推测它的内部工作原理是类似以下方式：</p><ol><li><p>在创建 <code>NSMutableArray</code> 时，会分配一段连续的内存空间，用来保存元素。当元素数量超过数组的容量时，Foundation 框架会创建一个新的、更大的数组，然后将旧数组的元素拷贝到新数组中，然后释放旧数组。</p></li><li><p>当从 <code>NSMutableArray</code> 中删除元素时，它并不会立即收缩内存空间。相反，被删除元素的位置会被标记为空闲，可以用来保存新添加的元素。只有当数组的空闲空间达到一定的阈值时，Foundation 框架才会创建一个新的、更小的数组，然后将旧数组的元素拷贝到新数组中，然后释放旧数组。</p></li></ol><p>关于第二个问题，肯定可以用别的方法来实现一个动态数组。例如，我们可以用 C 语言的动态内存分配函数 <code>malloc</code>、<code>realloc</code> 和 <code>free</code> 来实现一个动态数组。另外，我们也可以使用链表数据结构来实现类似 <code>NSMutableArray</code> 的功能，但是链表的随机访问性能较差，不适合需要频繁随机访问元素的场景。</p></details><hr><h2 id="nsdictionary" tabindex="-1"><a class="header-anchor" href="#nsdictionary" aria-hidden="true">#</a> NSDictionary</h2><h3 id="❓nsdictionary-大致实现原理" tabindex="-1"><a class="header-anchor" href="#❓nsdictionary-大致实现原理" aria-hidden="true">#</a> ❓<code>NSDictionary</code> 大致实现原理</h3><details class="hint-container details"><summary>💡</summary><p><code>NSDictionary</code> 是 Objective-C 中一个非常重要的类，它提供了一个以键值对形式存储对象的接口，是一种哈希表实现。</p><p>尽管 Foundation 框架的具体实现没有公开，但我们可以推测一下它的实现原理：</p><ol><li><p>哈希表：在哈希表中，哈希函数将每个键 (key) 映射到表中一个唯一的索引。通过该索引，存储的相关值 (value) 可以快速地被找到。这就是 <code>NSDictionary</code> 可以快速查找键对应值的原因。</p></li><li><p>碰撞处理：当两个或者更多的键映射到哈希表的同一个位置时，就会发生碰撞。通常情况下，有几种策略可以处理这种碰撞，比如链地址法，开放地址法等。不过具体 Foundation 使用哪种策略去处理碰撞我们并不清楚。</p></li><li><p>动态调整：随着数据的增加，为了保持操作的高效，哈希表可能需要进行扩容。扩容经常伴随着数据的重新哈希，为的就是保持哈希值均匀分布，保持优秀的查找效率。</p></li></ol><p>注意，以上都是关于哈希表的一般原理，对 <code>NSDictionary</code> 的具体实现来说只是推测。如果你想深入了解哈希表的实现原理，可能需要参考计算机科学的相关资料或教程进一步学习。</p></details><h3 id="❓nsdictionary-vs-nshashtable-vs-nsmaptable" tabindex="-1"><a class="header-anchor" href="#❓nsdictionary-vs-nshashtable-vs-nsmaptable" aria-hidden="true">#</a> ❓<code>NSDictionary</code> vs <code>NSHashTable</code> vs <code>NSMapTable</code></h3><details class="hint-container details"><summary>💡</summary><p><code>NSDictionary</code>，<code>NSHashTable</code> 和 <code>NSMapTable</code> 都是 Objective-C 中常用的集合类型，各有其特性和适用场景：</p><ol><li><p><code>NSDictionary</code>：存储键值对的集合，每个键都映射到一个值。对键进行<code>copy</code>操作，对值进行<code>retain</code>(强引用)操作。键必须要保持唯一。查找操作性能高效，时间复杂性为O(1)。</p></li><li><p><code>NSHashTable</code>：一个可以存储任意对象的集合，集合中的每个对象都是唯一的。该类提供了对对象强引用、弱引用的选项，可以更加灵活地进行内存管理。</p></li><li><p><code>NSMapTable</code>：和 <code>NSDictionary</code> 非常相似，但 <code>NSMapTable</code> 对键和值可以选择强引用或弱引用，比 <code>NSDictionary</code> 更加灵活。另外，<code>NSMapTable</code> 允许键和值为<code>nil</code>，而 <code>NSDictionary</code> 则不允许。</p></li></ol><p>在考虑使用哪一个时，视具体需求而定：</p><ul><li><p>当你需要按照键查找值，且对键和值的生命周期没有特殊要求时，使用 <code>NSDictionary</code>。</p></li><li><p>当你需要存储一组唯一对象，并且想要对对象的生命周期有更细粒度的控制时，使用 <code>NSHashTable</code>。</p></li><li><p>当你需要存储键值对对象，并且想要对键和值的生命周期有细粒度的控制，或者你需要在集合中存储<code>nil</code>对象时，使用 <code>NSMapTable</code>。</p></li></ul></details><blockquote><p>分别有哪些使用场景？</p></blockquote><details class="hint-container details"><summary>💡</summary><ol><li><p><code>NSDictionary</code>：<code>NSDictionary</code> 由于其高效的查找速度，广泛用于需要依据键查找值的场景。例如，配置文件的存储和读取，网络请求的参数封装，JSON 数据的解析等。</p></li><li><p><code>NSHashTable</code>：<code>NSHashTable</code> 适用于存储不需要重复的对象，并且对内存管理有一定要求的场景。例如，在缓存中存储对象但不引入强引用，防止内存无法释放的问题；或者在存储 delegate 对象时，避免引入循环引用。</p></li><li><p><code>NSMapTable</code>：<code>NSMapTable</code> 可以实现强引用和弱引用键值对，所以适用于一些特殊场景。例如，你想在不引入强引用的情况下缓存对象，或者你希望跟踪哪些对象引用了其它对象等。</p></li></ol><p>在实际开发中，正确地选择并使用这些集合可以帮助你更好地管理内存和提高程序性能。</p></details><h3 id="❓nsdictionary-vs-nscache" tabindex="-1"><a class="header-anchor" href="#❓nsdictionary-vs-nscache" aria-hidden="true">#</a> ❓<code>NSDictionary</code> vs <code>NSCache</code></h3><details class="hint-container details"><summary>💡</summary><p><code>NSDictionary</code> 和 <code>NSCache</code> 都是能够存储键值对的集合，但它们有一些主要的区别，使它们适用于不同的使用场景：</p><ol><li><p><code>NSDictionary</code> 是一个不可变的哈希表，其中的键值对都是强引用的。键是唯一的且不可变的。相比之下，<code>NSCache</code> 是一个可变的集合，其中的键值对都是强引用的，但有一个例外：键不是像 <code>NSDictionary</code> 管理那样通过 <code>copy</code> 管理，而是通过 <code>retain</code> 管理，因此键可以是可变的。</p></li><li><p>另一个重要的区别在于内存管理。<code>NSCache</code> 在系统内存不足时，会自动释放缓存的部分或全部数据以节省内存。这使得在处理大量数据时，<code>NSCache</code> 可以自动处理低内存警告，而 <code>NSDictionary</code> 则需要手动处理低内存警告。</p></li><li><p><code>NSCache</code> 支持通过 <code>NSDiscardableContent</code> 协议来控制缓存内容的生命周期。这样的对象在被缓存时，可以增加一个 <code>beginContentAccess</code> 之后配对的 <code>endContentAccess</code> 调用计数器。当对象的计数器为 0 时，那么这个对象就有可能被清出缓存。</p></li></ol><p>综上所述，<code>NSDictionary</code> 和 <code>NSCache</code> 的不同主要在于内存管理和可变性。实际上，<code>NSCache</code> 更类似一个优化了内存管理的 <code>NSMutableDictionary</code>，更适合用于存储大量会占据大量内存的对象，例如图片等。</p></details><blockquote><p><code>NSCache</code> 中缓存的对象，在什么时候被释放？</p></blockquote><details class="hint-container details"><summary>💡</summary><p><code>NSCache</code> 中缓存的对象被释放的时机取决于几个因素：</p><ol><li><p>内存压力：当操作系统触发低内存警告时，<code>NSCache</code> 会自动释放一些缓存的对象以节约内存。</p></li><li><p>缓存某个对象的 <code>NSCache</code> 实例被销毁时，该对象会被释放。</p></li><li><p>当缓存达到其极限时（这取决于你设置的缓存的数量和总成本的上限），<code>NSCache</code> 可能会开始移除一些对象。</p></li><li><p>如果缓存的对象实现了 <code>NSDiscardableContent</code> 协议，那么对象的释放还会取决于它的 <code>beginContentAccess</code> 和 <code>endContentAccess</code> 方法的调用计数。当这个计数为 0 并且内存压力较大时，对象可能被释放。</p></li></ol><p>这样的设计使 <code>NSCache</code> 非常适合用于缓存大量占用内存的对象（例如图片等）。因为它的自动内存管理机制可以防止应用因为低内存压力而崩溃，同时还能确保缓存的有效利用。</p></details><h3 id="❓isequal-和-hash-的关系" tabindex="-1"><a class="header-anchor" href="#❓isequal-和-hash-的关系" aria-hidden="true">#</a> ❓<code>isEqual</code> 和 <code>hash</code> 的关系？</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，<code>isEqual:</code> 和 <code>hash</code> 两个方法在很多场合都需要同时使用，比如在对自定义对象进行集合操作（存入 <code>NSSet</code>，作为 <code>NSDictionary</code> 的 Key 等）时。它们的关系可以这样理解：</p><ol><li><p><code>isEqual:</code> 方法是用来判断两个对象是否相等。如何判断对象的相等性，你可以在自定义类中重写这个方法来自定义判断规则。</p></li><li><p><code>hash</code> 方法返回一个无符号整数（<code>NSUInteger</code>）的哈希值，这个值被用于 UITableView、NSSet、NSDictionary 等集合类的操作以提升性能。对象的哈希值主要是通过对象的内容进行计算得出。</p></li><li><p><code>isEqual:</code>方法和 <code>hash</code> 方法需要保持一致，意思是当两个对象 <code>isEqual</code> 的返回值相等时，它们的 <code>hash</code> 方法返回值必须也相等。反过来并不一定，即如果两个对象的 <code>hash</code> 值相等，并不一定要求他们 <code>isEqual</code> 值也一定相等，因为不同的对象有可能产生相同的哈希值（通常被称为哈希冲突）。</p></li></ol><p>总结来说，如果你需要自定义对象及其相等性的规则，那么通常需要同时重载 <code>isEqual:</code> 和 <code>hash</code> 方法，并确保它们的行为是一致的。</p></details><hr><h2 id="拷贝" tabindex="-1"><a class="header-anchor" href="#拷贝" aria-hidden="true">#</a> 拷贝</h2><h3 id="❓copy-和-mutablecopy-的区别" tabindex="-1"><a class="header-anchor" href="#❓copy-和-mutablecopy-的区别" aria-hidden="true">#</a> ❓<code>copy</code> 和 <code>mutableCopy</code> 的区别？</h3><details class="hint-container details"><summary>💡</summary><p><code>copy</code> 和 <code>mutableCopy</code> 是 <code>Foundation</code> 框架 <code>NSObject</code> 类中定义的方法，被用来创建对象的拷贝，它们的区别如下：</p><ol><li><p><code>copy</code>：执行的是浅拷贝（也叫做指针拷贝），拷贝后的对象类型是不可变的。这意味着在实际操作中，<code>copy</code> 并没有开辟新的内存空间，而是拷贝对象持有的指针。因此原始对象和拷贝对象指向相同的内存地址。</p></li><li><p><code>mutableCopy</code>：执行的是深拷贝（也叫做内容拷贝），拷贝后的对象类型是可变的。这意味着在实际操作中，<code>mutableCopy</code> 会开辟一块新的内存空间，拷贝对象的值到这块新的内存空间。</p></li></ol><p>主要的差异是拷贝出来的对象可变性和持有数据的方式（指针拷贝或者内容拷贝）。需要注意的是，浅拷贝并不意味着拷贝出来的对象一定是不可变的，具体还需要看拷贝的源对象是什么类型。如果拷贝的源对象是不可变的，那么 <code>copy</code> 方法拷贝出来的对象也是不可变的；如果拷贝的源对象是可变的，那么 <code>copy</code> 方法拷贝出来的对象就是可变的。</p></details><h3 id="❓自定义类如何实现-copy-功能" tabindex="-1"><a class="header-anchor" href="#❓自定义类如何实现-copy-功能" aria-hidden="true">#</a> ❓自定义类如何实现 <code>copy</code> 功能？</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，如果你的类需要支持拷贝功能，那么你需要声明并实现 NSCopying 或 NSMutableCopying 协议。这通常涉及重写 <code>copyWithZone:</code> 方法。</p><p>以一个简单的自定义类 <code>Person</code> 为例，假设它有两个属性 <code>name</code> 和 <code>age</code>：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> Person <span class="token punctuation">:</span> NSObject <span class="token operator">&lt;</span>NSCopying<span class="token operator">&gt;</span>
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> copy<span class="token punctuation">)</span> NSString <span class="token operator">*</span>name<span class="token punctuation">;</span>
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> assign<span class="token punctuation">)</span> NSUInteger age<span class="token punctuation">;</span>
<span class="token keyword">@end</span>
 
<span class="token keyword">@implementation</span> Person
<span class="token operator">-</span> <span class="token punctuation">(</span>id<span class="token punctuation">)</span>copyWithZone<span class="token punctuation">:</span><span class="token punctuation">(</span>NSZone <span class="token operator">*</span><span class="token punctuation">)</span>zone <span class="token punctuation">{</span>
    Person <span class="token operator">*</span>copyPerson <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token keyword">self</span> class<span class="token punctuation">]</span> allocWithZone<span class="token punctuation">:</span>zone<span class="token punctuation">]</span>init<span class="token punctuation">]</span><span class="token punctuation">;</span>
    copyPerson<span class="token punctuation">.</span>name <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token keyword">self</span><span class="token punctuation">.</span>name copyWithZone<span class="token punctuation">:</span>zone<span class="token punctuation">]</span><span class="token punctuation">;</span>
    copyPerson<span class="token punctuation">.</span>age <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">.</span>age<span class="token punctuation">;</span>
    <span class="token keyword">return</span> copyPerson<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这样，通过 [xx copy] 的语法就可以获取到 <code>Person</code> 的拷贝了。其中，<code>copyWithZone:</code> 方法创建了一个新的实例，然后复制了 <code>name</code> 和 <code>age</code> 属性。</p></details><h3 id="❓自定义类如何实现-mutablecopy-功能" tabindex="-1"><a class="header-anchor" href="#❓自定义类如何实现-mutablecopy-功能" aria-hidden="true">#</a> ❓自定义类如何实现 <code>mutableCopy</code> 功能？</h3><details class="hint-container details"><summary>💡</summary><p>我们以一个Person类为例，如果需要自定义类型实现mutableCopy，需要遵循<code>NSMutableCopying</code>协议并实现<code>mutableCopyWithZone:</code>方法。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> Person <span class="token punctuation">:</span> NSObject <span class="token operator">&lt;</span>NSMutableCopying<span class="token operator">&gt;</span>
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> copy<span class="token punctuation">)</span> NSString <span class="token operator">*</span>name<span class="token punctuation">;</span>
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> assign<span class="token punctuation">)</span> NSUInteger age<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> Person
<span class="token operator">-</span> <span class="token punctuation">(</span>id<span class="token punctuation">)</span>mutableCopyWithZone<span class="token punctuation">:</span><span class="token punctuation">(</span>NSZone <span class="token operator">*</span><span class="token punctuation">)</span>zone <span class="token punctuation">{</span>
    Person <span class="token operator">*</span>copyPerson <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token keyword">self</span> class<span class="token punctuation">]</span> allocWithZone<span class="token punctuation">:</span>zone<span class="token punctuation">]</span>init<span class="token punctuation">]</span><span class="token punctuation">;</span>
    copyPerson<span class="token punctuation">.</span>name <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token keyword">self</span><span class="token punctuation">.</span>name mutableCopyWithZone<span class="token punctuation">:</span>zone<span class="token punctuation">]</span><span class="token punctuation">;</span> 
    copyPerson<span class="token punctuation">.</span>age <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">.</span>age<span class="token punctuation">;</span>  
    <span class="token keyword">return</span> copyPerson<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个示例中，我们声明Person类遵循NSMutableCopying协议，并实现其<code>mutableCopyWithZone:</code>方法。在该方法中，我们创建一个新的Person实例，然后逐个将原始对象的属性值复制到新的实例上。对于<code>name</code>属性，我们调用其<code>mutableCopyWithZone:</code>方法来得到一个可变副本。而对于<code>age</code>属性，由于它是一个基本数据类型，我们直接赋值即可。</p><p>记住，<code>mutableCopyWithZone:</code>方法返回的应该是一个全新的实例，而不应该是原始对象自身，因为这样才是deep copy, 克隆出来的对象和原对象没有任何影响。</p><p>同样的，我们还可将Person类遵循NSCopying协议，并实现其<code>copyWithZone:</code>方法，这样Person类就可以支持<code>mutableCopy</code>和<code>copy</code>操作了。</p></details><h3 id="❓如何重写带-copy-关键字的-setter" tabindex="-1"><a class="header-anchor" href="#❓如何重写带-copy-关键字的-setter" aria-hidden="true">#</a> ❓如何重写带 <code>copy</code> 关键字的 <code>setter</code>？</h3><details class="hint-container details"><summary>💡</summary><p>在Objective-C中，如果想要重写带<code>copy</code>关键字的<code>setter</code>方法，你可以这样进行：</p><p>首先我们假设有一个属性：<code>@property (nonatomic, copy) NSString *name;</code></p><p>其重写的<code>setter</code>方法将会是这样：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>setName<span class="token punctuation">:</span><span class="token punctuation">(</span>NSString <span class="token operator">*</span><span class="token punctuation">)</span>newName <span class="token punctuation">{</span>
    <span class="token keyword">if</span><span class="token punctuation">(</span>_name <span class="token operator">!=</span> newName<span class="token punctuation">)</span> <span class="token punctuation">{</span>
       <span class="token punctuation">[</span>_name release<span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// MRC 下</span>
       _name <span class="token operator">=</span> <span class="token punctuation">[</span>newName copy<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个例子中，<code>if(_name != newName)</code>是为了防止传入的新值和旧值是同一个对象，如果是同一个对象而直接release掉，那么接下来进行copy操作就可能会出现问题。</p><p>接着<code>[_name release]</code>是为了释放掉旧值对象，因为重写setter通常是在改变属性值时进行的。</p><p>最后<code>_name = [newName copy]</code>是为了对新值对象进行copy操作，这样就能保证_name属性始终持有一个对应字符串的copy。</p><p>记住，在你释放旧值前，要先将新值copy或retain了，因为如果新值和旧值是同一个对象的话，你释放了旧值也就意味着释放了新值，这可能会导致unpredictable的后果。</p></details><h3 id="❓-property-copy-nsmutablearray-array-这个写法正确吗" tabindex="-1"><a class="header-anchor" href="#❓-property-copy-nsmutablearray-array-这个写法正确吗" aria-hidden="true">#</a> ❓<code>@property (copy) NSMutableArray *array;</code> 这个写法正确吗？</h3><details class="hint-container details"><summary>💡</summary><p>这个写法可以运行通过，但从代码规范和最佳实践来说，这并不是一个好的写法。</p><p>当你给一个属性添加 <code>copy</code> 标识时，这个属性的 <code>setter</code> 将会使用传入对象的 <code>copy</code> 方法来创建一个新的对象。然而，在 Objective-C 中，<code>copy</code> 方法对 <code>NSMutableArray</code> 实例调用将返回一个 <code>NSArray</code> 实例。这是由于 <code>copy</code> 策略总是返回一个不可变的副本。</p><p>所以，如果你用 <code>@property (copy) NSMutableArray *array;</code> 这个写法，实际上你得到的 <code>array</code> 是一个不可变数组 <code>NSArray</code>。当你在后续代码中尝试向这个“可变数组”(<code>NSMutableArray</code>) 中添加或删除元素时，将会导致运行时错误。</p><p>如果你需要属性为一个可变数组并支持 <code>copy</code> 行为，你需要手动实现属性的 <code>getter</code>和<code>setter</code>方法，并在<code>setter</code>方法中使用 <code>mutableCopy</code> 进行拷贝。</p><p>下面是一个例子来说明如何操作：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> strong<span class="token punctuation">)</span> NSMutableArray <span class="token operator">*</span>array<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> MyClass

<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>setArray<span class="token punctuation">:</span><span class="token punctuation">(</span>NSMutableArray <span class="token operator">*</span><span class="token punctuation">)</span>newArray <span class="token punctuation">{</span>
    _array <span class="token operator">=</span> <span class="token punctuation">[</span>newArray mutableCopy<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这样你可以保证每次设置新数组时都创建一个可变副本，而不是指向原始数组。</p></details><h3 id="❓为什么不可变对象要用-copy" tabindex="-1"><a class="header-anchor" href="#❓为什么不可变对象要用-copy" aria-hidden="true">#</a> ❓为什么不可变对象要用 <code>copy</code>？</h3><details class="hint-container details"><summary>💡</summary><p>第一，使用 <code>copy</code> 可以防止对象在外部被意外修改：</p><p>设想这样一种情况，你有一个类，这个类有一个公共的 NSString 属性，你希望这个属性的值在你设置后就不会被改变，如果你将该属性声明为 <code>strong</code> 属性，这个属性就可以被外部改变。例如：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">self</span><span class="token punctuation">.</span>myObject<span class="token punctuation">.</span>name <span class="token operator">=</span> <span class="token string">@&quot;Original&quot;</span><span class="token punctuation">;</span>
NSMutableString <span class="token operator">*</span>nameString <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableString stringWithString<span class="token punctuation">:</span><span class="token string">@&quot;Change&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">self</span><span class="token punctuation">.</span>myObject<span class="token punctuation">.</span>name <span class="token operator">=</span> nameString<span class="token punctuation">;</span>
<span class="token punctuation">[</span>nameString appendString<span class="token punctuation">:</span><span class="token string">@&quot;Add&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>

<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;Name: %@&quot;</span><span class="token punctuation">,</span> <span class="token keyword">self</span><span class="token punctuation">.</span>myObject<span class="token punctuation">.</span>name<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>上述代码将打印 &quot;ChangeAdd&quot;，因为 nameString 与 myObject.name 指向的是同一个对象。为了防止可能的外部访问改变我们的对象，我们应该创建一份独立的副本，这可以通过声明该属性为 <code>copy</code> 来实现。</p><p>第二，某些具有父子关系的类，子类对象赋值给用 <code>copy</code> 修饰的父类变量后，父类变量会变为原来的父类特性(里氏替换)。如：</p><p>假设你将一个 NSMutableString 实例赋值给一个类型为 NSString 且用 copy 修饰的变量，那么这个变量就不再有 NSMutableString 的特性，而只有 NSString 的特性。所以从某种意义上说，用 copy 也是为了类型安全。</p></details><hr><h2 id="属性" tabindex="-1"><a class="header-anchor" href="#属性" aria-hidden="true">#</a> 属性</h2><h3 id="❓-property-属性是什么" tabindex="-1"><a class="header-anchor" href="#❓-property-属性是什么" aria-hidden="true">#</a> ❓<code>@property</code> 属性是什么？</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>@property</code> 是 <code>Objective-C</code> 中的一个关键字，用于声明一个属性。主要作用是为类的实例变量（属性）生成对应的访问器方法，即 <code>getter</code> 方法和 <code>setter</code> 方法。</p></blockquote><p><code>@property</code> 访问器</p><ul><li>原子性 : <code>atomic</code>、<code>nonatomic</code></li><li>读写性 : <code>readwrite</code>、<code>readonly</code></li><li>内存管理 : <code>assign</code>、<code>strong</code>、<code>weak</code>、<code>copy</code></li></ul><p><code>@property</code> 优点</p><ul><li>隔离性：可以封装实例变量的直接访问，将类的内部实现与外部接口隔离，提高数据的安全性。</li><li>可维护性：可以在 <code>getter</code> 和 <code>setter</code> 中添加特定的逻辑如设置新值时进行验证，获取值时返回一个计算后的值等，提高代码的可维护性。</li></ul></details><h3 id="❓-property-的默认关键字" tabindex="-1"><a class="header-anchor" href="#❓-property-的默认关键字" aria-hidden="true">#</a> ❓<code>@property</code> 的默认关键字？</h3><details class="hint-container details"><summary>💡</summary><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// 基本数据类型 =&gt; 默认是: atomic, readwrite, assign</span>
<span class="token keyword">@property</span> <span class="token keyword">int</span> num<span class="token punctuation">;</span>
<span class="token comment">// 对象数据类型 =&gt; 默认是: atomic, readwrite, strong</span>
<span class="token keyword">@property</span> NSObject <span class="token operator">*</span>obj<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓-property-的底层实现" tabindex="-1"><a class="header-anchor" href="#❓-property-的底层实现" aria-hidden="true">#</a> ❓<code>@property</code> 的底层实现？</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>@property</code> 是一个关键字，用于声明类的属性。声明一个属性时，编译器会自动生成一些代码，这就是 <code>@property</code> 的底层实现。</p></blockquote><ul><li><p>实例变量：<code>@property</code> 在默认情况下会自动合成一个带下划线前缀的实例变量。例如声明了一个名为 <code>foo</code> 的属性，编译器会自动生成一个名为 <code>_foo</code> 的实例变量。</p></li><li><p><code>getter</code> 和 <code>setter</code> 方法：会为属性自动生成 <code>getter</code> 和 <code>setter</code> 方法。</p></li><li><p>内存管理：指定的属性修饰符（<code>assign</code>、<code>strong</code>、<code>weak</code>、<code>copy</code>、<code>retain</code> 等），编译器会在 <code>setter</code> 方法中生成相应的内存管理代码，比如引用计数的增加和减小，拷贝对象等。</p></li><li><p><code>KVO</code> 支持：<code>@property</code> 自动生成的 <code>getter</code> 和 <code>setter</code> 方法支持键值观察(<code>Key-Value Observing</code>)，允许其它对象观察和响应属性值的改变。</p><blockquote><p>runtime 层面</p></blockquote><p>声明 <code>@property</code> 后，编译器会根据属性的特性在 <code>class_rw_t</code> 结构中的 <code>methods</code> 和 <code>properties</code> 中生成 <code>getter</code> 和 <code>setter</code> 方法以及相应的属性。然后在运行时，这些 <code>getter</code> 和 <code>setter</code> 方法以及属性被引入到 <code>objc_class</code> 的结构中。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">struct</span> objc_class <span class="token punctuation">{</span>
    Class isa<span class="token punctuation">;</span>              <span class="token comment">// metaclass 的指针</span>
    Class super_class<span class="token punctuation">;</span>      <span class="token comment">// 父类</span>
    cache_t cache<span class="token punctuation">;</span>          <span class="token comment">// 缓存</span>
    class_data_bits_t bits<span class="token punctuation">;</span> <span class="token comment">// 类的相关信息，包含了一个指向 class_rw_t 数据结构的指针</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token comment">// 可读写结构，存储运行时添加的类信息</span>
<span class="token keyword">struct</span> class_rw_t <span class="token punctuation">{</span>
    uint32_t flags<span class="token punctuation">;</span>
    uint32_t version<span class="token punctuation">;</span>
    <span class="token keyword">const</span> class_ro_t <span class="token operator">*</span>ro<span class="token punctuation">;</span>           <span class="token comment">// 只读结构体，保存编译时确定的类信息</span>
    method_array_t methods<span class="token punctuation">;</span>         <span class="token comment">// 方法列表</span>
    property_array_t properties<span class="token punctuation">;</span>    <span class="token comment">// 属性列表</span>
    protocol_array_t protocols<span class="token punctuation">;</span>     <span class="token comment">// 协议列表</span>
    Class firstSubclass<span class="token punctuation">;</span>
    Class nextSiblingClass<span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul></details><h3 id="❓assign、strong、weak、copy-区别" tabindex="-1"><a class="header-anchor" href="#❓assign、strong、weak、copy-区别" aria-hidden="true">#</a> ❓<code>assign</code>、<code>strong</code>、<code>weak</code>、<code>copy</code> 区别？</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>assign</code>：是赋值特性，简单设置变量，不更改引用计数。</p><blockquote><p>主要用在基本的数据类型（如 int、float）或 C 数据类型（如 结构体、C 数组）而不是对象。</p></blockquote></li><li><p><code>strong</code>（类似于：retain）：是所有权（owning）特性。</p><blockquote><p>采用 <code>ARC</code>（自动引用计数）环境下，编译器会自动在设置方法中插入 <code>retain</code> 调用，使对象的引用计数增加，从而表示当前对象拥有数据值。</p></blockquote></li><li><p><code>weak</code>：是非拥有关系（non-owning）特性，即弱引用。</p><blockquote><p>当用 <code>weak</code> 修饰对象时，被指向的对象引用计数不会增加，更重要的是，当对象被释放时，被 <code>weak</code> 修饰的变量会自动被置为 <code>nil</code>，防止野指针的出现。<code>weak</code> 修饰的属性适合在已经存在对对象的 <code>strong</code> 引用，或者对象之间存在相互循环引用的情况下使用。</p></blockquote></li><li><p><code>copy</code>：是拷贝特性。</p><blockquote><p>适合 <code>NSString</code>、<code>NSArray</code>、<code>NSDictionary</code> 等不可变对象。最重要的一点是：使用 <code>copy</code> 保持封装性，对象在内部可能需要一个数据的完整副本，并且把这个副本给外界使用，防止作为接口提供的对象的初始数据被改变。</p></blockquote></li></ul><p>选择哪种属性修饰符要视情况而定，如果数据是不可变的，那么选择 <code>assign</code> 或 <code>copy</code> 较好；如果数据是可变的，可以选择 <code>strong</code> 或 <code>weak</code>。</p></details><h3 id="❓nsstring-为什么使用-copy-可以使用-strong-吗" tabindex="-1"><a class="header-anchor" href="#❓nsstring-为什么使用-copy-可以使用-strong-吗" aria-hidden="true">#</a> ❓<code>NSString</code> 为什么使用 <code>copy</code>，可以使用 <code>strong</code> 吗？</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>NSString</code> 使用 <code>copy</code> 是为了防止在赋值可变字符串时由于原字符串发生改变而引发非预期问题。</p></blockquote><ul><li><p><code>NSString</code> 的子类 <code>NSMutableString</code> 是可以变化的。如果使用 <code>strong</code> 并把一个 <code>NSMutableString</code> 实例赋值给 <code>NSString</code> 对象，然后改变该<code>NSMutableString</code> 实例，那么 <code>NSString</code> 实例也会随之改变。</p></li><li><p>如果使用 <code>copy</code>，当一个 <code>NSMutableString</code> 实例赋值给 <code>NSString</code> 对象时，实际上会复制一个不可变的副本。这样即使原字符串改变 <code>NSString</code> 实例也不会受影响。</p></li></ul></details><h3 id="❓assign-可以用于-oc-对象吗" tabindex="-1"><a class="header-anchor" href="#❓assign-可以用于-oc-对象吗" aria-hidden="true">#</a> ❓<code>assign</code> 可以用于 <code>OC</code> 对象吗？</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>assign</code> 是一种属性修饰符，用于指定属性的赋值语义。<code>assign</code> 可以用于任何数据类型，包括 <code>Objective-C</code> 对象。但是，对于对象来说不推荐使用 <code>assign</code>。</p></blockquote><p>将 <code>assign</code> 用于 <code>Objective-C </code>对象时，其会生成一个设置器，其行为将传入的值赋值给实例变量，并不改变该对象的引用计数。如果该对象在其它地方被释放，那么这个引用就成了一个悬挂指针（或者说“野指针”），这将会引起程序在调用该对象时崩溃。</p><blockquote><p>在某些特定场景下，可能会选择使用 <code>assign</code>。例如，当需要创建一个 “弱引用并且不希望在对象被释放时自动设置为 <code>nil</code>” 时，<code>assign</code> 就会是个不错的选择，但是需要确保不会发生悬挂指针。</p></blockquote></details><h3 id="❓nonnull-用过吗" tabindex="-1"><a class="header-anchor" href="#❓nonnull-用过吗" aria-hidden="true">#</a> ❓<code>nonnull</code> 用过吗</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>nonnull</code> 关键字在 <code>Objective-C</code> 中的出现是为了更好地与 <code>Swift</code> 兼容。</p></blockquote></details><h3 id="❓-synthesize-vs-dynamic" tabindex="-1"><a class="header-anchor" href="#❓-synthesize-vs-dynamic" aria-hidden="true">#</a> ❓<code>@synthesize</code> vs <code>@dynamic</code></h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>@synthesize</code>：告诉编译器编译时自动创建 <code>getter/setter</code> 方法，如果没有手动实现的话。</p></li><li><p><code>@dynamic</code>：这个关键字告诉编译器,属性的 <code>getter</code> 和 <code>setter</code> 方法由自己实现，不自动生成（当然对于<code>@dynamic var</code>，编译器还是会默认添加其成员变量<code>_var</code>），如果没有手动实现 <code>setter和getter</code>，那么程序运行时，编译器发现找不到 <code>setter/getter</code> 就会崩溃。好处是可以在运行时动态绑定对象。</p></li></ul><blockquote><p><code>@dynamic</code>常常和运行时函数 <code>class_addMethod</code> 配合使用，可以动态添加方法。最常见的应用是在 <code>Core Data</code> 的 <code>NSManagedObject</code> 子类中，<code>Core Data</code> 的属性经常被设置为 <code>@dynamic</code>，<code>NSManagedObject</code> 利用 <code>Core Data</code> 的底层存储访问这些属性，以便将对象的实际数据保存在磁盘上，而不是内存中。</p></blockquote></details><h3 id="❓-synthesize-合成实例变量的规则是什么-如果属性名为-foo-存在一个名为-foo-的实例变量-还会自动合成新变量么" tabindex="-1"><a class="header-anchor" href="#❓-synthesize-合成实例变量的规则是什么-如果属性名为-foo-存在一个名为-foo-的实例变量-还会自动合成新变量么" aria-hidden="true">#</a> ❓<code>@synthesize</code> 合成实例变量的规则是什么？如果属性名为 <code>foo</code>，存在一个名为 <code>_foo</code> 的实例变量，还会自动合成新变量么？</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>@synthesize</code> 关键字用于手动指定需要 <code>@property</code> 属性对应的成员变量名称。</p></blockquote><p>属性名为 <code>foo</code>，同时已经实现了一个名为 <code>_foo</code> 的实例变量，那么在默认情况下（也就是你没有使用 <code>@synthesize</code> 关键字来手动指定成员变量名），编译器不会再合成新的实例变量。因为编译器默认的命名规则是在属性名前加<code>_</code>作为成员变量名，如果已经存在这样的成员变量，编译器就不会再创建新的变量。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> strong<span class="token punctuation">)</span> id foo<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> MyClass <span class="token punctuation">{</span>
    id _foo<span class="token punctuation">;</span> <span class="token comment">// 不会再合成新的实例变量</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>使用了 <code>@synthesize</code> 来指定了一个新的名称，那么编译器便会按照指定名称创建新的实例变量。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@implementation</span> MyClass
<span class="token keyword">@synthesize</span> foo <span class="token operator">=</span> myFoo<span class="token punctuation">;</span> <span class="token comment">// 创建一个名为 myFoo 的实例变量</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓-synthesize-都有哪些使用场景" tabindex="-1"><a class="header-anchor" href="#❓-synthesize-都有哪些使用场景" aria-hidden="true">#</a> ❓<code>@synthesize</code> 都有哪些使用场景？</h3><details class="hint-container details"><summary>💡</summary><ul><li><p>设置实例变量名。<code>@synthesize</code>关键字允许手动改变自动生成的实例变量的名称。</p></li><li><p>协议属性自动合成。实现协议时，为了避免重复编写代码，可以使用 <code>@synthesize</code> 来自动合成协议中定义的属性。</p></li><li><p>不生成实例变量。当在类扩展 (<code>@interface MyClass()</code>) 或者父类中已经声明了属性的实例变量 <code>_foo</code>，此时在子类中如果 <code>@synthesize foo;</code>，则不会生成 <code>foo</code> 对应的实例变量。</p></li><li><p>生成实例变量。当在头文件或者类扩展（<code>@interface MyClass()</code>）中声明 <code>@property</code> 属性但没有 <code>@synthesize foo;</code>，则会自动生成 <code>_foo</code> 实例变量。如果在 <code>.m</code> 文件中声明 <code>@property</code> 属性但没有 <code>@synthesize foo;</code>，则不会自动合成属性的实例变量。</p><blockquote><p>在 <code>Xcode 4.4</code> 及以上版本中，默认已经开启了 <code>@synthesize</code> 自动生成，已经不需要显式地使用 <code>@synthesize</code>，除非在某些特殊情况下需要手动指定实例变量的名字。</p></blockquote></li></ul></details><hr><h2 id="block" tabindex="-1"><a class="header-anchor" href="#block" aria-hidden="true">#</a> Block</h2><h3 id="❓block-vs-delegate" tabindex="-1"><a class="header-anchor" href="#❓block-vs-delegate" aria-hidden="true">#</a> ❓<code>block</code> vs <code>Delegate</code></h3><details class="hint-container details"><summary>💡</summary><p>Block和代理（Delegate）都是iOS开发中用于实现逻辑回调的常用方式，但他们之间存在一些区别。</p><ol><li><p>定义方式：Block是一种特殊的数据类型，可以将一段代码封装到一个变量中，然后在需要的时候调用这个变量。而代理是一种设计模式，是通过protocol定义了一系列的方法，然后由其他类去实现这些方法。</p></li><li><p>使用场景：Block适合一些简单的逻辑处理，如动画回调，数组遍历等；代理通常用于处理复杂的交互，如TableView的数据源及代理方法，网络请求的回调等。</p></li><li><p>执行流程：Block在调用时是同一个函数或方法内部的，逻辑关系相对清晰，容易理解；而代理方法可能分散在代码的不同地方，使得调试和阅读代码稍微困难一些。</p></li><li><p>生命周期：Block有可能导致循环引用的问题；而Delegate由于是weak引用，不会出现循环引用的问题。</p></li><li><p>排序：Block可以有多个且同时存在，顺序可以自由改变。而delegate通常只有一个，如果有多个，通常需要其他方式进行排序。</p></li></ol><p>总结来说，具体使用哪种方式，需要根据代码的复杂性，执行环境，对内存管理的要求等因素综合考虑。</p></details><h3 id="❓block-vs-函数指针" tabindex="-1"><a class="header-anchor" href="#❓block-vs-函数指针" aria-hidden="true">#</a> ❓<code>block</code> vs 函数指针</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>block</code>：一种特殊的数据类型，可以存储一段代码，并且这段代码可以在任何时候被执行。可以访问在声明他的那个作用域中定义的变量，这被称为“变量捕获”。</p></li><li><p>函数指针：函数指针是存储着函数地址的指针变量，本质上是一个地址值，也就是函数在内存中的起始地址。函数指针不能像 <code>block</code> 那样访问变量，只能通过传参的方式去使用外部的数据。</p></li></ul></details><h3 id="❓block-什么情况会发生循环引用" tabindex="-1"><a class="header-anchor" href="#❓block-什么情况会发生循环引用" aria-hidden="true">#</a> ❓<code>block</code> 什么情况会发生循环引用？</h3><details class="hint-container details"><summary>💡</summary><p><code>Block</code> 会在以下情况下形成循环引用：</p><p>当在一个 Objective-C 对象的方法（或者属性）中引用了一个 Block，而这个 Block 中又引用了该对象（不论是直接引用还是间接引用）时，就会形成一个循环引用。这是由于 Block 在捕获对象时默认为强引用，而对象本身又强引用了 Block，形成了循环。</p><p>例如，以下代码就存在循环引用：</p><div class="language-objective-c line-numbers-mode" data-ext="objective-c"><pre class="language-objective-c"><code>__strong typeof(self) strongSelf = self;
self.myBlock = ^{
    [strongSelf doSomething];
};
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这段代码中，Block 引用了 self，而 self 又通过 <code>self.myBlock</code> 引用了 Block，从而形成循环引用。</p><p>为了打破这个循环，我们通常采用弱化 self 的方式：</p><div class="language-objective-c line-numbers-mode" data-ext="objective-c"><pre class="language-objective-c"><code>__weak typeof(self) weakSelf = self;
self.myBlock = ^{
    __strong typeof(weakSelf) strongSelf = weakSelf;
    [strongSelf doSomething];
};
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这里通过 <code>__weak typeof(self) weakSelf = self;</code> 创建了一个对 self 的弱引用，然后在 Block 中使用这个弱引用，从而避免了循环引用。但在 Block 中实际使用时，又通过 <code>__strong typeof(weakSelf) strongSelf = weakSelf;</code> 临时创建了一个对 weakSelf 的强引用，确保在 Block 执行过程中不会出现 weakSelf 被意外释放的情况。</p></details><h3 id="❓uiview-动画函数-block-版本是否需要处理循环引用" tabindex="-1"><a class="header-anchor" href="#❓uiview-动画函数-block-版本是否需要处理循环引用" aria-hidden="true">#</a> ❓<code>UIView</code> 动画函数 <code>block</code> 版本是否需要处理循环引用？</h3><details class="hint-container details"><summary>💡</summary><p>在 UIView 的动画函数 block 版本中使用 self，实际上不会造成引用循环。</p><p>因为这些动画 block 只会在动画结束后才被释放，而不是被 self 所拥有。另外，这种动画函数不会保持对动画 block 的长期持有，一般在动画结束后就会将其释放，因此也就不存在循环引用的问题。</p><p>所以在这种情况下，你可以直接使用 self 而不用担心引发循环引用的问题。例如：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token punctuation">[</span>UIView animateWithDuration<span class="token punctuation">:</span><span class="token number">1.0</span> animations<span class="token punctuation">:</span><span class="token operator">^</span><span class="token punctuation">{</span>
    <span class="token keyword">self</span><span class="token punctuation">.</span>myView<span class="token punctuation">.</span>alpha <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓block-底层实现" tabindex="-1"><a class="header-anchor" href="#❓block-底层实现" aria-hidden="true">#</a> ❓<code>block</code> 底层实现</h3>`,101),u={class:"hint-container details"},k=a(`<summary>💡</summary><p>在底层，Block 是一个 Objective-C 对象，它的定义中包含了一个结构体和函数指针。</p><p>一个简化版的 Block 的结构体可以这样描述：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">struct</span> Block_literal <span class="token punctuation">{</span>
    <span class="token keyword">void</span> <span class="token operator">*</span>isa<span class="token punctuation">;</span>
    <span class="token keyword">int</span> flags<span class="token punctuation">;</span>
    <span class="token keyword">int</span> reserved<span class="token punctuation">;</span>
    <span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">*</span>invoke<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span> <span class="token operator">*</span><span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">struct</span> block_descriptor <span class="token operator">*</span>descriptor<span class="token punctuation">;</span>
    <span class="token comment">/* captured variables... */</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这里面的元素分别表示：</p><ul><li><code>isa</code>：指向对象的类型，所有 Objective-C 对象都有该变量。</li><li><code>flags</code> 和 <code>reserved</code>：提供关于 Block 的一些信息，如 Block 类型等。</li><li><code>invoke</code>：一个函数指针，指向进行调用的函数。</li><li><code>descriptor</code>：一个指向 Block_descriptor 结构的指针，其中包含相关信息。</li></ul><p>除此以外，结构体中还包含通过闭包捕获的变量。</p><p>以上是 Block 的简化版的结构体描述，实际上 Block 的底层实现要复杂的多，应用到了大量的内存管理技术。在 Objective-C 中，对 Block 的内存管理方法为 global（全局块）、stack（栈块）和 malloc（堆块）。</p><p>在默认情况下，Objective-C 的编译器会根据情况适当地为 Block 分配内存。当 Block 没有捕获任何外部变量，或者仅使用了全局变量和静态变量时，它是一个 global Block，存储在程序的全局数据区，其生命周期从创建到程序结束。当 Block 使用了外部的自动变量（包括 ObjC 对象和基本数据类型），在未进行 copy 操作的情况下，它是一个 stack Block，存储在栈内存上，超出作用域即被销毁。如果对 Block 进行了 copy 操作，或者是用 __block 修饰的自动变量，那么这个 Block 是一个 malloc Block，存储在堆内存上，需要手动进行内存管理。</p>`,9),m={href:"https://www.mikeash.com/pyblog/friday-qa-2009-08-14-practical-blocks.html",target:"_blank",rel:"noopener noreferrer"},b=a(`<h3 id="❓block-内如何修改外部变量" tabindex="-1"><a class="header-anchor" href="#❓block-内如何修改外部变量" aria-hidden="true">#</a> ❓<code>block</code> 内如何修改外部变量</h3><details class="hint-container details"><summary>💡</summary><p>在Objective-C中，如果你想在 block 内部访问和修改 block 外部的变量，你需要在变量前加上 __block 修饰符，如下：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__block <span class="token keyword">int</span> num <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    num <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>  
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;Num: %d&quot;</span><span class="token punctuation">,</span> num<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 这里输出 &quot;Num: 1&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这里，num 变量前面加上了 __block 修饰符，所以可以在 block 内部被修改。myBlock 被调用后，num 的值被改变为1，因此 NSLog 可以打印出 &quot;Num: 1&quot;。</p><p>如果不加 __block 修饰符，编译器会报错，因为 block 默认捕获变量的值，而不是变量本身，因此不能修改这个变量的值。加了 __block 后，block 将捕获变量本身，使得可以在 block 内部修改该变量。</p></details><h3 id="❓block-在捕获自动变量-局部静态变量-全局变量-全局静态变量有什么区别" tabindex="-1"><a class="header-anchor" href="#❓block-在捕获自动变量-局部静态变量-全局变量-全局静态变量有什么区别" aria-hidden="true">#</a> ❓<code>block</code> 在捕获自动变量，局部静态变量，全局变量，全局静态变量有什么区别</h3><details class="hint-container details"><summary>💡</summary><p><code>Block</code> 对自动变量、局部静态变量、全局变量、全局静态变量的捕获方式是不同的：</p><ol><li><p>自动变量（auto variables）：这是我们在方法或函数中常见的局部变量，如 int num = 10。这种变量在 block 中默认以值传递形式捕获，即 block 捕获的是值，而不是变量本身。如果要在 block 中修改局部变量的值，必须在定义变量时加上 __block 修饰符。</p></li><li><p>局部静态变量、全局变量、全局静态变量：这些变量在 block 中以指针形式捕获，即 block 捕获的是对这些变量的指针。这意味着，可以在 block 中直接修改这些变量的值，不需要加 __block 修饰符。</p></li></ol><p>以下是个简单代码示例：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">int</span> globalVar <span class="token operator">=</span> <span class="token number">4</span><span class="token punctuation">;</span>
<span class="token keyword">static</span> <span class="token keyword">int</span> staticGlobalVar <span class="token operator">=</span> <span class="token number">5</span><span class="token punctuation">;</span>

<span class="token keyword">void</span> <span class="token function">Test</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">int</span> localVar <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
    <span class="token keyword">static</span> <span class="token keyword">int</span> staticVar <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>
    __block <span class="token keyword">int</span> blockVar <span class="token operator">=</span> <span class="token number">3</span><span class="token punctuation">;</span>

    <span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;localVar: %d&quot;</span><span class="token punctuation">,</span> localVar<span class="token punctuation">)</span><span class="token punctuation">;</span>          <span class="token comment">// 1</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;staticVar: %d&quot;</span><span class="token punctuation">,</span> staticVar<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 2</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;blockVar: %d&quot;</span><span class="token punctuation">,</span> blockVar<span class="token punctuation">)</span><span class="token punctuation">;</span>          <span class="token comment">// 3</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;globalVar: %d&quot;</span><span class="token punctuation">,</span> globalVar<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 4</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;staticGlobalVar: %d&quot;</span><span class="token punctuation">,</span> staticGlobalVar<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 5</span>

        <span class="token comment">// localVar++;              // Error! &quot;Variable is not assignable (missing __block type specifier)&quot;</span>
        staticVar<span class="token operator">++</span><span class="token punctuation">;</span>
        blockVar<span class="token operator">++</span><span class="token punctuation">;</span>
        globalVar<span class="token operator">++</span><span class="token punctuation">;</span>
        staticGlobalVar<span class="token operator">++</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span><span class="token punctuation">;</span>

    <span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;staticVar after block: %d&quot;</span><span class="token punctuation">,</span> staticVar<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 3</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;blockVar after block: %d&quot;</span><span class="token punctuation">,</span> blockVar<span class="token punctuation">)</span><span class="token punctuation">;</span>          <span class="token comment">// 4</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;globalVar after block: %d&quot;</span><span class="token punctuation">,</span> globalVar<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 5</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;staticGlobalVar after block: %d&quot;</span><span class="token punctuation">,</span> staticGlobalVar<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 6</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓block-捕获外部局部变量实际上发生了什么" tabindex="-1"><a class="header-anchor" href="#❓block-捕获外部局部变量实际上发生了什么" aria-hidden="true">#</a> ❓<code>block</code> 捕获外部局部变量实际上发生了什么</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，block 可以捕获其外部的局部变量，使得这些外部变量可以在 block 的执行体内部被访问到。捕获的过程其实是在 block 被创建时发生的。block 会创建对应的变量的 &quot;拷贝&quot;，这个拷贝的过程会考虑变量的类型：</p><ol><li><p>对于基本数据类型变量，比如 int、float，block 会复制其值，也就是在 heap(堆) 应用一段内存，将外部的变量值复制一份到这个内存上。</p></li><li><p>对于对象类型变量，block 会保留该对象，并复制对象指针。这个过程也被称为引用捕获。这样 block 对象可以在稍后的时间里随时引用到这个变量。</p></li></ol><p>然而，像上面所说的，block 捕获到的是外部局部变量的副本，所以你不能改变外部的局部变量。如果你想在 block 内修改外部局部变量，则需要在定义局部变量时加上 &quot;__block&quot; 修饰符。</p><p>__block关键字可以用于解决 block 内部无法修改外部变量的问题。这是因为，当捕获一个 __block 变量时，block 内部会保留这个 __block 变量。在堆上创建的 block，会在 heap 中维护一个 __block 变量的结构体，这个结构体中包含了 __block 变量的地址和其值，这样 block 就可以通过操作这个结构体来修改 __block 变量的值。</p></details><blockquote><p>如果 使用 __block 捕获了局部变量，这个 block 作为返回值 return 了，这个局部变量释放了，怎么办？</p></blockquote><details class="hint-container details"><summary>💡</summary><p>在使用 __block 捕获局部变量的同时，如果这个 block 作为返回值返回，那么这个局部变量就会被 block 捕获到，并且它的生命周期就延长到了 block 生命周期结束为止。也就是说，这个局部变量不会在离开定义它的作用域时释放，而是将在 block 被释放时才释放。</p><p>注意一点，正常情况下 block 是分配在栈上的，栈上的 block 在离开作用域后就会被销毁。但是如果这个 block 被赋值给了一个强引用的变量，或者被返回出去的话，就需要将栈上的 block 复制到堆上，这样 block 的生命周期就会得到延长，直到没有强引用指向它为止。</p><p>同时，对于使用 __block 修饰的局部变量，这个变量的存储地址会从栈复制到堆上。所以，返回的 block 中捕获的是移动到堆上的 __block 变量的地址，这也确保了返回后，能够正确地访问和修改这个 __block 变量。</p></details><h3 id="❓block-修改捕获变量除了用-block-还可以怎么做-有哪些局限性" tabindex="-1"><a class="header-anchor" href="#❓block-修改捕获变量除了用-block-还可以怎么做-有哪些局限性" aria-hidden="true">#</a> ❓<code>block</code> 修改捕获变量除了用 <code>__block</code> 还可以怎么做？有哪些局限性？</h3><details class="hint-container details"><summary>💡</summary><p>如果你希望在 block 中修改一个捕获变量，除了用 __block，可以使用 __strong 或者创建一个可修改的对象，如 NSMutableArray 或 NSMutableDictionary，然后在 block 中修改该对象的内容。但是，这并不能改变原始变量的值，只是改变了它所引用的对象的内容。</p><p>例如：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSMutableArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    <span class="token punctuation">[</span>array addObject<span class="token punctuation">:</span><span class="token string">@&quot;Object&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> array<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出：Object</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个例子中，我们并没有修改 array 这个变量本身，而是修改了它所引用的对象的内容。也就是说，我们不是改变 array 这个指针所指向的地址，而是改变了这个指针所指向的内存中的内容。</p><p>此种方式来达到block内部修改变量的效果的局限性在于，只有可变的引用类型（比如NSMutableArray）可以使用此方法，基本数据类型或者不可变的引用类型无法使用此方法在block中进行修改。与使用__block相比也更加的不直观。</p></details><h3 id="❓block-中修改-nsmutablearray-需不需要添加-block" tabindex="-1"><a class="header-anchor" href="#❓block-中修改-nsmutablearray-需不需要添加-block" aria-hidden="true">#</a> ❓<code>block</code> 中修改 <code>NSMutableArray</code> 需不需要添加 <code>__block</code></h3><details class="hint-container details"><summary>💡</summary><p>不需要。在 Objective-C 中，NSMutableArray 是一个引用类型的对象，即使不使用 <code>__block</code>，你也可以在 block 内部添加、删除或修改 NSMutableArray 的元素。因为你修改的是 NSMutableArray 对象的内容，而不是 NSMutableArray 变量本身。</p><p>例如，你可以这么写：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSMutableArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    <span class="token punctuation">[</span>array addObject<span class="token punctuation">:</span><span class="token string">@&quot;Hello, World&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> array<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 输出：[&quot;Hello, World&quot;]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这段代码在 block 中向 array 添加了一个字符串，然后打印 array 的内容。注意，这里并没有修改 array 这个变量本身，只修改了它所引用的对象的内容。所以，我们无需添加 <code>__block</code> 修饰符。</p><p>但是，如果你希望在 block 中修改 array 这个变量（比如使其指向一个全新的 NSMutableArray 对象），那就需要使用 <code>__block</code> 了。例如：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__block NSMutableArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    array <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token punctuation">[</span>array addObject<span class="token punctuation">:</span><span class="token string">@&quot;Hello, World&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> array<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 输出：[&quot;Hello, World&quot;]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓-block-vs-weak" tabindex="-1"><a class="header-anchor" href="#❓-block-vs-weak" aria-hidden="true">#</a> ❓<code>__block</code> vs <code>__weak</code></h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，我们经常使用 <code>__block</code> 和 <code>__weak</code> 这两个修饰符来修饰变量。尽管它们的用法看起来相似，但它们的作用和使用场景却有所不同。</p><ol><li><p><code>__block</code>：它到底是允许在块（Block）中改变变量的值。在 Objective-C 中，<code>__block</code> 是存储类型说明符，用于修饰一个变量，让该变量能在 Block 中被读取和写入。如果没有 <code>__block</code>，那么 Block 就不能修改该变量的值。</p></li><li><p><code>__weak</code>：它用于创建一个弱引用。在 Objective-C 中，弱引用是一种特殊类型的引用，它不会增加被引用对象的引用计数。这样可以解决对象互相引用，形成引用循环的问题。在处理Block 对象引用 self 时，我们通常会先创建一个弱引用的 self，然后在 Block 中使用这个弱引用，从而避免引用循环。</p></li></ol><p>在使用这两个修饰词来修饰变量时，我们需要根据需求和实际使用场景来选择。如果我们希望在 Block 中改变一个变量的值，应该使用 <code>__block</code>。而如果我们想避免引用循环，应该使用 <code>__weak</code>。</p></details><h3 id="❓-block-在-arc-和非-arc-有区别吗" tabindex="-1"><a class="header-anchor" href="#❓-block-在-arc-和非-arc-有区别吗" aria-hidden="true">#</a> ❓<code>__block</code> 在 <code>ARC</code> 和非 <code>ARC</code> 有区别吗</h3><details class="hint-container details"><summary>💡</summary><p>是的，<code>__block</code> 在 ARC（Automatic Reference Counting）和非 ARC 环境下确实有区别。</p><p>在非 ARC 环境下，__block不会改变对象的所有权。也就是说，如果我们有一个__block修饰的对象，block 不会为这个对象增加引用计数。因此，如果在 block 执行后，原来的对象被释放了，那么 block 内的这个对象就变成了悬浮指针。</p><p>在 ARC 环境下，__block 有另外一种行为。它会保留 block 中使用到的对象。也就是说，如果我们有一个 __block 修饰的对象，当 block 执行时，就会增加这个对象的引用计数。因此，在 ARC 下，我们可以安全地在 block 中引用 __block 修饰的对象，而不必担心它会被提前释放。</p><p>为了在两种环境下都能安全使用，我们一般会选择 __weak 代替 __block 来避免循环引用，特别是在引用 self 或者其成员变量的时候。</p></details><h3 id="❓一个-int-变量用-block-修饰与不修饰有区别吗" tabindex="-1"><a class="header-anchor" href="#❓一个-int-变量用-block-修饰与不修饰有区别吗" aria-hidden="true">#</a> ❓一个 <code>int</code> 变量用 <code>__block</code> 修饰与不修饰有区别吗</h3><details class="hint-container details"><summary>💡</summary><p>是的，对于一个 <code>int</code> 类型的变量，使用 <code>__block</code> 修饰与否是有区别的。</p><p>如果一个变量被 <code>__block</code> 修饰，那么它可以在 block 内部被更改。如果没有被 <code>__block</code> 修饰，那么在 block 内部是无法更改这个变量的，它在 block 中会被视为常量。</p><p>例如，对于以下代码：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">int</span> value <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    value <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>  <span class="token comment">// 这里会报错，因为 value 在 block 内部不能被更改</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>但如果 <code>value</code> 被 <code>__block</code> 修饰：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__block <span class="token keyword">int</span> value <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    value <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>  <span class="token comment">// 这里可以正常运行，value 在 block 内部可以被更改</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>因此，如果需要在 block 内部更改一个变量的值，那么这个变量必须使用 <code>__block</code> 修饰。</p></details><h3 id="❓block-如何进行内存管理的" tabindex="-1"><a class="header-anchor" href="#❓block-如何进行内存管理的" aria-hidden="true">#</a> ❓<code>block</code> 如何进行内存管理的</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，Block 的内存管理默认分为三个级别：_NSConcreteGlobalBlock、_NSConcreteStackBlock、_NSConcreteMallocBlock。</p><ol><li><p><code>__NSConcreteGlobalBlock__</code>：这种 Block 属于全局区，类似于全局变量，生命周期从应用开始直到应用结束。</p></li><li><p><code>__NSConcreteStackBlock__</code>：这种 Block 存放在栈内存中，当超出其作用域后就会被自动销毁。</p></li><li><p><code>__NSConcreteMallocBlock__</code>：这种 Block 存放在堆内存中，需要手动管理其内存，可以通过 retain 来增加其引用计数，通过 release 来减少其引用计数，当引用计数为 0 时，Block 会被销毁。</p></li></ol><p>在使用 ARC (Automatic Reference Counting) 时，对 Block 的内存管理会简化许多。当 Block 中引用到了外部变量时，Block 将会自动从栈复制到堆中（有复制行为的 Block 都会进入堆区），并且在引用计数为 0 后会自动销毁，不需要手动管理。</p><p>但要注意的一点是，对于 Block 内部应用到的对象，如果不加特殊处理，是会形成强引用的，因此可能会导致引用循环并引发内存泄漏。为了防止这种情况，我们经常使用 __weak 来修饰引用的对象，避免产生循环引用。</p></details><h3 id="❓block-一般使用-copy-修饰-可以使用-strong-吗-发生-copy-的时机" tabindex="-1"><a class="header-anchor" href="#❓block-一般使用-copy-修饰-可以使用-strong-吗-发生-copy-的时机" aria-hidden="true">#</a> ❓<code>block</code> 一般使用 <code>copy</code> 修饰，可以使用 <code>strong</code> 吗,发生 <code>copy</code> 的时机</h3><details class="hint-container details"><summary>💡</summary><p><code>block</code>使用<code>copy</code>修饰是为了保持<code>block</code>的引用计数的正确。<code>block</code>本身是在堆栈上分配的，<code>copy</code>操作将其复制到堆上，可以保证<code>block</code>在方法返回后不被销毁，保证其正确执行。</p><p>虽然你也可以使用<code>strong</code>来修饰<code>block</code>，但是这样做并不会将<code>block</code>从栈复制到堆。因此，如果<code>block</code>捕获了方法内部的变量，当方法返回后，这些变量都已经被释放，可能会导致程序崩溃。</p><p><code>copy</code>操作一般发生在你把<code>block</code>作为属性进行设置的时候，这个时候赋值方法（setter）会进行<code>copy</code>操作。也可以手动调用<code>copy</code>方法来进行操作。</p><p>总的来说，将<code>block</code>属性用<code>copy</code>修饰，能够更好地管理其内存，并确保其正确执行。</p></details><h3 id="❓为什么在-block-外部使用-weak-修饰的同时需要在内部使用-strong-修饰" tabindex="-1"><a class="header-anchor" href="#❓为什么在-block-外部使用-weak-修饰的同时需要在内部使用-strong-修饰" aria-hidden="true">#</a> ❓为什么在 <code>block</code> 外部使用 <code>__weak</code> 修饰的同时需要在内部使用 <code>__strong</code> 修饰</h3><details class="hint-container details"><summary>💡</summary><p>在 block 中使用 __weak 修饰符是为了防止循环引用。由于 block 会捕获其内部使用的变量，如果 block 内部访问了 self 或其成员变量，那么会形成循环引用，导致内存泄漏。而使用 __weak 修饰符可以将 self 或成员变量变成弱引用，从而避免循环引用。</p><p>然而，使用 __weak 修饰符也会有其不足之处：在 block 执行期间，若弱引用的对象被释放了，那么 block 内部对该对象的访问就会失效。为了避免这种情况，就需要在 block 内部使用 __strong 修饰符将弱引用变量提升为强引用。</p><p>这样做可以保证在 block 执行期间，这个被提升的强引用变量所指向的对象不会被释放，保证了 block 执行过程中对象的访问安全。而在 block 执行完毕后，强引用也会随之释放，和在 block 外部保持指向对象的弱引用并不存在循环引用问题。</p><p>以下是一个典型的例子：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__weak <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> weakSelf <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token punctuation">[</span><span class="token keyword">self</span> doSomethingWithBlock<span class="token punctuation">:</span><span class="token operator">^</span><span class="token punctuation">{</span>
    __strong <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> strongSelf <span class="token operator">=</span> weakSelf<span class="token punctuation">;</span>
    <span class="token punctuation">[</span>strongSelf doSomethingElse<span class="token punctuation">]</span><span class="token punctuation">;</span>  
    <span class="token comment">// 在此处，若 self 在 block 执行期间释放，由于存在 strongSelf 的强引用，self 所指向的对象不会被释放。</span>
<span class="token punctuation">}</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个例子中，会先创建一个 <code>weakSelf</code> 的弱引用，然后在 block 中将其提升为 <code>strongSelf</code> 的强引用。这样可以保证 <code>self</code> 在 block 执行期间不会被释放，从而避免了访问无效对象的风险。</p></details><h3 id="❓block-访问对象类型的-auto-变量时-在-arc-和-mrc-下有什么区别" tabindex="-1"><a class="header-anchor" href="#❓block-访问对象类型的-auto-变量时-在-arc-和-mrc-下有什么区别" aria-hidden="true">#</a> ❓<code>block</code> 访问对象类型的 <code>auto</code> 变量时，在 <code>ARC</code> 和 <code>MRC</code> 下有什么区别</h3><details class="hint-container details"><summary>💡</summary><p>在MRC和ARC下，<code>block</code>访问对象类型的<code>auto</code>变量时，有一些不同。</p><p>在MRC下，<code>block</code>只是简单地进行指针拷贝，不会对对象进行<code>retain</code>操作，这会导致可能的对象释放错误。</p><p>在ARC下，编译器会自动进行内存管理，在<code>block</code>被创建时，会对它所捕获的对象发送一次<code>retain</code>消息，同时在<code>block</code>被释放时，发送一次<code>release</code>消息。</p><p>这是在ARC和MRC环境下，<code>block</code>对<code>auto</code>变量的主要区别。为了避免内存管理问题，我们一般选择在ARC环境下使用<code>block</code>。</p></details><h3 id="❓block-中可以使用-kvc-来访问实例变量吗-可以使用-kvc-修改它的值吗" tabindex="-1"><a class="header-anchor" href="#❓block-中可以使用-kvc-来访问实例变量吗-可以使用-kvc-修改它的值吗" aria-hidden="true">#</a> ❓<code>block</code> 中可以使用 <code>KVC</code> 来访问实例变量吗？可以使用 <code>KVC</code> 修改它的值吗</h3><details class="hint-container details"><summary>💡</summary><p>在<code>block</code>中使用KVC(Key-Value Coding)访问和修改实例变量，必须注意以下两点：</p><ol><li><p>被访问的实例变量是否存在对应的访问器方法(getter 和 setter)。</p></li><li><p>被访问的实例变量在类的接口中是否有定义，或者有无@property声明。</p></li></ol><p>如果满足以上条件，你可以在<code>block</code>中使用KVC来访问和修改实例变量。例如：</p><pre><code>__weak typeof(self) weakSelf = self;
self.myBlock = ^{
    // 使用KVC访问实例变量
    NSInteger value = [[weakSelf valueForKey:@&quot;someValue&quot;] integerValue];
    
    // 使用KVC修改实例变量
    [weakSelf setValue:@(123) forKey:@&quot;someValue&quot;];
};
</code></pre><p>注意：在ARC环境下，不要直接在<code>block</code>虽然用<code>self</code>，否则可能导致循环引用问题。一定要记住使用弱引用或者强引用来防止循环引用。</p></details><hr><h2 id="分类" tabindex="-1"><a class="header-anchor" href="#分类" aria-hidden="true">#</a> 分类</h2><h3 id="❓category-vs-extension" tabindex="-1"><a class="header-anchor" href="#❓category-vs-extension" aria-hidden="true">#</a> ❓<code>Category</code> vs <code>Extension</code></h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>Category</code>(类别)：<code>Objective-C</code> 的一个特性，提供了一种给已存在类添加方法的机制.</p></blockquote><ul><li><code>Category</code> : 只能添加方法，不能添加实例变量(除非通过关联对象)。分类中的方法是运行时添加到方法列表中的。</li><li><code>Extension</code> : 可以添加方法和计算属性，也可以添加构造方法，也可以实现以一个协议。编译时已经确定，不支持动态添加方法。</li></ul></details><h3 id="❓nsobject-添加-extension-会怎么样" tabindex="-1"><a class="header-anchor" href="#❓nsobject-添加-extension-会怎么样" aria-hidden="true">#</a> ❓<code>NSObject</code> 添加 <code>Extension</code> 会怎么样</h3><details class="hint-container details"><summary>💡</summary><p>向<code>NSObject</code>添加<code>Extension</code>(类别、Category)会使<code>Extension</code>中的方法对继承自<code>NSObject</code>的所有对象可见和可用。这会对你的项目产生全局性的影响。</p><p><code>Extension</code>可以用来添加新的方法到现有的类中，但它没有自己的实例变量，所以不能存储新的状态。在使用<code>Extension</code>时，需要注意以下几点：</p><ul><li>如果你覆盖已有的方法，所有继承自<code>NSObject</code>的对象都会受到影响，可能出现无法预知的结果。</li><li>如果添加的新方法与其他库或者框架定义的方法发生冲突，同样可能导致问题。</li></ul><p>因此，虽然往<code>NSObject</code>添加<code>Extension</code>可以快速为所有对象添加功能，但需要谨慎使用，尽量避免覆盖现有的方法或者添加可能与其他方法发生冲突的方法。同时，由于该范围非常广，必要时最好选择子类化或协议等方式实现。</p><p>另外，如果只是想在项目内全局使用某些通用方法，可以考虑创建一个<code>NSObject</code>的子类，然后让需要使用这些方法的类继承自这个子类，这样就不会影响到不需要这些方法的类。</p></details><h3 id="❓如果分类中添加与类相同的方法签名会发生什么" tabindex="-1"><a class="header-anchor" href="#❓如果分类中添加与类相同的方法签名会发生什么" aria-hidden="true">#</a> ❓如果分类中添加与类相同的方法签名会发生什么</h3><details class="hint-container details"><summary>💡</summary><p>当一个OC的类别（Category）和主类都定义了相同签名的方法时，运行时系统会优先调用类别的方法，而忽略主类中的同名方法。这是因为在运行时的方法解析过程中，分类的方法会被先于主类方法加入到方法列表中。</p><p>在代码中，如果主类和分类都实现了同一个方法，那么分类的方法会覆盖掉主类中的方法，也就是说，在方法调用时，实际上执行的是分类中的方法。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>@interface MyClass : NSObject
- (void)print;
@end

@implementation MyClass
- (void)print {
    NSLog(@&quot;print in MyClass&quot;);
}
@end

@interface MyClass (MyCategory)
- (void)print;
@end

@implementation MyClass (MyCategory)
- (void)print {
    NSLog(@&quot;print in MyCategory&quot;);
}
@end

// 调用时
MyClass *myClass = [[MyClass alloc] init];
[myClass print]; // 输出：print in MyCategory
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这种特性在某些情况下非常有用，可以实现方法的替换和拦截等。但也需要格外注意，如果不小心覆盖了主类本身的重要方法，可能会造成程序的异常。</p></details><h3 id="❓分类是如何实现的-它为什么会覆盖掉原来的方法" tabindex="-1"><a class="header-anchor" href="#❓分类是如何实现的-它为什么会覆盖掉原来的方法" aria-hidden="true">#</a> ❓分类是如何实现的，它为什么会覆盖掉原来的方法</h3><details class="hint-container details"><summary>💡</summary><p>底层实现：是一个 <code>objc_category</code> 结构体，它包含了类名(name)、类(cls)、类别中所有方法的列表(method_list)、类别的协议列表(protocol_list)、类别中所有的属性列表(property_list)。</p><p>加载过程：在程序运行时的 <code>load</code> 阶段进行的，所有类和分类的 <code>+load</code> 方法（如果有）被调用。在调用 <code>+load</code> 方法之前，<code>runtime</code> 会先将分类的方法、协议、属性合并到类的数据结构中。这个过程中，分类的方法被放在了原来类方法列表的前面，所以在类和分类中有相同方法名的方法的时候，分类的方法会被优先调用，原类的方法被覆盖。</p><p>Objective-C 中的分类 (Category) 实质上是对一个存在的类添加额外的方法，而不改变它的声明和定义。分类是在运行时通过Objective-C的动态性质合并到类上的，这使得我们可以随意地向任何类添加方法。</p><p>分类实现的本质是这样的：在编译阶段，每一个分类会被编译成一个特殊的结构体（如 <code>_category_t</code>），其中包含了分类名，分类所在类的名字，以及分类中所有添加的方法的列表。在程序启动的时候，这个结构有一个初始化函数会被自动调用。在这个函数中，分类中的方法会被添加到类的方法列表中。因此你可以在分类中添加新的方法。</p><p>分类中的方法会覆盖掉原来的方法，主要是因为分类的方法被添加到方法列表的顺序优先级更高。在Objective-C查找方法实现的时候，是按照方法在列表中的顺序，从前往后查找的，当找到第一个匹配的方法时，就停止查找，并且调用这个方法。因此，如果分类中的方法与原类中的方法同名，分类中的方法就会被优先调用，从而覆盖掉原来的方法。</p></details><h3 id="❓分类中有与主类相同的方法-如果想要调用主类中的方法如何处理" tabindex="-1"><a class="header-anchor" href="#❓分类中有与主类相同的方法-如果想要调用主类中的方法如何处理" aria-hidden="true">#</a> ❓分类中有与主类相同的方法，如果想要调用主类中的方法如何处理</h3><details class="hint-container details"><summary>💡</summary><p>如果分类中的方法与主类的方法名称相同，由于运行时会优先调用分类的方法，所以我们通常无法直接调用到主类中的方法。不过有一种间接的办法可以实现。</p><p>你可以在分类方法内部通过 <code>NSInvocation</code> 或 <code>methodSignatureForSelector:</code> 和 <code>forwardInvocation:</code> 的方式调用主类的原方法。下面给出 <code>NSInvocation</code> 实现的例子：</p><div class="language-objectivec line-numbers-mode" data-ext="objectivec"><pre class="language-objectivec"><code><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">import</span> <span class="token string">&quot;MyClass.h&quot;</span></span>

<span class="token keyword">@implementation</span> <span class="token function">MyClass</span> <span class="token punctuation">(</span>MyCategory<span class="token punctuation">)</span>
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>print <span class="token punctuation">{</span>
    <span class="token comment">// 获取主类原方法</span>
    SEL origSel <span class="token operator">=</span> <span class="token keyword">@selector</span><span class="token punctuation">(</span>print<span class="token punctuation">)</span><span class="token punctuation">;</span>
    Method origMethod <span class="token operator">=</span> <span class="token function">class_getInstanceMethod</span><span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token keyword">self</span> class<span class="token punctuation">]</span><span class="token punctuation">,</span> origSel<span class="token punctuation">)</span><span class="token punctuation">;</span>
    
    <span class="token comment">// 构建 NSInvocation</span>
    NSInvocation <span class="token operator">*</span>inv <span class="token operator">=</span> <span class="token punctuation">[</span>NSInvocation invocationWithMethodSignature<span class="token punctuation">:</span><span class="token punctuation">[</span><span class="token keyword">self</span> methodSignatureForSelector<span class="token punctuation">:</span>origSel<span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token punctuation">[</span>inv setSelector<span class="token punctuation">:</span>origSel<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token punctuation">[</span>inv setTarget<span class="token punctuation">:</span><span class="token keyword">self</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
    
    <span class="token comment">// 调用 NSInvocation</span>
    <span class="token punctuation">[</span>inv invoke<span class="token punctuation">]</span><span class="token punctuation">;</span>
    
    <span class="token comment">// 获取返回值</span>
    NSString <span class="token operator">*</span>returnValue<span class="token punctuation">;</span>
    <span class="token punctuation">[</span>inv getReturnValue<span class="token punctuation">:</span><span class="token operator">&amp;</span>returnValue<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> returnValue<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>需要注意的是，仅当主类方法和分类方法没有强制要求的执行顺序时才可以这样做，否则可能会破坏类的封装性并导致不明确的行为。如果你的代码常常需要这么做，那可能是设计有问题，应该考虑重构代码以避免这种情况。</p></details><h3 id="❓两个分类的-load-方法的加载顺序" tabindex="-1"><a class="header-anchor" href="#❓两个分类的-load-方法的加载顺序" aria-hidden="true">#</a> ❓两个分类的 <code>load</code> 方法的加载顺序</h3><details class="hint-container details"><summary>💡</summary><p>Objective-C 类和分类中的 <code>+load</code> 方法会在类或分类被加载到运行时时调用。对于同一个类的不同分类，各自的 <code>+load</code> 方法的调用顺序与编译顺序有关，最后编译的分类的 <code>+load</code> 方法会被先调用。</p><p>但是要注意，虽然编译顺序是影响 <code>+load</code> 方法调用顺序的因素之一，但不是唯一的决定因素。在一个复杂的项目中，编译顺序可能会受到 Xcode 编译设置、文件名字母序、Xcode 版本、甚至 Mac 系统版本的影响。因此，关于这个问题，Apple 官方文档建议不要依赖于 <code>+load</code> 方法的调用顺序。如果项目中多个 <code>+load</code> 方法之间有依赖关系，那么可能需要仔细设计代码，以避免因 <code>+load</code> 方法调用顺序引起的问题。</p></details><h3 id="❓两个分类的同名方法的加载顺序" tabindex="-1"><a class="header-anchor" href="#❓两个分类的同名方法的加载顺序" aria-hidden="true">#</a> ❓两个分类的同名方法的加载顺序</h3><details class="hint-container details"><summary>💡</summary><p>在Objective-C中，编译器按照.m文件编译顺序来加载分类，如果存在多个分类都实现了同一方法，那么顺序是&quot;编译顺序的逆序&quot;，也就是说最后编译的分类会首先被加载，同名方法就会被最后编译的那个分类所覆盖。</p><p>需要注意的是，依赖于这种顺序来设计代码是很不稳定的，因为当其他开发者操作或更改了文件的编译顺序，可能会导致相关代码的运行出问题。此外，互相覆盖的设计也会降低代码的可读性和维护性。所以最好避免在多个分类中都实现同一方法，以保证代码的稳定运行。</p></details><h3 id="❓一个类中有个多个分类都实现了相同的方法-会调用哪个方法" tabindex="-1"><a class="header-anchor" href="#❓一个类中有个多个分类都实现了相同的方法-会调用哪个方法" aria-hidden="true">#</a> ❓一个类中有个多个分类都实现了相同的方法，会调用哪个方法</h3><details class="hint-container details"><summary>💡</summary><p>当一个类有多个分类（Category），并且这些分类都实现了相同的方法时，会优先调用最后编译的分类中的方法。这是因为Objective-C在运行时加载分类的顺序是根据编译的顺序，最后编译的分类的方法会被放到方法列表的最前面，从而覆盖掉其他同名方法。</p><p>但是具体调用哪个方法并不是确定的，因为分类的编译顺序可能会因为项目配置的改变而改变，比如编译选项的修改、文件名的修改等。所以，实际开发中我们并不建议在不同的分类中实现相同的方法，这可能会引起不可预见的bug。如果需要在分类中添加方法，应确保新添加的方法与主类和其他分类的方法不冲突。</p></details><h3 id="❓分类中为什么不能添加-property" tabindex="-1"><a class="header-anchor" href="#❓分类中为什么不能添加-property" aria-hidden="true">#</a> ❓分类中为什么不能添加 <code>@property</code></h3><details class="hint-container details"><summary>💡</summary><p>在Objective-C中，<code>@property</code>会告诉编译器我们在类中声明了一个属性。默认情况下，编译器会生成一个后备存储（实例变量），并且为属性生成getter和setter方法。然而，对于分类（Category），一旦对象一旦在运行时被注册，我们就不能再动态地添加实例变量。</p><p>从runtime角度来看， 这是由于类型的数据布局已经在编译阶段确定并且注册到了Objective-C的runtime系统中，而分类是在运行时被加载，所以无法更改基类的数据布局，也就无法添加实例变量。</p><p>具体来说，类结构体<code>struct objc_class</code>中的<code>ivar_list（实例变量列表）</code>是确定的：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code><span class="token keyword">struct</span> <span class="token class-name">objc_class</span> <span class="token punctuation">{</span>
    Class isa<span class="token punctuation">;</span>
    Class super_class<span class="token punctuation">;</span>
    <span class="token keyword">const</span> <span class="token keyword">char</span> <span class="token operator">*</span>name<span class="token punctuation">;</span>
    <span class="token keyword">long</span> version<span class="token punctuation">;</span>
    <span class="token keyword">long</span> info<span class="token punctuation">;</span>
    <span class="token keyword">long</span> instance_size<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">objc_ivar_list</span> <span class="token operator">*</span>ivars<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>而分类结构体<code>struct category_t</code>中只有方法列表，协议列表和属性列表，并没有实例变量列表：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code><span class="token keyword">struct</span> <span class="token class-name">category_t</span> <span class="token punctuation">{</span>
    <span class="token keyword">const</span> <span class="token keyword">char</span> <span class="token operator">*</span>name<span class="token punctuation">;</span>
    <span class="token class-name">classref_t</span> cls<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">method_list_t</span> <span class="token operator">*</span>instanceMethods<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">method_list_t</span> <span class="token operator">*</span>classMethods<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">protocol_list_t</span> <span class="token operator">*</span>protocols<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">property_list_t</span> <span class="token operator">*</span>instanceProperties<span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>所以在Objective-C分类中不能添加实例变量，也就不能添加带有自动生成的getter和setter的<code>@property</code>。但是你可以在分类中声明属性，并通过对已有的实例变量手动编写getter和setter，或者使用关联对象(Associated Objects)来模拟实例变量。</p><p>总的来说，之所以不能在分类中添加<code>@property</code>，是因为在编译器层面和runtime层面上，Objective-C的设计都不允许我们动态地向类中添加实例变量。</p></details><h3 id="❓分类的结构体里面有哪些成员" tabindex="-1"><a class="header-anchor" href="#❓分类的结构体里面有哪些成员" aria-hidden="true">#</a> ❓分类的结构体里面有哪些成员</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 的运行时系统中，每个 Category 都对应着一个结构体 category_t。这个结构体的定义（在 objc-runtime-new.h 中）如下：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code><span class="token keyword">typedef</span> <span class="token keyword">struct</span> <span class="token class-name">category_t</span> <span class="token punctuation">{</span>
    <span class="token keyword">const</span> <span class="token keyword">char</span> <span class="token operator">*</span>name<span class="token punctuation">;</span>
    <span class="token class-name">classref_t</span> cls<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">method_list_t</span> <span class="token operator">*</span>instanceMethods<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">method_list_t</span> <span class="token operator">*</span>classMethods<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">protocol_list_t</span> <span class="token operator">*</span>protocols<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> <span class="token class-name">property_list_t</span> <span class="token operator">*</span>instanceProperties<span class="token punctuation">;</span>
<span class="token punctuation">}</span> <span class="token class-name">category_t</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这个结构体中的字段包含以下内容：</p><ul><li><code>name</code>：分类的名字。</li><li><code>cls</code>：分类所属的类。</li><li><code>instanceMethods</code>：分类中新增的实例方法列表。</li><li><code>classMethods</code>：分类中新增的类方法列表。</li><li><code>protocols</code>：分类实现的协议列表。</li><li><code>instanceProperties</code>：分类中新增的实例属性列表。</li></ul><p>因此，你可以在分类中声明和实现新的实例方法和类方法，实现新的协议，以及添加新的实例属性。</p><p>需要注意的是，虽然可以在分类中声明和实现新的属性，但是你不能使用 <code>@synthesize</code> 来生成存取方法，也无法生成实例变量。你只能手动实现 getter 和 setter 来操作已存在的实例变量，或者使用关联对象（Associated Object）来模拟实例变量。</p></details><h3 id="❓分类中如何添加属性" tabindex="-1"><a class="header-anchor" href="#❓分类中如何添加属性" aria-hidden="true">#</a> ❓分类中如何添加属性</h3><details class="hint-container details"><summary>💡</summary><p>可以使用关联对象（<code>Associated Objects</code>）让分类拥有“伪属性”。</p><p>在Objective-C中，分类（Category）的定义中并不支持直接添加属性（instance variable）。但我们可以通过 Objective-C 的关联对象（Associative References）机制实现给分类添加属性。</p><p>以下是一个例子，演示如何给一个分类添加一个属性：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">import</span> <span class="token string">&quot;NSObject+Property.h&quot;</span></span>
<span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">import</span> <span class="token expression"><span class="token operator">&lt;</span>objc<span class="token operator">/</span>runtime<span class="token punctuation">.</span>h<span class="token operator">&gt;</span></span></span>

<span class="token keyword">static</span> <span class="token keyword">const</span> <span class="token keyword">void</span> <span class="token operator">*</span>MyKey <span class="token operator">=</span> <span class="token operator">&amp;</span>MyKey<span class="token punctuation">;</span>

<span class="token keyword">@implementation</span> <span class="token function">NSObject</span> <span class="token punctuation">(</span>Property<span class="token punctuation">)</span>
<span class="token comment">// getter</span>
<span class="token operator">-</span> <span class="token punctuation">(</span>NSString <span class="token operator">*</span><span class="token punctuation">)</span>myProperty <span class="token punctuation">{</span>
  <span class="token comment">// \`objc_getAssociatedObject\` 用于取关联对象。</span>
  <span class="token comment">//  - 首个参数是要存取的对象</span>
  <span class="token comment">//  - 第二个参数是键，我们用一个静态常量 \`MyKey\` 作为键（每个关联到对象的键必须全局唯一）</span>
  <span class="token keyword">return</span> <span class="token function">objc_getAssociatedObject</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">,</span> MyKey<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token comment">// setter</span>
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>setMyProperty<span class="token punctuation">:</span><span class="token punctuation">(</span>NSString <span class="token operator">*</span><span class="token punctuation">)</span>myProperty <span class="token punctuation">{</span>
  <span class="token comment">// \`objc_setAssociatedObject\` 第三个参数是存储的值，第四个参数是关联策略</span>
  <span class="token comment">// \`OBJC_ASSOCIATION_COPY_NONATOMIC\`，表示这是一个 copy ，非原子操作的特性</span>
  <span class="token function">objc_setAssociatedObject</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">,</span> MyKey<span class="token punctuation">,</span> myProperty<span class="token punctuation">,</span> OBJC_ASSOCIATION_COPY_NONATOMIC<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个示例中，我们定义了一个 <code>NSObject</code> 的分类并为它添加了一个 <code>myProperty</code> 属性。</p><p>。对于 ，例如在这里我们将它设为了 。</p><p>通过关联对象可以间接实现在分类中&quot;添加&quot;属性，这也是 <code>Objective-C</code> 动态的特性之一。</p></details><hr><h2 id="消息发送" tabindex="-1"><a class="header-anchor" href="#消息发送" aria-hidden="true">#</a> 消息发送</h2><h3 id="❓oc-中的内省和反射-底层实现原理-应用场景有哪些" tabindex="-1"><a class="header-anchor" href="#❓oc-中的内省和反射-底层实现原理-应用场景有哪些" aria-hidden="true">#</a> ❓<code>OC</code> 中的内省和反射，底层实现原理，应用场景有哪些</h3><details class="hint-container details"><summary>💡</summary><p>Objective-C中的内省和反射是基于其动态的特性实现的，底层依赖于runtime系统。</p><p>内省是面向对象编程中的一种能力，允许程序检查对象的类型或属性。在Objective-C中，可以通过runtime系统提供的各种API（如<code>isKindOfClass:</code>,<code>respondsToSelector:</code>,等方法）来实现内省。</p><p>反射是指程序能够观察和修改自身结构和行为的一种能力。在Objective-C中，反射主要依赖于runtime系统提供的API，如<code>NSClassFromString</code>,<code>NSStringFromClass</code>,<code>NSSelectorFromString</code>, <code>NSStringFromSelector</code>等函数。</p><p>底层实现原理： Objective-C对象中都有一个isa指针指向对象所属的类，这个类中存储了类的方法列表、属性列表，并且如果类继承自其他类，那么这个类还会通过superclass指针找到其父类，这样层层递推，形成了一条类链，利用这条链条就可以检查和操作对象的行为和结构。</p><p>应用场景： 内省和反射在很多场景下都非常有用，例如：</p><ol><li>数据和模型的自动映射，例如JSON数据转Model对象，通常需要写大量的映射代码。利用内省我们可以在运行时获取对象的属性信息，从而实现自动映射。</li><li>在运行时动态地添加类，方法，属性。例如我们可以在运行时给一个类添加属性或方法，或者修改方法的实现等。</li><li>在面向切面编程（AOP）中，经常需要运行时修改对象的行为。</li><li>在执行单元测试时，可以利用反射机制来动态地调用需要测试的方法。</li></ol></details><h3 id="❓oc-如何实现多重继承" tabindex="-1"><a class="header-anchor" href="#❓oc-如何实现多重继承" aria-hidden="true">#</a> ❓<code>OC</code> 如何实现多重继承</h3><details class="hint-container details"><summary>💡</summary><p>Objective-C 并不直接支持多重继承（即一个子类可以拥有多个父类的属性和方法）。但 Objective-C 提供了几种方法可以实现类似多重继承的效果：</p><ol><li><p><strong>协议（Protocol）</strong>：OC 中的协议类似于其他语言中的接口，可以定义一组方法和属性，然后由类去实现。一个类可以同时实现多个协议，这就实现了类似多重继承的效果，只是实现协议的类需要自己实现协议中定义的所有方法。</p></li><li><p><strong>组合</strong>：组合是指在一个类中包含其他类的实例作为成员变量，借此可以使用其他类的功能，间接实现多重继承的效果。例如，类A包含类B和类C的实例，那么类A就可以使用类B和类C的功能。</p></li><li><p><strong>分类（Category）</strong>：分类是一种使得现有类增加方法的方式，是在运行时决议的。可以通过创建分类，将其他类的方法引入，实现类似多继承的效果。但是分类不能添加新的实例变量。</p></li><li><p><strong>消息转发（Message Forwarding）</strong>：这是 Objective-C 的动态特性，允许将一个方法调用转发给其他对象进行处理。如果某个类无法响应某个消息，Objective-C 允许我们在运行时将这个消息转发给另一个能处理这个消息的对象。</p></li></ol><p>注意，以上方式只能部分模拟多重继承，并不能完全替代多重继承，在使用时应根据具体需求和情况选择最合适的方式。</p></details><h3 id="❓oc-是否支持方法重载" tabindex="-1"><a class="header-anchor" href="#❓oc-是否支持方法重载" aria-hidden="true">#</a> ❓<code>OC</code> 是否支持方法重载</h3><details class="hint-container details"><summary>详情</summary><p><code>OC</code> 不支持方法重载。方法重载指的是在同一个类里面，方法名字相同，但参数不同的多个方法。</p><p><code>OC</code> 是一种动态类型语言，其方法的调用不是在编译期决定的，而是在运行时决定的。<code>OC</code> 使用消息派发机制，方法调用更依赖于方法的唯一性，如果有多个方法名字完全相同（即使参数类型不同），编译器和运行时系统就无法准确找到你要调用的方法。</p></details><h3 id="❓静态方法是否能被重写" tabindex="-1"><a class="header-anchor" href="#❓静态方法是否能被重写" aria-hidden="true">#</a> ❓静态方法是否能被重写</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，通常所说的 &quot;静态方法&quot; 实际上是指&quot;类方法&quot;，用 &quot;+&quot; 符号来声明。这种方法是可以被子类重写的。也就是说，如果在子类中重写了一个已经在父类中定义的类方法，那么当你用子类来调用这个类方法的时候，实际上执行的是子类中的版本，这就是方法重写。所以，Objective-C 的&quot;静态方法&quot;是可以被子类重写的。</p><p>然而这和其他一些语言如 Java 或 C++ 中的静态方法是不一样的。在 Java 或 C++ 中，声明为 &quot;static&quot; 的方法是不能被子类重写的。</p></details><h3 id="❓怎么保证自己的类一定能调用到自己写的方法" tabindex="-1"><a class="header-anchor" href="#❓怎么保证自己的类一定能调用到自己写的方法" aria-hidden="true">#</a> ❓怎么保证自己的类一定能调用到自己写的方法</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，分类（Category）是一种可以为已存在的类添加新方法的方式。同时，分类中的方法会替换掉原来类中同名的方法。此特性在一些情况下可能引起不确定的行为，比如可能无意间覆盖掉你写的方法。</p><p>若要阻止自己写的方法被分类覆盖，可以考虑以下方法：</p><ol><li><p><strong>方法命名规范：</strong> 尽可能为方法命名时，添加前缀。这样可以避免和系统默认的方法或他人写的分类中的方法冲突。</p></li><li><p><strong>使用子类化代替分类：</strong> 如果你控制的类可能被其他开发者使用，那么建议使用子类代替分类。这样，即使有人创建了同名的分类，也不会对原类产生影响。</p></li><li><p><strong>在源代码级别进行检查：</strong> 在项目编译的时候，通过检查操作来警告和暴露任何重定义的方法。</p></li></ol><p>但是，以上的方式并不能100%的避免在运行时被覆盖的情况，因为 Objective-C 是一种动态类型的语言，它允许在运行时添加或替换方法。这种动态性是 Objective-C 的一大特性，但同时也可能带来风险。所以编程时应该遵循良好的开发习惯，避免不必要的麻烦。</p></details><h3 id="❓oc-中的类方法和实例方法有什么本质区别和联系" tabindex="-1"><a class="header-anchor" href="#❓oc-中的类方法和实例方法有什么本质区别和联系" aria-hidden="true">#</a> ❓<code>OC</code> 中的类方法和实例方法有什么本质区别和联系</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 的 runtime 层面，类方法和实例方法有一些本质的区别：</p><p><strong>存储的位置：</strong></p><p>实例方法列表存储在类对象（Class）中，而类方法列表存储在元类对象（Meta-Class）中。</p><p><strong>调用者：</strong></p><p>实例方法由实例对象调用，即 <code>[instanceObject exampleMethod];</code>，其中<code>exampleMethod</code>是实例对象的方法；类方法由类对象调用，即 <code>[ClassObject exampleMethod];</code>，其中<code>exampleMethod</code>是类方法。</p><p><strong>方法查找过程：</strong></p><p>调用实例方法时，runtime 系统会在类的方法列表中查找这个方法；调用类方法时，runtime 系统会在元类的方法列表中查找这个方法。</p><p><strong>self 指针：</strong></p><p>在实例方法和类方法中，self 指针的含义是不同的。在实例方法中，self 指向调用这个方法的实例对象；而在类方法中，self 指向调用这个方法的类对象。</p><p><strong>能访问的变量：</strong></p><p>实例方法可以访问实例变量和静态变量，而类方法只能访问静态变量。</p><p>在类方法和实例方法之间也存在一些联系。例如，它们都可以调用彼此。实例方法可以通过实例化对象来调用类方法，类方法也可以通过创建类的实例来调用实例方法。它们都是满足 Objective-C 面向对象特性的一部分，各自处理与对象或类相关的行为。</p></details><h3 id="❓在类方法中是否可以使用-self" tabindex="-1"><a class="header-anchor" href="#❓在类方法中是否可以使用-self" aria-hidden="true">#</a> ❓在类方法中是否可以使用 <code>self</code></h3><details class="hint-container details"><summary>💡</summary><p>在Objective-C中，你可以在类方法中使用<code>self</code>。但在类方法中，<code>self</code>表示的是类对象本身，而不是类的实例。这意味着你不能用<code>self</code>来访问实例变量，只能用它来调用其他类方法。例如：</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token operator">+</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>aClassMethod <span class="token punctuation">{</span>
    <span class="token comment">// 这里的self是类对象本身</span>
    <span class="token punctuation">[</span><span class="token keyword">self</span> anotherClassMethod<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个例子中，<code>anotherClassMethod</code>会被正确地调用，因为<code>self</code>在这里代表类对象。</p><p>但是，你不能在类方法中使用<code>self</code>来访问实例变量或调用实例方法，因为在类方法中没有实例来调用它们。尝试这样做会在编译时出错。</p><p>总的来说，<code>self</code>在类方法中指的是类对象本身，用来调用其他类方法；在实例方法中指的是调用该方法的实例对象，用来访问实例变量或调用实例方法。</p></details><h3 id="❓sel、-cmd-是什么" tabindex="-1"><a class="header-anchor" href="#❓sel、-cmd-是什么" aria-hidden="true">#</a> ❓<code>SEL</code>、<code>_cmd</code> 是什么</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，<code>SEL</code> 是 <code>selector</code> 在 Obj-C 中的类型，<code>_cmd</code> 是当前方法的 <code>SEL</code> 。<code>SEL</code> 是方法选择器，可以看做是一个查找表，用于在运行时查找方法的实现。</p><p>具体来说：</p><ol><li><p>SEL： 是一个表示方法的类型，实际上是一个字符串，不同的方法对应不同的 <code>SEL</code> 。例如 <code>SEL methodSEL = @selector(methodName);</code></p></li><li><p>_cmd：是一个隐藏的参数，表示当前调用的方法的 <code>SEL</code>。每个方法的实现的第二个参数_cmd，它是一个 <code>SEL</code> 类型的，即当前方法的选择器。</p></li></ol><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>someMethod <span class="token punctuation">{</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> <span class="token function">NSStringFromSelector</span><span class="token punctuation">(</span>_cmd<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出 &quot;someMethod&quot;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这个例子中，_cmd 被用来输出当前方法的名称。通常情况下，你可能不需要直接使用 _cmd，但它在调试，特别是在动态方法解析（dynamic method resolution）时非常有用。</p></details><h3 id="❓oc-中对象消息发送的大致流程" tabindex="-1"><a class="header-anchor" href="#❓oc-中对象消息发送的大致流程" aria-hidden="true">#</a> ❓<code>OC</code> 中对象消息发送的大致流程</h3><details class="hint-container details"><summary>💡</summary><p>Objective-C 对象消息发送主要包含以下步骤：</p><ol><li><p><strong>消息发送者：</strong> 当调用一个 Objective-C 对象的方法时，实际上是向这个对象发送了一条消息。例如，<code>[object methodWithParameter:parameter];</code>，这里的“object”是消息的接受者，“methodWithParameter:”是消息的选择器（SEL，Selector）。</p></li><li><p><strong>动态绑定：</strong> Objective-C 采用动态绑定，不是在编译的时候决定函数地址，而是在运行时根据接收者和选择器找到对应的函数实现。</p></li><li><p><strong>寻找接受者：</strong> 明确了消息的接收者后，OC 会在对应的类以及其父类中查找这个方法的实现。如果在消息接收者的类中找不到响应的方法，则继续往它的父类中寻找。</p></li><li><p><strong>消息转发：</strong> 如果到了根类（一般是 NSObject ）都没有找到方法实现，那么就会进入对象的 <code>doesNotRecognizeSelector:</code> 方法，进入此方法就算出错了，未对此方法做处理的话程序会 crash。不过，NSObject 为我们提供了最后挽救的机会，走进消息转发机制，你可以指定一个新的接受者来响应这条消息。如果消息转发也没有处理，就真的会 crash 了。</p></li></ol><p>以上就是 Objective-C 中对象消息发送的主要流程。这些步骤大大增加了 Objective-C 的灵活性，让代码具有更好的扩展性和维护性。即使一个对象没有实现某个方法，也不会在编译阶段报错，而会在运行时进入消息转发阶段，给我们处理未实现方法的机会。</p></details><h3 id="❓向一个-nil-对象发送消息将会发生什么" tabindex="-1"><a class="header-anchor" href="#❓向一个-nil-对象发送消息将会发生什么" aria-hidden="true">#</a> ❓向一个 <code>nil</code> 对象发送消息将会发生什么</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，向 <code>nil</code> 对象发送消息是安全的，不会导致程序崩溃。这一特性是 Objective-C 语言中的一项重要特性，使其在处理对象的空态时更加灵活。</p><p>对一个 <code>nil</code> 对象发送消息，下面是可能会发生的情况：</p><ol><li><p><strong>方法返回非对象类型：</strong> 如果该消息是一个返回非对象类型的方法（例如 int，float，struct 等），会返回零值（0，0.0，相应类型的零值等）。</p></li><li><p><strong>方法返回对象类型：</strong> 如果该消息是一个返回对象的方法，那么将会返回 <code>nil</code>。</p></li><li><p><strong>方法不返回值（例如 void 类型）：</strong> 如果该消息是一个不返回值的方法，那么什么都不会发生。</p></li></ol><p>这种特性使得你可以在不检查一个对象是否为 <code>nil</code> 的情况下向它发送消息，这通常能使你的代码更加简洁。反过来，如果你在像其他语言（例如 Java 或 C#）那样，在向对象发送消息之前总是先检查它是否为 <code>nil</code>，那么你可能会编写出很多不必要的代码。然而，注意这并不意味着你可以忽略 <code>nil</code> 检查，在某些情况下，<code>nil</code> 检查仍然是必要的。</p><p>需要注意的是，向一个 <code>nil</code> 对象发送消息会返回 <code>nil</code> 或零值，但是在某些情况下，这可能会隐藏程序中的错误，如果密切注意这一点，这一特性可以使你的代码更加简洁和强大。</p></details><h3 id="❓向一个对象发送消息-obj-foo-和-objc-msgsend-函数之间有什么关系" tabindex="-1"><a class="header-anchor" href="#❓向一个对象发送消息-obj-foo-和-objc-msgsend-函数之间有什么关系" aria-hidden="true">#</a> ❓向一个对象发送消息 <code>[obj foo]</code> 和 <code>objc_msgSend()</code> 函数之间有什么关系</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 语言中，<code>[obj foo]</code> 这种语义实际上是在运行时通过调用 <code>objc_msgSend(obj, @selector(foo))</code> 来实现的。</p><p>在 Objective-C 中，方法(methods)的调用被称为向一个对象发送消息。当你使用方括号语法向对象发送消息时，如 <code>[obj foo]</code>，实际上 Objective-C 的运行时系统会将其转化为 <code>objc_msgSend()</code> 函数的调用。</p><p><code>objc_msgSend()</code> 是 Objective-C runtime 库中的函数，它接受两个以上的参数。第一个参数(receiver)是你要发送消息的对象，第二个参数(selector)是你要发送的消息(method)。例如，<code>objc_msgSend(obj, @selector(foo))</code> 就是向 obj 对象发送名为 foo 的消息。</p><p>这表明了 Objective-C 中的方法调用实际上是基于运行时的消息传递机制。Objective-C 将类和实例的方法调用抽象化为对特定对象发送特定消息，通过动态的消息分派机制找到并执行对应的方法实现。这种特性也是 Objective-C 存在动态特性的基础。</p></details><h3 id="❓一个对象向一个不存在的方法发送消息-消息是如何转发的" tabindex="-1"><a class="header-anchor" href="#❓一个对象向一个不存在的方法发送消息-消息是如何转发的" aria-hidden="true">#</a> ❓一个对象向一个不存在的方法发送消息，消息是如何转发的</h3><details class="hint-container details"><summary>💡</summary><p>当 Objective-C 的一个对象接收到一个不存在的方法时，程序将通过以下步骤进行消息转发：</p><ol><li><p>动态方法解析（Dynamic Method Resolution） 运行时系统会调用 <code>+resolveInstanceMethod:</code>（对于类方法是 <code>+resolveClassMethod:</code>）。在这个阶段，我们可以为这个类动态添加一个方法。如果此方法返回 <code>YES</code>，那么运行时系统会重新发送消息。</p></li><li><p>备援接收者（Backup Receiver） 如果第一步未处理消息（即 <code>+resolveInstanceMethod:</code> 返回 <code>NO</code>），系统会调用 <code>-forwardingTargetForSelector:</code> 方法，允许我们返回一个替代的对象。这个方法返回了一个实例后，系统会把这条消息发送给这个实例。</p></li><li><p>完整消息转发（Complete Message Forwarding） 如果 <code>-forwardingTargetForSelector:</code> 返回了 <code>nil</code>，运行时就会进入完整的消息转发阶段。首先会发送 <code>-methodSignatureForSelector:</code> 消息来获取方法的返回和参数类型。然后会发送 <code>-forwardInvocation:</code> 消息。我们可以在 <code>-forwardInvocation:</code> 中自行处理消息，或者转发给其他对象处理。</p></li><li><p>消息转发失败 如果 在 <code>-forwardInvocation:</code> 中未处理消息，那么运行时会调用 <code>-doesNotRecognizeSelector:</code> 方法， 默认会抛出异常。</p></li></ol><p>需要注意的是，消息转发虽然强大，但由于需要经过多次查找，性能开销较大。当频繁调用时应用程序的运行效率会受到很大影响。因此，我们通常仅在需要动态处理未知消息时才会使用消息转发。</p></details><h3 id="❓unrecognized-selector-异常是如何发生-应该如何避免" tabindex="-1"><a class="header-anchor" href="#❓unrecognized-selector-异常是如何发生-应该如何避免" aria-hidden="true">#</a> ❓<code>unrecognized selector</code> 异常是如何发生，应该如何避免</h3><details class="hint-container details"><summary>💡</summary><p><code>unrecognized selector</code> 异常是 Objective-C 中的一个常见运行时错误，当向一个对象发送它不能响应的消息时，就会抛出这个异常。</p><p>在 Objective-C 中，当你调用一个对象的方法时，实际上是在向这个对象发送一条消息。运行时系统会在对象的类及其所有的父类中寻找与这个消息对应的方法。如果找到这个方法，就会调用它。如果在最底层的父类（通常是 NSObject）中也找不到这个方法，那么就会抛出<code>unrecognized selector</code>异常。</p><p>避免 <code>unrecognized selector</code> 异常的方法有以下几种：</p><ol><li><strong>检查对象是否能响应此消息：</strong> 在向对象发送消息之前，使用 <code>respondsToSelector:</code> 方法检查这个对象是否能响应这个消息。</li></ol><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token punctuation">[</span>obj respondsToSelector<span class="token punctuation">:</span><span class="token keyword">@selector</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token punctuation">[</span>obj foo<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ol start="2"><li><p><strong>使用异常处理：</strong> Objective-C 提供了异常处理语句 @try、@catch、@finally。你可以将可能抛出异常的代码放在 @try 块中，将处理异常的代码放在 @catch 块中。</p></li><li><p><strong>消息转发：</strong> 如果一个对象无法识别引发异常的方法，那么在引发异常之前，会先调用这个对象的 <code>- (void)forwardInvocation:(NSInvocation *)anInvocation;</code> 方法，我们可以在这个方法里面向其他能响应此消息的对象发送该消息。这就相当于将此消息转发给其他对象。</p></li></ol><p>以上就是避免 <code>unrecognized selector</code> 异常的一些方法，希望对你有所帮助。</p></details><h3 id="❓-objc-msgforward-函数是做什么的-直接调用它将会发生什么" tabindex="-1"><a class="header-anchor" href="#❓-objc-msgforward-函数是做什么的-直接调用它将会发生什么" aria-hidden="true">#</a> ❓<code>_objc_msgForward</code> 函数是做什么的，直接调用它将会发生什么</h3><details class="hint-container details"><summary>💡</summary><p><code>_objc_msgForward</code> 是 Objective-C 运行时系统中用于对消息进行转发（Message Forwarding）的函数。</p><p>在 Objective-C 中，当你向一个对象发送一条消息，运行时系统会在该对象的类以及其父类链中查找是否有可以响应这条消息的方法。如果没有找到的话，运行时系统会调用 <code>_objc_msgForward</code> 函数进行消息转发。</p><p>消息转发是 Objective-C 动态特性的一部分，它允许你在运行时动态地处理不能被对象直接响应的消息。也就是说，你可以将消息转发给其他可以处理此消息的对象，或者生成可以处理此消息的方法。</p><p>也就是说，你一般不会直接调用 <code>_objc_msgForward</code> 函数。因为这个函数是由运行时系统在需要进行消息转发时自动调用的。</p><p>如果你硬要直接调用 <code>_objc_msgForward</code>，那么一般而言，这将会引起 <code>unrecognized selector</code> 异常，因为这个函数的作用就是处理不能识别的选择器（即方法）。如果你手动调用它，系统会在尝试进行消息转发时找不到能响应此消息的对象或方法，从而引发异常。基本上，直接调用 <code>_objc_msgForward</code> 而没有适当的转发目标和处理机制，是没有意义的。</p></details><h3 id="❓oc-中是如何通过-selector-找到对应的-imp-地址" tabindex="-1"><a class="header-anchor" href="#❓oc-中是如何通过-selector-找到对应的-imp-地址" aria-hidden="true">#</a> ❓<code>OC</code> 中是如何通过 <code>selector</code> 找到对应的 <code>IMP</code> 地址</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 中，通过 selector 找到对应的 IMP（Implementation）地址的过程是这样的：</p><ol><li><p><strong>查找类方法列表：</strong> 当你向一个对象发送一条消息，运行时系统首先会在该对象的类的方法列表中查找与这个 selector 相对应的方法。如果找到，那么就直接调用这个方法的实现。在这个步骤中，selector 会被映射到其对应方法的 IMP。</p></li><li><p><strong>从父类中查找：</strong> 如果在对象的类的方法列表中没有找到对应的方法，那么运行时系统会在该类的父类中查找，如果在父类中找到，同样直接调用对应的 IMP。这个过程会一直沿着类的继承链查找，直到 NSObject。</p></li><li><p><strong>调用动态方法解析：</strong> 如果在整个继承链上都没有找到对应的方法，那么运行时系统会给你一个“补救”的机会，即会调用该类的 <code>+resolveInstanceMethod:</code> 或 <code>+resolveClassMethod:</code> 方法（取决于是实例方法还是类方法）。你可以在此方法中为对应的 selector 动态添加实现。</p></li><li><p><strong>消息转发：</strong> 如果 <code>+resolveInstanceMethod:</code> 或 <code>+resolveClassMethod:</code> 返回NO或者没有被重写，那么运行时就会启动完整消息转发，此时会调用 <code>- (void)forwardInvocation:(NSInvocation *)anInvocation;</code> 方法。你还可以在这个方法内将消息转发给能响应此消息的其他对象。</p></li><li><p><strong>抛出异常：</strong> 如果以上步骤都没有找到并执行相应的方法，那么运行时系统就会抛出 <code>unrecognized selector</code> 异常。</p></li></ol><p>以上就是 Objective-C 通过 selector 找到对应的 IMP 地址的过程。你可能已经注意到了，这个过程包含了运行时系统的很多动态特性，使得我们能在运行时动态地处理对象的行为。</p></details><h3 id="❓c-调用虚方法与-oc-发消息有什么区别" tabindex="-1"><a class="header-anchor" href="#❓c-调用虚方法与-oc-发消息有什么区别" aria-hidden="true">#</a> ❓<code>C++</code> 调用虚方法与 <code>OC</code> 发消息有什么区别</h3><details class="hint-container details"><summary>💡</summary><p>C++ 虚方法和 Objective-C 发消息是两种不同编程语言的两种不同的机制，它们在工作原理和应用场景上都有一些不同。</p><p><strong>C++ 虚方法：</strong> 是 C++ 支持多态性的一种方式。在 C++ 中，如果一个父类指针指向派生类对象，并通过该指针调用虚函数，那么调用的将是派生类中的虚函数实现。这是通过 C++ 的虚函数表（vtable）来实现的，虚函数表是在编译期确定的。</p><p><strong>Objective-C 发消息：</strong> 是 Objective-C 进行方法调用的方式。它会在运行时动态地确定应该调用对象的哪个方法。当给 Objective-C 对象发送消息时，运行时系统会搜索接收者的类以及所有父类的方法列表，找到第一个可以响应该消息的方法并调用。</p><p>这两种机制的主要区别在于：</p><ol><li><p><strong>动态性：</strong> Objective-C 的消息发送更具有动态性，因为它在运行时确定方法调用，而 C++ 的虚函数在编译时已经确定。</p></li><li><p><strong>灵活性：</strong> Objective-C 的消息发送机制允许我们在运行时为对象动态添加方法，这使得 Objective-C 具有更高的灵活性。</p></li><li><p><strong>性能：</strong> 由于C++的虚函数调用是在编译期就已经确定，因此它的性能上优于 Objective-C 的动态消息发送。</p></li><li><p><strong>安全性：</strong> C++的类型检查更为严格，因此调用虚方法要比 Objective-C 发送消息更安全，后者如果发送了对象无法响应的消息就会在运行时崩溃。</p></li></ol><p>由于这两种机制的区别，所以在实际开发中，我们需要根据不同的场景和需求选择使用 C++ 的虚函数还是 Objective-C 的消息发送。</p></details><h3 id="❓消息转发机制和其他语言的消息机制优劣对比" tabindex="-1"><a class="header-anchor" href="#❓消息转发机制和其他语言的消息机制优劣对比" aria-hidden="true">#</a> ❓消息转发机制和其他语言的消息机制优劣对比</h3><details class="hint-container details"><summary>💡</summary><p>Objective-C 的消息转发机制是一种非常强大的动态特性，在一些其他的面向对象编程语言中，并没有类似的功能。下面我们从几个方面来分析 Objective-C 消息转发机制和其他语言的消息机制的优劣。</p><p><strong>1. 动态性：</strong> Objective-C 的消息转发机制可以让我们在运行时动态地处理未知的消息。而其他一些语言，如 C++, Java, C# 等，在编译期间就已经确定了方法的调用，并不能在运行时动态地处理未知的消息。</p><p><strong>2. 灵活性：</strong> Objective-C 的消息转发可以不仅仅是转发给其他对象，甚至可以动态地为类生成新的方法。这在很多其他语言中是无法做到的。</p><p><strong>3. 性能：</strong> Objective-C 的消息转发机制相对于其他语言的消息机制，如 C++ 的虚函数，性能上会稍差一些。因为 Objective-C 是在运行时通过查找类的方法列表来确定方法调用，而不是在编译时就确定。</p><p><strong>4. 安全性：</strong> Objective-C 的消息转发机制如果使用不当，可能会产生一些难以发现的错误。例如，如果错误地转发了消息，可能会导致运行时崩溃。而其他一些语言，如 Java, C#, 是在编译期就检查了方法的调用，相对来说更加安全。</p><p>所以，Objective-C 的消息转发机制和其他语言的消息机制各有优劣，需要根据具体的需求和场景来选择使用哪种机制。</p></details><h3 id="❓在方法调用的时候-方法查询-动态解析-消息转发-之前做了什么" tabindex="-1"><a class="header-anchor" href="#❓在方法调用的时候-方法查询-动态解析-消息转发-之前做了什么" aria-hidden="true">#</a> ❓在方法调用的时候，<code>方法查询-&gt; 动态解析-&gt; 消息转发</code> 之前做了什么</h3><details class="hint-container details"><summary>💡</summary><p>在 Objective-C 运行时系统中，仅当通过发送消息调用方法失败的时候，才会启动方法查找（method lookup）、动态解析（dynamic resolution）和消息转发（message forwarding）这些步骤。在这之前，已经经过了一系列过程：</p><ol><li><p><strong>消息发送：</strong> 当代码中发送了一条消息给某个 Objective-C 对象时，实际上是调用了 Objective-C 运行时库中的 <code>objc_msgSend</code> 函数。方法的调用者（即消息的接收者）和方法选择器（selector）都作为 <code>objc_msgSend</code> 的参数。</p></li><li><p><strong>查找对象的类：</strong> <code>objc_msgSend</code> 需要知道对象的类信息，以便在类的方法列表里查找可以响应该消息的方法。对象内部有一个指针指向它的类，<code>objc_msgSend</code> 可以通过这个指针获取对象的类。</p></li><li><p><strong>缓存查找：</strong> Objective-C 运行时系统为了优化性能，已经将一部分方法的调用结果缓存起来，所以 <code>objc_msgSend</code> 会先从缓存中查找 selector 对应的方法实现。如果缓存中有，则直接调用该方法结束查找流程；如果没有，进入下一步。</p></li><li><p><strong>方法列表查找：</strong> <code>objc_msgSend</code> 会在对象的类的方法列表中搜索 selector 对应的方法。如果找到，则调用该方法，将方法结果添加到缓存中，并结束查找流程。</p></li></ol><p>以上就是在“方法查询 -&gt; 动态解析 -&gt; 消息转发”之前所做的事情。如果经过上述步骤仍然找不到可以响应消息的方法，运行时系统就会启动方法查询、动态解析等步骤，尝试在运行时解决这个问题。</p></details><h3 id="❓下面代码能正常运行吗-如果能-输出是什么" tabindex="-1"><a class="header-anchor" href="#❓下面代码能正常运行吗-如果能-输出是什么" aria-hidden="true">#</a> ❓下面代码能正常运行吗(如果能，输出是什么)</h3><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> Person <span class="token punctuation">:</span> NSObject 
<span class="token keyword">@property</span><span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span>copy<span class="token punctuation">)</span> NSString <span class="token operator">*</span>name<span class="token punctuation">;</span> 
<span class="token keyword">@end</span>
<span class="token keyword">@implementation</span> Person
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>speak <span class="token punctuation">{</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;My name is:%@&quot;</span><span class="token punctuation">,</span><span class="token keyword">self</span><span class="token punctuation">.</span>name<span class="token punctuation">)</span><span class="token punctuation">;</span> 
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> ViewController
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>viewDidLoad <span class="token punctuation">{</span>
    <span class="token punctuation">[</span><span class="token keyword">super</span> viewDidLoad<span class="token punctuation">]</span><span class="token punctuation">;</span>
    
    id cls <span class="token operator">=</span> <span class="token punctuation">[</span>Person class<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token keyword">void</span> <span class="token operator">*</span>obj <span class="token operator">=</span> <span class="token operator">&amp;</span>cls<span class="token punctuation">;</span>
    <span class="token punctuation">[</span><span class="token punctuation">(</span>__bridge id<span class="token punctuation">)</span>obj speak<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><details class="hint-container details"><summary>💡</summary><p>这段代码可以正常运行，其输出将是 <code>My name is:(null)</code>。</p><p>在这段代码中 <code>id cls = [Person class];</code>这行代码是获取 Person 类。然后 <code>void *obj = &amp;cls;</code> 这行代码是获取了指向 cls 的指针，所以 obj 指向的是一个 Person 类的对象，而不是 Person 类的实例。然后 <code>[(__bridge id)obj speak];</code> 这行代码就是发送一个消息给 obj，所以最终调用的是 Person 类的 speak 方法。</p><p>但是这里你需要注意的是，我们并没有给 name 属性赋值，所以当我们打印 name 属性的时候，它的值是 nil，也就是 <code>(null)</code>。</p></details>`,93);function v(h,y){const s=o("ExternalLinkIcon");return c(),t("div",null,[i(" more "),r,n("details",u,[k,n("p",null,[n("a",m,[p("👉🏻"),l(s)])])]),b])}const _=e(d,[["render",v],["__file","0x01.objective-c.html.vue"]]);export{_ as default};
