import{_ as t}from"./plugin-vue_export-helper-x3n3nnut.js";import{r as d,o as i,c as p,a as o,b as n,e as a,d as e,f as c}from"./app-vBpLfDrO.js";const l={},r=n("h2",{id:"reference",tabindex:"-1"},[n("a",{class:"header-anchor",href:"#reference","aria-hidden":"true"},"#"),a(" reference")],-1),u={href:"https://blog.ibireme.com/2013/11/27/objc-block/",target:"_blank",rel:"noopener noreferrer"},k={href:"http://www.cnblogs.com/biosli/archive/2013/05/29/iOS_Objective-C_Block.html",target:"_blank",rel:"noopener noreferrer"},m={href:"https://github.com/nixzhu/dev-blog/blob/master/2015-12-09-nscache.md",target:"_blank",rel:"noopener noreferrer"},b={href:"http://southpeak.github.io/2015/02/11/cocoa-foundation-nscache",target:"_blank",rel:"noopener noreferrer"},v={href:"http://nshipster.com/nscache",target:"_blank",rel:"noopener noreferrer"},h=c(`<hr><h2 id="设计理念" tabindex="-1"><a class="header-anchor" href="#设计理念" aria-hidden="true">#</a> 设计理念</h2><p><code>OC</code> 底层是使用 <code>C/C++</code> 实现。定一个继承 <code>NSObject</code> 的类，这个类底层使用 <code>C</code> 语言中的 <code>struct</code> 结构体来实现的。</p><h3 id="❓oc、java、python-运行效率孰高" tabindex="-1"><a class="header-anchor" href="#❓oc、java、python-运行效率孰高" aria-hidden="true">#</a> ❓<code>OC、Java、Python</code> 运行效率孰高</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>Objective-C</code>: 编译型语言，运行效率通常优于解释型语言。底层是基于 <code>C</code>，但是由于动态派发的特性会一定程度上影响效率。</p></li><li><p><code>Java</code>: 编译型语言，运行环境是 <code>JVM</code>(<code>Java</code> 虚拟机)，通过 <code>JIT</code>(Just In Time compiler)技术，<code>Java</code> 能将字节码实时地编译成机器码运行，因此运行效率<strong>较高</strong>，而且在大数据和 <code>Web</code> 服务领域表现优秀。</p></li><li><p><code>Python</code>: 是解释型语言，其运行效率<strong>较低</strong>，无法与编译型语言相比。但是 <code>Python</code> 的易读性、易写性以及丰富的第三方库，使它在科学计算、人工智能等领域得到了广泛的应用。</p></li></ul></details><h3 id="❓oc-的动态性" tabindex="-1"><a class="header-anchor" href="#❓oc-的动态性" aria-hidden="true">#</a> ❓<code>OC</code> 的动态性</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>OC</code> 的 <code>runtime</code> 文档中介绍中就详细写明了，<code>runtime</code> 的设计是为了将更多的处理延迟到编译后。</p></blockquote><ul><li><p>动态类型 (<code>Dynamic typing</code>) : 编译时并不确定对象类型，在运行时才判断对象的类型。<code>OC</code> 中一切对象都可以用 <code>id</code> 指针指向。</p><ul><li><code>isKindOfClass:</code> : 判断某一对象是否是某个类或其子类的实例</li><li><code>isMemberOfClass:</code> : 判断某一对象是否是某个类的实例</li></ul></li><li><p>动态绑定 (<code>Dynamic binding</code>) : 方法的调用也是运行时才决定，而并非编译时。</p><ul><li>传统函数 : 编译时期，将函数参数和实现打包到源码</li><li><code>OC</code> 的消息机制 : 运行时，才去动态查找与实例绑定的属性和方法</li></ul></li><li><p>动态加载 (<code>Dynamic loading</code>) : 运行时可以动态加载其它模块或框架。</p><ul><li>运行时启动时才进行加载(如 : <code>+load</code> 函数中的一些初始化)</li><li>根据需求动态加载资源(如 : @2x,@3x图片的加载)</li></ul></li></ul></details><h3 id="❓oc-是如何保证系统升级后-abi-的稳定性" tabindex="-1"><a class="header-anchor" href="#❓oc-是如何保证系统升级后-abi-的稳定性" aria-hidden="true">#</a> ❓<code>OC</code> 是如何保证系统升级后 <code>ABI</code> 的稳定性</h3><details class="hint-container details"><summary>💡</summary><ul><li><p>向后兼容（<code>Backward Compatibility</code>）：新版本的系统库在设计和更新时，会尽量保证向下兼容。</p></li><li><p>动态性（<code>Dynamic Nature</code>）：作为一种动态语言，<code>OC</code> 大量使用动态分发和运行时解析。这意味着很多事情在运行时才确定，例如方法的调用、对象的生成等。这种机制在维护 <code>ABI</code> 稳定性时有很大的灵活性，可以在运行时解析和处理一些可能的问题。</p></li><li><p>利用类和分类（<code>Class and Category</code>）：使用类和分类来模块化代码，使得代码逻辑更容易管理，也更利于维护 <code>ABI</code> 稳定性。在新增或者修改功能时，可以通过添加新的类或者分类，而不是修改旧的代码，这样就能保证旧的代码逻辑和 <code>ABI</code> 不被破坏。</p></li><li><p>接口过期标注(<code>API Deprecation</code>)：系统库在引入新的接口时，会把不推荐使用的旧接口标记为 <code>deprecated</code>，但不会立即删除，这样就可以保证依赖旧接口的代码在一段时间内可以正常运行。</p></li></ul><p>注：以上这些都只能尽可能地保证 <code>ABI</code> 的稳定性，但并不能绝对地保证。因为系统升级往往会引入新的功能、新的架构，或者修复旧的 <code>bug</code>，这些都可能对 <code>ABI</code> 产生影响。所以，作为开发者在编写代码时，也需要注意兼容性，避免过度依赖可能会变动的接口和行为。</p></details><hr><h2 id="数据类型" tabindex="-1"><a class="header-anchor" href="#数据类型" aria-hidden="true">#</a> 数据类型</h2><h3 id="id" tabindex="-1"><a class="header-anchor" href="#id" aria-hidden="true">#</a> id</h3><h4 id="❓id-vs-self-vs-super" tabindex="-1"><a class="header-anchor" href="#❓id-vs-self-vs-super" aria-hidden="true">#</a> ❓<code>id</code> vs <code>self</code> vs <code>super</code></h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>id</code>：一种数据类型，也叫做动态对象类型，所有的 <code>Objective-C</code> 对象都可以用 <code>id</code> 类型来接收。<code>id</code> 类型会忽略类型信息，可以给它发送任何消息，如果在运行时对象能够响应这个消息，则代码可以正常运行，否则会在运行时报错。</p></li><li><p><code>self</code>: 每个对象方法的隐含参数，它指向调用当前方法的对象。<code>self</code> 的数据类型也是 <code>id</code>，但它通常用来引用对象自己。可以用 <code>self</code> 来访问属性或者调用方法。</p></li><li><p><code>super</code>: 并不是一个指针，是一个编译器标识符，代表当前对象的父类。当你向 <code>super</code> 发送消息时，编译器会沿着方法的继承链向上寻找，直到找到对应的方法实现为止。这个机制可用于在子类中调用父类的同名方法，以实现代码的复用。</p></li></ul></details><h4 id="❓id-vs-nsobject" tabindex="-1"><a class="header-anchor" href="#❓id-vs-nsobject" aria-hidden="true">#</a> ❓<code>id</code> vs <code>NSObject</code></h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>id</code>: 一个动态类型的对象指针，它可以指向任何类型的 <code>Objective-C</code> 对象。<code>id</code> 类型的对象可以接收任何消息而不会在编译期报错，运行期若无法响应则会引发异常。使用 <code>id</code> 类型可以编写出非常灵活的代码，但是也需要面对潜在的运行期错误。</p></li><li><p><code>NSObject</code>: 是所有 <code>Objective-C</code> 类的基类，定义了一些所有对象都能响应的基本方法，例如 -<code>init</code>、<code>retain</code>、<code>release</code> 等。如果一个对象类型声明为 <code>NSObject *</code>，只能给它发送 <code>NSObject</code> 类中定义的方法。如果试图发送其他消息，会得到一个编译错误。</p></li></ul></details><h4 id="❓id-vs-instancetype" tabindex="-1"><a class="header-anchor" href="#❓id-vs-instancetype" aria-hidden="true">#</a> ❓<code>id</code> vs <code>instancetype</code></h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>id</code>：一个动态类型的对象指针，可以指向任何类型的 <code>Objective-C</code> 对象。使用 <code>id</code> 类型的变量可以调用任何方法，如果该对象的类实现了该方法，那么就会在运行时调用。缺点是编译器无法进行类型检查和自动补全。</p></li><li><p><code>instancetype</code>：一种特殊的类型，主要用于表示对象的实例，用在方法的返回类型中。与 <code>id</code> 类型不同，方法使用 <code>instancetype</code> 作为返回类型，那么编译器会认为该方法返回一个当前类的实例。这个特性可以帮助编译器进行更准确的类型检查，同时还能提供自动补全的支持。</p></li></ul></details><h4 id="❓id-为什么不能用点语法" tabindex="-1"><a class="header-anchor" href="#❓id-为什么不能用点语法" aria-hidden="true">#</a> ❓<code>id</code> 为什么不能用点语法</h4><details class="hint-container details"><summary>💡</summary><blockquote><p>点语法是一种简化属性访问的语法。底层实际上是编译器做了一个转换操作，把点语法转换成相应的 <code>getter</code>（对于获取属性）或者 <code>setter</code>（对于设置属性）方法的调用。</p></blockquote><ul><li><p>明确类型的对象，编译器可以知道对象的类定义，知道该对象所拥有的属性和方法，所以在编译期可以正确地做出转换。</p></li><li><p><code>id</code> 类型的对象，编译器无法知道它的实际类型以及拥有的属性和方法，所以无法进行点语法的转换。</p></li></ul></details><h3 id="❓nil-vs-nil-vs-null-vs-nsnull" tabindex="-1"><a class="header-anchor" href="#❓nil-vs-nil-vs-null-vs-nsnull" aria-hidden="true">#</a> ❓<code>nil</code> vs <code>Nil</code> vs <code>NULL</code> vs <code>NSNull</code></h3><details class="hint-container details"><summary>💡</summary><blockquote><p>这些都是表示&quot;无&quot;或&quot;空&quot;的特定值</p></blockquote><ul><li><p><code>nil</code>：一个表示对象指针为空的特殊值。</p></li><li><p><code>Nil</code>：一个表示类指针为空的特殊值。</p></li><li><p><code>NULL</code>：一个表示未指向任何东西的原始指针的特殊值。正如你在任何纯<code>C</code>的代码和<code>Objective-C</code>代码中看到的，<code>NULL</code>通常指针表示&quot;没有东西&quot;或者是&quot;无&quot;。</p></li><li><p><code>NSNull</code>：一个表示空值的类，通常在不能使用<code>nil</code>的情况下表示空值。比如在<code>NSArray</code>或<code>NSDictionary</code>对象中，你不能直接使用<code>nil</code>，但你可以用一个<code>NSNull</code>对象来代表&quot;没有值&quot;的情况。</p></li></ul></details><h3 id="❓类簇的优缺点" tabindex="-1"><a class="header-anchor" href="#❓类簇的优缺点" aria-hidden="true">#</a> ❓类簇的优缺点</h3><details class="hint-container details"><summary>💡</summary><blockquote><p>类簇（<code>Class Cluster</code>）是一种设计模式，在 <code>Objective-C</code> 的类库中有广泛的运用，例如<code>NSString</code>，<code>NSArray</code>，<code>NSDictionary</code>等都是类簇的例子。类簇将一些实现细节封装在内部，对外只提供一个简单的公共接口。</p></blockquote><ul><li><p>优点：</p><ul><li><p>封装：类簇将相关的子类放在同一个父类中，使得框架更加简洁，使用者无需熟悉大量类就能够做很多事情。</p></li><li><p>隐藏实现细节：对外只暴露抽象的接口，隐藏了具体实现的细节。这也保护了代码的稳定性，即使在实现过程中改变了相关的类或算法，对外接口的不变就保证了代码的稳定性。</p></li><li><p>代码的分层更加清晰：类簇模式将数据和操作数据的方法绑定在同一个对象上，使得类的分层关系更清楚。</p></li></ul></li><li><p>缺点：</p><ul><li><p>可扩展性差：由于类簇的具体实现不对外暴露，所以如果需要定制某些特殊的行为，扩展性就会变得很差。</p></li><li><p>创建子类困难：由于大部分方法在私有子类中实现，如果要创建类簇的子类并覆盖某些方法，可能需要实现大量的代码才能获得预期的行为。</p></li><li><p>维护难度大：由于类簇中包含了大量的私有子类，如果要修改或扩展代码，就需要对所有的子类进行处理，这会增加维护难度。</p></li></ul></li></ul></details><h3 id="❓nsnumber-是如何实现的" tabindex="-1"><a class="header-anchor" href="#❓nsnumber-是如何实现的" aria-hidden="true">#</a> ❓<code>NSNumber</code> 是如何实现的</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>NSNumber</code>是 <code>Foundation</code> 框架中的一个类，它是包装类型，也是对基本的 <code>C</code> 和 <code>Objective-C</code> 数据类型(<code>Integer</code>、<code>Float</code>、<code>Bool</code>等)进行封装的一个类。在 <code>Objective-C</code> 不能直接在<code>NSArray</code>、<code>NSDictionary</code> 等集合类中存放非对象类型的数据，这时我们就需要用到 <code>NSNumber</code> 这个类。</p></blockquote><p>大致实现原理：<code>NSNumber</code>的对象很可能内部包含一个 <code>union</code> 类型的实例变量，这个 <code>union</code> 包含了所有可以表示的数据类型（整数，浮点，布尔，etc）。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// 构造方法可能是根据传入的参数类型来设置对应的 union 成员变量的值</span>
<span class="token operator">-</span> <span class="token punctuation">(</span>instancetype<span class="token punctuation">)</span>initWithInt<span class="token punctuation">:</span><span class="token punctuation">(</span><span class="token keyword">int</span><span class="token punctuation">)</span>value <span class="token punctuation">{</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">self</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token keyword">super</span> init<span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        _value<span class="token punctuation">.</span>intVal <span class="token operator">=</span> value<span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
    <span class="token keyword">return</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token operator">-</span> <span class="token punctuation">(</span>instancetype<span class="token punctuation">)</span>initWithFloat<span class="token punctuation">:</span><span class="token punctuation">(</span><span class="token keyword">float</span><span class="token punctuation">)</span>value <span class="token punctuation">{</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">self</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token keyword">super</span> init<span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        _value<span class="token punctuation">.</span>floatVal <span class="token operator">=</span> value<span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
    <span class="token keyword">return</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token comment">// 取值方法也是类似，根据取值的类型返回对应的 union 成员变量的值</span>
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">int</span><span class="token punctuation">)</span>intValue <span class="token punctuation">{</span>
    <span class="token keyword">return</span> _value<span class="token punctuation">.</span>intVal<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">float</span><span class="token punctuation">)</span>floatValue <span class="token punctuation">{</span>
    <span class="token keyword">return</span> _value<span class="token punctuation">.</span>floatVal<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="nsstring" tabindex="-1"><a class="header-anchor" href="#nsstring" aria-hidden="true">#</a> NSString</h3><h4 id="❓oc-中的三种字符串" tabindex="-1"><a class="header-anchor" href="#❓oc-中的三种字符串" aria-hidden="true">#</a> ❓<code>OC</code> 中的三种字符串</h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>__NSCFConstantString</code> ：在编译时被创建的常量字符串对象，如<code>NSString *str = @&quot;Hello, World!&quot;</code>。这些对象会在编译时就被创建，并且在程序的整个生命周期中都不会被销毁。</p></li><li><p><code>__NSTaggedPointerString</code> ：使用 <code>Tagged Pointer</code> 技术来提高内存使用效率，通常用于存储较小的字符串数据。</p></li><li><p><code>__NSCFString</code> ：本质上是一个 <code>Core Foundation</code> 字符串的引用（<code>CFStringRef</code>），这种类型的字符串通常会在运行时程序动态创建的对象字符串。</p></li></ul></details><h4 id="❓tagged-pointer-了解吗" tabindex="-1"><a class="header-anchor" href="#❓tagged-pointer-了解吗" aria-hidden="true">#</a> ❓<code>Tagged Pointer</code> 了解吗</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>iOS 7</code> 之后 <code>Apple</code> 为了优化引入的一项技术。从字面意思上来看<code>Tagged Pointer</code>就是带有标签的指针。</p></blockquote><p>一般来说，在 <code>64-bit</code> 架构下，一个指针的值需要占用 <code>8 byte</code> 的内存空间；然而对于一些非常小的数据，比如 <code>NSNumber</code>、<code>NSDate</code>、<code>NSString</code> 等，使用 <code>8 byte</code> 存储对象指针就显得非常浪费。为了解决这个问题，苹果引入了 <code>Tagged Pointer</code> 技术，通过标签直接将小的数据对象嵌入到指针中，这样对于小的数据对象，就无需额外的内存来存储了，能够显著提高内存的利用率。需要注意，一个对象是否使用 <code>Tagged Pointer</code> 是由系统决定的，不能人为控制。</p><p>具体到 <code>NSString</code> 这种场景中，如果字符串的内容非常短，那么它的实现方式可能就是 <code>Tagged Pointer</code>，直接将字符串内容存在指针中，提高了内存利用率。</p></details><h4 id="❓nsstring-如何计算字符的个数" tabindex="-1"><a class="header-anchor" href="#❓nsstring-如何计算字符的个数" aria-hidden="true">#</a> ❓<code>NSString</code> 如何计算字符的个数</h4><details class="hint-container details"><summary>💡</summary><p>可以使用 <code>NSString</code> 的 <code>length</code> 属性来获取字符串长度，但这实际上返回的是字符串中 <code>Unicode</code> 编码的字符数量，并不是通常意义上的字符数量。例如，一个中文字符和一个 <code>emoji</code> 表情，可能都会被算作多个 <code>Unicode</code> 字符。</p><p>可以使用 <code>NSString</code> 的 <code>enumerateSubstringsInRange:options:usingBlock:</code> 方法，用 <code>NSStringEnumerationByComposedCharacterSequences</code> 选项来统计字符串的字符数量。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__block NSUInteger characterCount <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token punctuation">[</span>myString enumerateSubstringsInRange<span class="token punctuation">:</span><span class="token function">NSMakeRange</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> myString<span class="token punctuation">.</span>length<span class="token punctuation">)</span>
                           options<span class="token punctuation">:</span>NSStringEnumerationByComposedCharacterSequences
                        usingBlock<span class="token punctuation">:</span><span class="token operator">^</span><span class="token punctuation">(</span>NSString <span class="token operator">*</span>substring<span class="token punctuation">,</span> NSRange substringRange<span class="token punctuation">,</span> NSRange enclosingRange<span class="token punctuation">,</span> BOOL <span class="token operator">*</span>stop<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    characterCount<span class="token operator">++</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="nsarray" tabindex="-1"><a class="header-anchor" href="#nsarray" aria-hidden="true">#</a> NSArray</h3><h4 id="❓-nsmutablearray-alloc-init-vs-nsmutablearray-array" tabindex="-1"><a class="header-anchor" href="#❓-nsmutablearray-alloc-init-vs-nsmutablearray-array" aria-hidden="true">#</a> ❓<code>[[NSMutableArray alloc] init]</code> vs <code>[NSMutableArray array]</code></h4><details class="hint-container details"><summary>💡</summary><ul><li><p>相同点：都是用来创建一个可变数组的。</p></li><li><p>不同点</p><ul><li>代码简洁度：<code>[NSMutableArray array]</code> 比 <code>[[NSMutableArray alloc] init]</code> 代码更简洁，更易于阅读和理解。</li><li>返回的对象所有权：<code>[[NSMutableArray alloc] init]</code> 返回的是一个引用计数为 1 的对象，而 <code>[NSMutableArray array]</code> 返回的是一个自动释放的对象（<code>autoreleased object</code>）。因此，如果在方法内创建了数组，然后需要返回这个数组，那么 <code>[NSMutableArray array]</code> 是一个更好的选择，因为它返回的对象会在方法结束后自动释放，不需要手动释放。如果是在方法内部创建数组，且数组在方法结束后就不再需要，那么 <code>[[NSMutableArray alloc] init]</code> 是一个更好的选择，因为它的引用计数为1，当不再需要它时，可以通过调用 <code>release</code> 方法将其释放。</li></ul></li></ul></details><h4 id="❓nsmutablearray-的底层实现" tabindex="-1"><a class="header-anchor" href="#❓nsmutablearray-的底层实现" aria-hidden="true">#</a> ❓<code>NSMutableArray</code> 的底层实现</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>NSMutableArray</code> 是一个动态数组，可以增加和删除元素。</p></blockquote><p>实现原理：</p><ul><li><p>在创建 <code>NSMutableArray</code> 时，会分配一段连续的内存空间，用来保存元素。当元素数量超过数组的容量时，<code>Foundation</code> 框架会创建一个新的、更大的数组，然后将旧数组的元素拷贝到新数组中，然后释放旧数组。</p></li><li><p>当从 <code>NSMutableArray</code> 中删除元素时，它并不会立即收缩内存空间。相反，被删除元素的位置会被标记为空闲，可以用来保存新添加的元素。只有当数组的空闲空间达到一定的阈值时，<code>Foundation</code> 框架才会创建一个新的、更小的数组，然后将旧数组的元素拷贝到新数组中，然后释放旧数组。</p></li></ul></details><h3 id="nsdictionary" tabindex="-1"><a class="header-anchor" href="#nsdictionary" aria-hidden="true">#</a> NSDictionary</h3><h4 id="❓nsdictionary-的底层实现" tabindex="-1"><a class="header-anchor" href="#❓nsdictionary-的底层实现" aria-hidden="true">#</a> ❓<code>NSDictionary</code> 的底层实现</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>NSDictionary</code> 是一个以键值对形式存储对象的接口，是一种哈希表实现。</p></blockquote><p>实现原理：</p><ul><li><p>哈希表：在哈希表中，哈希函数将每个键 (<code>key</code>) 映射到表中一个唯一的索引。通过该索引，存储的相关值 (<code>value</code>) 可以快速地被找到。这就是 <code>NSDictionary</code> 可以快速查找键对应值的原因。</p></li><li><p>碰撞处理：当两个或者更多的键映射到哈希表的同一个位置时，就会发生碰撞。通常情况下，有几种策略可以处理这种碰撞，比如链地址法，开放地址法等。不过具体 <code>Foundation</code> 使用哪种策略去处理碰撞我们并不清楚。</p></li><li><p>动态调整：随着数据的增加，为了保持操作的高效，哈希表可能需要进行扩容。扩容经常伴随着数据的重新哈希，为的就是保持哈希值均匀分布，保持优秀的查找效率。</p></li></ul></details><h4 id="❓nsdictionary-vs-nshashtable-vs-nsmaptable" tabindex="-1"><a class="header-anchor" href="#❓nsdictionary-vs-nshashtable-vs-nsmaptable" aria-hidden="true">#</a> ❓<code>NSDictionary</code> vs <code>NSHashTable</code> vs <code>NSMapTable</code></h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>NSDictionary</code>，<code>NSHashTable</code> 和 <code>NSMapTable</code> 都是 <code>Objective-C</code> 中常用的集合类型。</p></blockquote><ul><li><p><code>NSDictionary</code>：存储键值对的集合，每个键都映射到一个值。对键进行 <code>copy</code> 操作，对值进行 <code>retain</code>(强引用)操作。键必须要保持唯一。查找操作性能高效，时间复杂性为 <code>O(1)</code>。</p></li><li><p><code>NSHashTable</code>：一个可以存储任意对象的集合，集合中的每个对象都是唯一的。该类提供了对对象强引用、弱引用的选项，可以更加灵活地进行内存管理。</p></li><li><p><code>NSMapTable</code>：和 <code>NSDictionary</code> 非常相似，但 <code>NSMapTable</code> 对键和值可以选择强引用或弱引用，比 <code>NSDictionary</code> 更加灵活。另外，<code>NSMapTable</code> 允许键和值为<code>nil</code>，而 <code>NSDictionary</code> 则不允许。</p></li></ul></details><blockquote><p>❓各自有哪些使用场景</p></blockquote><details class="hint-container details"><summary>💡</summary><ul><li><p><code>NSDictionary</code>：<code>NSDictionary</code> 由于其高效的查找速度，广泛用于需要依据键查找值的场景。</p><blockquote><p>例如，配置文件的存储和读取，网络请求的参数封装，JSON 数据的解析等。</p></blockquote></li><li><p><code>NSHashTable</code>：<code>NSHashTable</code> 适用于存储不需要重复的对象，并且对内存管理有一定要求的场景。</p><blockquote><p>例如，在缓存中存储对象但不引入强引用，防止内存无法释放的问题；或者在存储 <code>delegate</code> 对象时，避免引入循环引用。</p></blockquote></li><li><p><code>NSMapTable</code>：<code>NSMapTable</code> 可以实现强引用和弱引用键值对，所以适用于一些特殊场景。</p><blockquote><p>例如，在不引入强引用的情况下缓存对象，或者希望跟踪哪些对象引用了其它对象等。</p></blockquote></li></ul></details><h4 id="❓nsdictionary-vs-nscache" tabindex="-1"><a class="header-anchor" href="#❓nsdictionary-vs-nscache" aria-hidden="true">#</a> ❓<code>NSDictionary</code> vs <code>NSCache</code></h4><details class="hint-container details"><summary>💡</summary><p><code>NSDictionary</code> 和 <code>NSCache</code> 都是能够存储键值对的集合，但它们有一些主要的区别，使它们适用于不同的使用场景：</p><ul><li><p><code>NSDictionary</code> 是一个不可变的哈希表，其中的键值对都是强引用的。键是唯一的且不可变的。相比之下，<code>NSCache</code> 是一个可变的集合，其中的键值对都是强引用的，但有一个例外：键不是像 <code>NSDictionary</code> 管理那样通过 <code>copy</code> 管理，而是通过 <code>retain</code> 管理，因此键可以是可变的。</p></li><li><p>另一个重要的区别在于内存管理。<code>NSCache</code> 在系统内存不足时，会自动释放缓存的部分或全部数据以节省内存。这使得在处理大量数据时，<code>NSCache</code> 可以自动处理低内存警告，而 <code>NSDictionary</code> 则需要手动处理低内存警告。</p></li><li><p><code>NSCache</code> 支持通过 <code>NSDiscardableContent</code> 协议来控制缓存内容的生命周期。这样的对象在被缓存时，可以增加一个 <code>beginContentAccess</code> 之后配对的 <code>endContentAccess</code> 调用计数器。当对象的计数器为 0 时，那么这个对象就有可能被清出缓存。</p></li></ul><p>总结：<code>NSDictionary</code> 和 <code>NSCache</code> 的不同主要在于内存管理和可变性。实际上，<code>NSCache</code> 更类似一个优化了内存管理的 <code>NSMutableDictionary</code>，更适合用于存储大量会占据大量内存的对象，例如图片等。</p></details><blockquote><p>❓<code>NSCache</code> 中缓存的对象，在什么时候被释放</p></blockquote><details class="hint-container details"><summary>💡</summary><ul><li><p>内存压力：当操作系统触发低内存警告时，<code>NSCache</code> 会自动释放一些缓存的对象以节约内存。</p></li><li><p>缓存某个对象的 <code>NSCache</code> 实例被销毁时，该对象会被释放。</p></li><li><p>当缓存达到其极限时（这取决于你设置的缓存的数量和总成本的上限），<code>NSCache</code> 可能会开始移除一些对象。</p></li><li><p>如果缓存的对象实现了 <code>NSDiscardableContent</code> 协议，那么对象的释放还会取决于它的 <code>beginContentAccess</code> 和 <code>endContentAccess</code> 方法的调用计数。当这个计数为 0 并且内存压力较大时，对象可能被释放。</p></li></ul></details><h4 id="❓isequal-和-hash-的关系" tabindex="-1"><a class="header-anchor" href="#❓isequal-和-hash-的关系" aria-hidden="true">#</a> ❓<code>isEqual</code> 和 <code>hash</code> 的关系</h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>isEqual:</code> 方法是用来判断两个对象是否相等。</p><blockquote><p>如何判断对象的相等性，可以在自定义类中重写这个方法来自定义判断规则。</p></blockquote></li><li><p><code>hash</code> 方法返回一个无符号整数（<code>NSUInteger</code>）的哈希值，这个值被用于 <code>UITableView</code>、<code>NSSet</code>、<code>NSDictionary</code> 等集合类的操作以提升性能。</p><blockquote><p>对象的哈希值主要是通过对象的内容进行计算得出。</p></blockquote></li><li><p><code>isEqual:</code> 方法和 <code>hash</code> 方法需要保持一致，意思是当两个对象 <code>isEqual</code> 的返回值相等时，它们的 <code>hash</code> 方法返回值必须也相等。</p><blockquote><p>⚠️ 反过来并不一定，即如果两个对象的 <code>hash</code> 值相等，并不一定要求他们 <code>isEqual</code> 值也一定相等，因为不同的对象有可能产生相同的哈希值（通常被称为哈希冲突）。</p></blockquote></li></ul><p>总结：如果需要自定义对象及其相等性的规则，那么通常需要同时重载 <code>isEqual:</code> 和 <code>hash</code> 方法，并确保它们的行为是一致的。</p></details><h4 id="❓ios-中哪些地方用到了哈希算法" tabindex="-1"><a class="header-anchor" href="#❓ios-中哪些地方用到了哈希算法" aria-hidden="true">#</a> ❓<code>iOS</code> 中哪些地方用到了哈希算法</h4><details class="hint-container details"><summary>💡</summary><ul><li><code>Dictionary</code> 和 <code>Set</code> 这两个集合类在进行元素存储时，需要通过 <code>hash</code> 函数来确定元素的存储位置。</li><li>对象间进行比较时，常常会用到 <code>hash</code> 属性。</li><li>在进行一些数据加密或者验证时，比如 <code>MD5</code>、<code>SHA-1</code> 等也是利用了哈希算法来实现的。</li><li>文件校验也经常用哈希。</li></ul></details><hr><h2 id="流程控制" tabindex="-1"><a class="header-anchor" href="#流程控制" aria-hidden="true">#</a> 流程控制</h2><h3 id="❓介绍一下-ios-中的遍历方法" tabindex="-1"><a class="header-anchor" href="#❓介绍一下-ios-中的遍历方法" aria-hidden="true">#</a> ❓介绍一下 <code>iOS</code> 中的遍历方法</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><strong>传统的 for 循环</strong>：对于可以通过索引访问元素的集合（如数组）。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span>NSInteger i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> array<span class="token punctuation">.</span>count<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    id obj <span class="token operator">=</span> array<span class="token punctuation">[</span>i<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token comment">// 使用 obj 和 i</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>for-in 循环</strong>：这是最常用的遍历方式，简单且性能优秀。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span>id obj <span class="token keyword">in</span> array<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 使用 obj</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>Block 枚举</strong>：很多集合类都提供了基于 <code>block</code> 的遍历方法，比如<code>enumerateObjectsUsingBlock:</code>。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">;</span>
<span class="token punctuation">[</span>array enumerateObjectsUsingBlock<span class="token punctuation">:</span><span class="token operator">^</span><span class="token punctuation">(</span>id  _Nonnull obj<span class="token punctuation">,</span> NSUInteger idx<span class="token punctuation">,</span> BOOL <span class="token operator">*</span> _Nonnull stop<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 使用 obj 和 idx</span>
<span class="token punctuation">}</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>NSEnumerator</strong>：一个用于遍历集合的类，使用 <code>nextObject</code> 方法可以取出下一个元素。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">;</span>
NSEnumerator <span class="token operator">*</span>enumerator <span class="token operator">=</span> <span class="token punctuation">[</span>array objectEnumerator<span class="token punctuation">]</span><span class="token punctuation">;</span>
id obj<span class="token punctuation">;</span>
<span class="token keyword">while</span> <span class="token punctuation">(</span>obj <span class="token operator">=</span> <span class="token punctuation">[</span>enumerator nextObject<span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 使用 obj</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul></details><h3 id="❓for-in-遍历的时候在里面增加一个元素会怎么样-删除一个元素会怎么样-为什么" tabindex="-1"><a class="header-anchor" href="#❓for-in-遍历的时候在里面增加一个元素会怎么样-删除一个元素会怎么样-为什么" aria-hidden="true">#</a> ❓<code>for in</code> 遍历的时候在里面增加一个元素会怎么样，删除一个元素会怎么样，为什么</h3><details class="hint-container details"><summary>💡</summary><blockquote><p>在使用 <code>for in</code> 遍历集合类（比如数组或字典）的时候，<code>Objective-C</code> 是不允许在遍历过程中更改集合（即在增加元素或删除元素）的。如果试图在 <code>for in</code> 遍历过程中添加或删除元素，会在运行时抛出异常，具体表现为 <code>NSException</code>。</p></blockquote><p>原因：对于容器的遍历通常使用的是迭代器模式，迭代的过程需要一个稳定的环境。如果在遍历过程中修改了容器（增加或者删除元素），就可能导致迭代器的状态异常，比如迭代顺序出现错误，或者遍历到不存在的元素。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// 可以先将需要删除的元素记录下来，等遍历结束后再进行删除</span>
NSMutableArray <span class="token operator">*</span>removeArray <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span>id obj <span class="token keyword">in</span> originalArray<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token function">shouldRemove</span><span class="token punctuation">(</span>obj<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token punctuation">[</span>removeArray addObject<span class="token punctuation">:</span>obj<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
<span class="token punctuation">[</span>originalArray removeObjectsInArray<span class="token punctuation">:</span>removeArray<span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><hr><h2 id="类" tabindex="-1"><a class="header-anchor" href="#类" aria-hidden="true">#</a> 类</h2><h3 id="load" tabindex="-1"><a class="header-anchor" href="#load" aria-hidden="true">#</a> load</h3><h4 id="❓-load-与-initialize-区别" tabindex="-1"><a class="header-anchor" href="#❓-load-与-initialize-区别" aria-hidden="true">#</a> ❓<code>+load</code> 与 <code>+initialize</code> 区别</h4><details class="hint-container details"><summary>💡</summary><p>这两个都是类方法，可以从下面几个方面去对比两者的区别</p><ul><li><p>调用时机</p><ul><li><code>+load</code> : 这是在运行时加载时，进行触发调用。</li><li><code>+initialize</code> : 会在类第一次使用时，进行触发调用。</li></ul></li><li><p>调用次数</p><ul><li><code>+load</code> : 只会在运行时加载时，调用一次。</li><li><code>+initialize</code> : 第一次调用时，调用一次。</li></ul></li><li><p>调用父类</p><ul><li><code>+load</code> : 不会调用父类的 <code>+load</code></li><li><code>+initialize</code> : 如果当前类没有</li></ul></li><li><p>手动调用</p><ul><li><code>+load</code> : 不能手动调用，运行时进行调用的。手动调用会触发异常。</li><li><code>+initialize</code> : 可以手动调用。</li></ul></li></ul></details><blockquote><p>❓<code>+load</code> 的调用顺序</p></blockquote><details class="hint-container details"><summary>💡</summary><ul><li><code>+load</code> 方法调用顺序与编译连接顺序有关。</li><li>不同类之间的<code>+load</code>方法的调用顺序不能确定，取决于编译连接顺序。</li><li>如果父类、子类和分类都实现了 <code>+load</code> 方法，<code>+load</code> 方法的调用顺序是：先调用父类的<code>+load</code>，再调用子类的<code>+load</code>，最后调用分类的<code>+load</code>。</li></ul></details><h4 id="❓两个类的-load-方法里互相创建另一个类会怎么样" tabindex="-1"><a class="header-anchor" href="#❓两个类的-load-方法里互相创建另一个类会怎么样" aria-hidden="true">#</a> ❓两个类的 <code>load</code> 方法里互相创建另一个类会怎么样</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>load</code> 方法是在一个类被引入到运行时系统时调用，每个类的 <code>load</code> 方法只被调用一次。它们往往在 <code>main</code> 函数启动之前就开始执行。</p></blockquote><p>如果两个类的<code>load</code>方法里互相创建对方：</p><ul><li><p>在加载类 A 时，如果<code>load</code>方法里创建了类 B 的实例，会导致立即加载类 B。</p></li><li><p>在加载类 B 时，如果<code>load</code>方法里创建了类 A 的实例，但此时因为类 A 已经在加载中，所以并不会触发类 A 的<code>load</code>方法。</p></li></ul><p>注意：此时会产生一个循环引用的情况，也有可能导致其他不可预知的副作用或错误，因此在编程过程中应避免这样做。</p><p>在实际开发中，<code>load</code> 方法通常用来执行只需要执行一次并且涉及到类本身的操作，例如注册类到一个注册表，或者设置为观察者等等。在 <code>load</code> 方法中执行耗时操作、修改环境状态或者产生副作用通常是不推荐的。如果需要在类加载时执行初始化代码，可以考虑使用 <code>+initialize</code> 方法，它在类第一次收到消息时被调用，而且如果子类没有定义 <code>initialize</code> 方法，那么父类的 <code>initialize</code> 方法会被调用多次。</p></details><h3 id="初始化" tabindex="-1"><a class="header-anchor" href="#初始化" aria-hidden="true">#</a> 初始化</h3><h4 id="❓alloc、init、new-做了什么" tabindex="-1"><a class="header-anchor" href="#❓alloc、init、new-做了什么" aria-hidden="true">#</a> ❓<code>alloc</code>、<code>init</code>、<code>new</code> 做了什么</h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>alloc</code>：<code>alloc</code> 意为 <code>allocation</code>，用于在堆内存中分配内存，创建对象实例。这个方法会返回一个内存块的地址，这个内存块刚好够存放对象的所有实例变量。但是，这个内存块中的内容是未初始化的。</p></li><li><p><code>init</code>：<code>init</code> 是用来初始化对象的。使用 <code>alloc</code> 分配地址创建的对象，所有的实例变量都会被初始化为 0，但通常需要对这些实例变量进行初始化，所以就调用 <code>init</code> 方法。<code>init</code> 方法会返回一个初始化后的对象。</p></li><li><p><code>new</code>：<code>new</code> 是 <code>alloc</code> 和 <code>init</code> 的简写，它等同于 <code>alloc</code> 方法后面跟一个 <code>init</code> 方法。使用 <code>new</code>方法可以一步完成对象的分配和初始化。</p></li></ul><blockquote><p><code>alloc-init</code> vs <code>new</code></p></blockquote><ul><li>使用 <code>new</code> 执行的是 <code>alloc</code> 和 <code>init</code>，并且两者都不能带参数。</li><li>使用 <code>alloc</code> 和 <code>init</code>，可以对 <code>init</code> 方法传递参数，比如 <code>initWithFrame:</code>， <code>initWithNibName:bundle:</code> 等等。 这也是 <code>new</code> 和 <code>alloc-init</code> 的主要区别，也是为什么在开发中往往使用 <code>alloc-init</code> 来创建对象，而不是 <code>new</code>。</li></ul></details><h4 id="❓ns-designated-initializer-如何添加" tabindex="-1"><a class="header-anchor" href="#❓ns-designated-initializer-如何添加" aria-hidden="true">#</a> ❓<code>NS_DESIGNATED_INITIALIZER</code> 如何添加</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>NS_DESIGNATED_INITIALIZER</code> 是一个宏，用于向编译器标明这个是<code> Designated Initializer</code>。主要目的就是明确指定初始化方法，并帮助我们遵守初始化方法的基本规则：所有的初始化方法最终都应该通过调用同一类中的一个指定初始化方法来完成初始化。</p></blockquote><p><code>NS_DESIGNATED_INITIALIZER</code> 这个宏不是必须的。但加上这个宏可以明确指出哪个是指定初始化方法，有助于提高代码的可读性。另外，如果你在使用 <code>Xcode</code> 开发，并且启用了一些警告选项，没有使用 <code>NS_DESIGNATED_INITIALIZER</code> 宏来标记指定初始化方法可能会导致 <code>Xcode</code> 发出警告信息。</p><p><code>NS_DESIGNATED_INITIALIZER</code> 这个宏的工作原理是，如果你的类中的某个初始化方法使用了这个宏，但是这个方法并没有调用同类中的其他使用了这个宏的初始化方法，或者调用了父类中使用了这个宏的初始化方法，那么编译器就会发出一个警告。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token operator">-</span> <span class="token punctuation">(</span>instancetype<span class="token punctuation">)</span>initWithMyParameter<span class="token punctuation">:</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>myParameter NS_DESIGNATED_INITIALIZER<span class="token punctuation">;</span>
<span class="token keyword">@end</span>
<span class="token keyword">@implementation</span> MyClass
<span class="token operator">-</span> <span class="token punctuation">(</span>instancetype<span class="token punctuation">)</span>initWithMyParameter<span class="token punctuation">:</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>myParameter <span class="token punctuation">{</span>
    <span class="token comment">// 先调用父类的 init</span>
    <span class="token keyword">self</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token keyword">super</span> init<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// 初始化操作...</span>
    <span class="token punctuation">}</span>
    <span class="token keyword">return</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓oc-如何实现多重继承" tabindex="-1"><a class="header-anchor" href="#❓oc-如何实现多重继承" aria-hidden="true">#</a> ❓<code>OC</code> 如何实现多重继承</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><strong>协议（Protocol）</strong>：<code>OC</code> 中的协议类似于其他语言中的接口，可以定义一组方法和属性，然后由类去实现。一个类可以同时实现多个协议，这就实现了类似多重继承的效果，只是实现协议的类需要自己实现协议中定义的所有方法。</p></li><li><p><strong>组合</strong>：在一个类中包含其他类的实例作为成员变量，借此可以使用其他类的功能，间接实现多重继承的效果。</p><blockquote><p>例如，类A包含类B和类C的实例，那么类A就可以使用类B和类C的功能。</p></blockquote></li><li><p><strong>分类（Category）</strong>：一种使得现有类增加方法的方式，是在运行时决议的。可以通过创建分类，将其他类的方法引入，实现类似多继承的效果。但是分类不能添加新的实例变量。</p></li><li><p><strong>消息转发（Message Forwarding）</strong>：允许将一个方法调用转发给其他对象进行处理。如果某个类无法响应某个消息，允许在运行时将这个消息转发给另一个能处理这个消息的对象。</p></li></ul></details><h3 id="反射" tabindex="-1"><a class="header-anchor" href="#反射" aria-hidden="true">#</a> 反射</h3><h4 id="❓oc-中的内省和反射-底层实现原理-应用场景有哪些" tabindex="-1"><a class="header-anchor" href="#❓oc-中的内省和反射-底层实现原理-应用场景有哪些" aria-hidden="true">#</a> ❓<code>OC</code> 中的内省和反射，底层实现原理，应用场景有哪些</h4><details class="hint-container details"><summary>💡</summary><p>内省和反射是基于其动态的特性实现的，底层依赖于 <code>runtime</code> 系统。</p><ul><li><p>内省：面向对象编程中的一种能力，允许程序检查对象的类型或属性。</p><blockquote><p>可以通过系统提供的各种 <code>API</code>（如<code>isKindOfClass:</code>, <code>respondsToSelector:</code> 等方法）来实现内省。</p></blockquote></li><li><p>反射：程序能够观察和修改自身结构和行为的一种能力。</p><blockquote><p>反射主要依赖于系统提供的 <code>API</code>，如<code>NSClassFromString</code>, <code>NSStringFromClass</code>, <code>NSSelectorFromString</code>, <code>NSStringFromSelector</code> 等函数。</p></blockquote></li></ul><p>实现原理：</p><p><code>Objective-C</code> 对象中都有一个 <code>isa</code> 指针指向对象所属的类，这个类中存储了类的方法列表、属性列表，并且如果类继承自其他类，那么这个类还会通过 <code>superclass</code> 指针找到其父类，这样层层递推，形成了一条类链，利用这条链条就可以检查和操作对象的行为和结构。</p><p>应用场景：</p><ul><li>数据和模型的自动映射。 <blockquote><p>例如 <code>JSON</code> 数据转 <code>Model</code> 对象，通常需要写大量的映射代码。利用内省我们可以在运行时获取对象的属性信息，从而实现自动映射。</p></blockquote></li><li>在运行时动态地添加类，方法，属性。 <blockquote><p>例如可以在运行时给一个类添加属性或方法，或者修改方法的实现等。</p></blockquote></li><li>在面向切面编程（<code>AOP</code>）中，经常需要运行时修改对象的行为。</li><li>在执行单元测试时，可以利用反射机制来动态地调用需要测试的方法。</li></ul></details><h4 id="❓有用过反射吗-什么情况下会想到用反射-有没有别的办法" tabindex="-1"><a class="header-anchor" href="#❓有用过反射吗-什么情况下会想到用反射-有没有别的办法" aria-hidden="true">#</a> ❓有用过反射吗，什么情况下会想到用反射，有没有别的办法</h4><details class="hint-container details"><summary>💡</summary><ul><li><p>反射：</p><ul><li><p>运行时（<code>Runtime</code>）：<code>Objective-C</code> 的运行时系统使得能够在运行时检查和修改类和对象的信息。因此，可以在运行时查询和修改类和实例变量，动态创建类，动态替换方法等等。</p></li><li><p><code>NSClassFromString</code> 和 <code>NSSelectorFromString</code> 这两个函数可以从字符串中得到类和方法。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">/** SEL 反射 */</span>
<span class="token comment">//  String -&gt; SEL</span>
SEL selector <span class="token operator">=</span> <span class="token function">NSSelectorFromString</span><span class="token punctuation">(</span><span class="token string">@&quot;setName:&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">[</span>stu performSelector<span class="token punctuation">:</span>selector withObject<span class="token punctuation">:</span><span class="token string">@&quot;Kobe&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token comment">//  SEL -&gt; String</span>
<span class="token function">NSStringFromSelector</span><span class="token punctuation">(</span><span class="token keyword">@selector</span><span class="token punctuation">(</span>setName<span class="token punctuation">:</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">/**  Class 反射 */</span>
<span class="token comment">//  String -&gt; Class</span>
Class class <span class="token operator">=</span> <span class="token function">NSClassFromString</span><span class="token punctuation">(</span><span class="token string">@&quot;Student&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
Student <span class="token operator">*</span>student <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span>class alloc<span class="token punctuation">]</span> init<span class="token punctuation">]</span><span class="token punctuation">;</span> 
<span class="token comment">//  Class -&gt; String</span>
Class class <span class="token operator">=</span> <span class="token punctuation">[</span>Student class<span class="token punctuation">]</span><span class="token punctuation">;</span>
NSString <span class="token operator">*</span>className <span class="token operator">=</span> <span class="token function">NSStringFromClass</span><span class="token punctuation">(</span>class<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">/** Protocol 反射 */</span>
<span class="token comment">//  String -&gt; Protocol</span>
Protocol protocol <span class="token operator">=</span> <span class="token function">NSProtocolFromString</span><span class="token punctuation">(</span><span class="token string">@&quot;People&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">//  Protocol -&gt; String</span>
NSString <span class="token operator">*</span>protocolName <span class="token operator">=</span> <span class="token function">NSStringFromProtocol</span><span class="token punctuation">(</span>protocol<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul></li><li><p>使用场景：</p><ul><li>解析复杂的 <code>JSON</code> 或 <code>XML</code> 结构的时候，比如想把 <code>JSON</code> 转换为特定的对象，而 <code>JSON</code> 的键值恰好对应了这个对象的属性，可以使用反射遍历对象的属性，并填充对应的值。</li><li>如果需要编写更通用性的代码，例如正在编写一个通用的数据源，它需要处理任何类型的数据，这种情况下需要使用反射。</li></ul></li><li><p>替代方案：如果明确知道要操作的类型，那么通常不需要使用反射。可以直接通过属性名称访问属性，或者直接调用方法。</p></li></ul><p>总结：虽然反射是一个强大的工具，能够提供极大的灵活性，但是它应该谨慎使用。不正确的使用反射可能会导致代码难以理解和维护，甚至产生错误。只有在必要的时候才应该使用反射。</p></details><hr><h2 id="属性" tabindex="-1"><a class="header-anchor" href="#属性" aria-hidden="true">#</a> 属性</h2><h3 id="❓-property-是什么" tabindex="-1"><a class="header-anchor" href="#❓-property-是什么" aria-hidden="true">#</a> ❓<code>@property</code> 是什么</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>@property</code> 是 <code>Objective-C</code> 中的一个关键字，用于声明一个属性。主要作用是为类的实例变量（属性）生成对应的访问器方法，即 <code>getter</code> 方法和 <code>setter</code> 方法。</p></blockquote><ul><li><p><code>@property</code> 修饰器</p><ul><li>原子性 : <code>atomic</code>、<code>nonatomic</code></li><li>读写性 : <code>readwrite</code>、<code>readonly</code></li><li>内存管理 : <code>assign</code>、<code>strong</code>、<code>weak</code>、<code>copy</code></li></ul></li></ul><ul><li>兼容性(<code>Swift</code>) : <code>nullable</code>、<code>nonnull</code>、<code>class</code></li></ul><ul><li><p><code>@property</code> 优点</p><ul><li>隔离性：可以封装实例变量的直接访问，将类的内部实现与外部接口隔离，提高数据的安全性。</li><li>可维护性：可以在 <code>getter</code> 和 <code>setter</code> 中添加特定的逻辑如设置新值时进行验证，获取值时返回一个计算后的值等，提高代码的可维护性。</li></ul></li></ul></details><h3 id="❓-property-的默认关键字" tabindex="-1"><a class="header-anchor" href="#❓-property-的默认关键字" aria-hidden="true">#</a> ❓<code>@property</code> 的默认关键字</h3><details class="hint-container details"><summary>💡</summary><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// 基本数据类型 =&gt; 默认是: atomic, readwrite, assign</span>
<span class="token keyword">@property</span> <span class="token keyword">int</span> num<span class="token punctuation">;</span>
<span class="token comment">// 对象数据类型 =&gt; 默认是: atomic, readwrite, strong, nullable</span>
<span class="token keyword">@property</span> NSObject <span class="token operator">*</span>obj<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓-property-的底层实现" tabindex="-1"><a class="header-anchor" href="#❓-property-的底层实现" aria-hidden="true">#</a> ❓<code>@property</code> 的底层实现</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>@property</code> 是一个关键字，用于声明类的属性。声明一个属性时，编译器会自动生成一些代码，这就是 <code>@property</code> 的底层实现。</p></blockquote><ul><li><p>实例变量：<code>@property</code> 在默认情况下会自动合成一个带下划线前缀的实例变量。</p><blockquote><p>例如声明了一个名为 <code>foo</code> 的属性，编译器会自动生成一个名为 <code>_foo</code> 的实例变量。</p></blockquote></li><li><p><code>getter</code> 和 <code>setter</code> 方法：会为属性自动生成 <code>getter</code> 和 <code>setter</code> 方法。</p></li><li><p>内存管理：指定的属性修饰符（<code>assign</code>、<code>strong</code>、<code>weak</code>、<code>copy</code> 等），编译器会在 <code>setter</code> 方法中生成相应的内存管理代码，比如引用计数的增加和减小，拷贝对象等。</p></li><li><p><code>KVO</code> 支持：<code>@property</code> 自动生成的 <code>getter</code> 和 <code>setter</code> 方法支持键值观察(<code>Key-Value Observing</code>)，允许其它对象观察和响应属性值的改变。</p></li></ul><blockquote><p><code>runtime</code> 层面</p></blockquote><p>声明 <code>@property</code> 后，编译器会根据属性的特性在 <code>class_rw_t</code> 结构中的 <code>methods</code> 和 <code>properties</code> 中生成 <code>getter</code> 和 <code>setter</code> 方法以及相应的属性。然后在运行时，这些 <code>getter</code> 和 <code>setter</code> 方法以及属性被引入到 <code>objc_class</code> 的结构中。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">struct</span> objc_class <span class="token punctuation">{</span>
    Class isa<span class="token punctuation">;</span>              <span class="token comment">// metaclass 的指针</span>
    Class super_class<span class="token punctuation">;</span>      <span class="token comment">// 父类</span>
    cache_t cache<span class="token punctuation">;</span>          <span class="token comment">// 缓存</span>
    class_data_bits_t bits<span class="token punctuation">;</span> <span class="token comment">// 类的相关信息，包含了一个指向 class_rw_t 数据结构的指针</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token comment">// 可读写结构，存储运行时添加的类信息</span>
<span class="token keyword">struct</span> class_rw_t <span class="token punctuation">{</span>
    uint32_t flags<span class="token punctuation">;</span>
    uint32_t version<span class="token punctuation">;</span>
    <span class="token keyword">const</span> class_ro_t <span class="token operator">*</span>ro<span class="token punctuation">;</span>           <span class="token comment">// 只读结构体，保存编译时确定的类信息</span>
    method_array_t methods<span class="token punctuation">;</span>         <span class="token comment">// 方法列表</span>
    property_array_t properties<span class="token punctuation">;</span>    <span class="token comment">// 属性列表</span>
    protocol_array_t protocols<span class="token punctuation">;</span>     <span class="token comment">// 协议列表</span>
    Class firstSubclass<span class="token punctuation">;</span>
    Class nextSiblingClass<span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="原子性" tabindex="-1"><a class="header-anchor" href="#原子性" aria-hidden="true">#</a> 原子性</h3><h4 id="❓atomic-vs-nonatomic" tabindex="-1"><a class="header-anchor" href="#❓atomic-vs-nonatomic" aria-hidden="true">#</a> ❓<code>atomic</code> vs <code>nonatomic</code></h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>atomic</code> 和 <code>nonatomic</code> 是 <code>Objective-C</code> 中声明属性时使用的两种属性原子性（<code>atomicity</code>）修饰符。</p></blockquote><ul><li><p><code>atomic</code>：代表属性是原子操作。这意味着对该属性的 <code>setter</code> 和 <code>getter</code> 方法在多线程环境下保证了线程安全，不会出现数据不一致的现象。但，这并不代表所有的操作都是线程安全的，比如对属性的读-写-写操作就不是线程安全的，同时atomic会带来一定的性能开销。如果没有指定atomic或nonatomic，则默认为atomic。</p></li><li><p><code>nonatomic</code>：代表属性非原子操作。在多线程环境下对该属性的存取操作可能会出现数据不一致情况，但在性能上有所优势，因为nonatomic避免了加锁开销。</p></li></ul><p>为了提高效率，在没有并发访问的情况下，我们通常使用<code>nonatomic</code>。对于有并发访问的属性，我们需要在getter和setter中实现适当的锁来管理对这些属性的访问，而不是仅仅依赖于<code>atomic</code>，因为<code>atomic</code>并不能完全解决并发的问题。</p></details><h4 id="❓atomic-修饰的属性是否可以被重载" tabindex="-1"><a class="header-anchor" href="#❓atomic-修饰的属性是否可以被重载" aria-hidden="true">#</a> ❓<code>atomic</code> 修饰的属性是否可以被重载</h4><details class="hint-container details"><summary>💡</summary><p>不论一个属性是通过<code>atomic</code>还是<code>nonatomic</code>修饰，它都可以被子类重载。重载一个属性主要就是提供一个新的 <code>setter</code> 或者 <code>getter</code>。在子类中重载一个 <code>atomic</code> 属性时，除非明确重载了这个属性的全部存取器（包括 <code>getter</code> 和 <code>setter</code>），否则它仍然是一个原子操作。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token keyword">@property</span> <span class="token punctuation">(</span>atomic<span class="token punctuation">,</span> strong<span class="token punctuation">)</span> NSString <span class="token operator">*</span>name<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> MyClass
<span class="token keyword">@synthesize</span> name <span class="token operator">=</span> _name<span class="token punctuation">;</span>

<span class="token comment">// 重载 getter 方法</span>
<span class="token operator">-</span> <span class="token punctuation">(</span>NSString <span class="token operator">*</span><span class="token punctuation">)</span>name <span class="token punctuation">{</span>
  <span class="token comment">// 使用 @synchronized 来确保原子操作</span>
  <span class="token operator">@</span><span class="token function">synchronized</span> <span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">return</span> _name<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
<span class="token comment">// 重载 setter 方法</span>
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>setName<span class="token punctuation">:</span><span class="token punctuation">(</span>NSString <span class="token operator">*</span><span class="token punctuation">)</span>name <span class="token punctuation">{</span>
  <span class="token operator">@</span><span class="token function">synchronized</span> <span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      _name <span class="token operator">=</span> name<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="内存管理" tabindex="-1"><a class="header-anchor" href="#内存管理" aria-hidden="true">#</a> 内存管理</h3><h4 id="❓assign、strong、weak、copy-区别" tabindex="-1"><a class="header-anchor" href="#❓assign、strong、weak、copy-区别" aria-hidden="true">#</a> ❓<code>assign</code>、<code>strong</code>、<code>weak</code>、<code>copy</code> 区别</h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>assign</code>：是赋值特性，简单设置变量，不更改引用计数。</p><blockquote><p>主要用在基本的数据类型（如 <code>int</code>、<code>float</code>）或 <code>C</code> 数据类型（如 结构体、数组）而不是对象。</p></blockquote></li><li><p><code>strong</code>（类似于：<code>retain</code>）：是所有权（owning）特性。</p><blockquote><p>采用 <code>ARC</code>（自动引用计数）环境下，编译器会自动在设置方法中插入 <code>retain</code> 调用，使对象的引用计数增加，从而表示当前对象拥有数据值。</p></blockquote></li><li><p><code>weak</code>：是非拥有关系（non-owning）特性，即弱引用。</p><blockquote><p>当用 <code>weak</code> 修饰对象时，被指向的对象引用计数不会增加，更重要的是，当对象被释放时，被 <code>weak</code> 修饰的变量会自动被置为 <code>nil</code>，防止野指针的出现。<code>weak</code> 修饰的属性适合在已经存在对对象的 <code>strong</code> 引用，或者对象之间存在相互循环引用的情况下使用。</p></blockquote></li><li><p><code>copy</code>：是拷贝特性。</p><blockquote><p>适合 <code>NSString</code>、<code>NSArray</code>、<code>NSDictionary</code> 等不可变对象。最重要的一点是：使用 <code>copy</code> 保持封装性，对象在内部可能需要一个数据的完整副本，并且把这个副本给外界使用，防止作为接口提供的对象的初始数据被改变。</p></blockquote></li></ul><p>选择哪种属性修饰符要视情况而定，如果数据是不可变的，那么选择 <code>assign</code> 或 <code>copy</code> 较好；如果数据是可变的，可以选择 <code>strong</code> 或 <code>weak</code>。</p></details><h4 id="❓assign-可以用于-oc-对象吗" tabindex="-1"><a class="header-anchor" href="#❓assign-可以用于-oc-对象吗" aria-hidden="true">#</a> ❓<code>assign</code> 可以用于 <code>OC</code> 对象吗</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>assign</code> 是一种属性修饰符，用于指定属性的赋值语义。<code>assign</code> 可以用于任何数据类型，包括 <code>Objective-C</code> 对象。但是，对于对象来说不推荐使用 <code>assign</code>。</p></blockquote><p>将 <code>assign</code> 用于 <code>Objective-C </code>对象时，其会生成一个设置器，其行为将传入的值赋值给实例变量，并不改变该对象的引用计数。如果该对象在其它地方被释放，那么这个引用就成了一个悬挂指针（或者说“野指针”），这将会引起程序在调用该对象时崩溃。</p><blockquote><p>在某些特定场景下，可能会选择使用 <code>assign</code>。例如，当需要创建一个 “弱引用并且不希望在对象被释放时自动设置为 <code>nil</code>” 时，<code>assign</code> 就会是个不错的选择，但是需要确保不会发生悬挂指针。</p></blockquote></details><h4 id="❓copy-一般会用来修饰-nsstring-可以使用-strong-吗" tabindex="-1"><a class="header-anchor" href="#❓copy-一般会用来修饰-nsstring-可以使用-strong-吗" aria-hidden="true">#</a> ❓<code>copy</code> 一般会用来修饰 <code>NSString</code>，可以使用 <code>strong</code> 吗</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>NSString</code> 使用 <code>copy</code> 是为了防止在赋值可变字符串时由于原字符串发生改变而引发非预期问题。</p></blockquote><ul><li><p><code>NSString</code> 的子类 <code>NSMutableString</code> 是可以变化的。如果使用 <code>strong</code> 并把一个 <code>NSMutableString</code> 实例赋值给 <code>NSString</code> 对象，然后改变该 <code>NSMutableString</code> 实例，那么 <code>NSString</code> 实例也会随之改变。</p></li><li><p>如果使用 <code>copy</code>，当一个 <code>NSMutableString</code> 实例赋值给 <code>NSString</code> 对象时，实际上会复制一个不可变的副本。这样即使原字符串改变 <code>NSString</code> 实例也不会受影响。</p></li></ul></details><h3 id="兼容性" tabindex="-1"><a class="header-anchor" href="#兼容性" aria-hidden="true">#</a> 兼容性</h3><h4 id="❓nonnull、nullable-用过吗" tabindex="-1"><a class="header-anchor" href="#❓nonnull、nullable-用过吗" aria-hidden="true">#</a> ❓<code>nonnull</code>、<code>nullable</code> 用过吗</h4><details class="hint-container details"><summary>💡</summary><p>这两个修饰符是在 <code>Objective-C</code> 中引入的一个新特性，它起源于 <code>Swift</code> 语言中的 <code>Optional</code> 类型。在 <code>Swift</code> 中，每个变量或者函数返回值都必须明确说明是否可以为空。为了在 <code>Objective-C</code> 和 <code>Swift</code> 的桥接中保持一致，引入了这两个修饰符来明确对象的 <code>nil</code> 状态。</p><ul><li><p><code>nonnull</code>：这个修饰符表示一个对象不应该为空，也就是说这个参数或者返回值绝对不能为 <code>nil</code>。如果试图将 <code>nil</code> 赋值给一个 <code>nonnull</code> 的变量，编译器会给出警告。</p></li><li><p><code>nullable</code>：这个修饰符和 <code>nonnull</code> 的功能恰恰相反，它表示一个对象可以为空，也就是这个参数或者返回值可以为 <code>nil</code>。一个没有被显式声明为 <code>nonnull</code> 或者 <code>nullable</code> 的对象默认为 <code>nullable</code>。</p></li></ul><p>注意：这两个修饰符只是用来帮助编译器检查代码的错误，并不能真正阻止程序员将 <code>nil</code> 赋值给 <code>nonnull</code> 的对象，这个判断还需要开发者在编码时自己注意。</p></details><h4 id="❓class-属性修饰器" tabindex="-1"><a class="header-anchor" href="#❓class-属性修饰器" aria-hidden="true">#</a> ❓<code>class</code> 属性修饰器</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>class</code> 属性修饰符主要运用于定义一个类类型的属性，它的用途主要是标明一个 <code>property</code> 的关联对象类型是一个类属性，而非一个实例属性。</p></blockquote><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@property</span> <span class="token punctuation">(</span>class<span class="token punctuation">,</span> nonatomic<span class="token punctuation">,</span> readonly<span class="token punctuation">)</span> NSString <span class="token operator">*</span>appName<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></details><h3 id="synthesize" tabindex="-1"><a class="header-anchor" href="#synthesize" aria-hidden="true">#</a> @synthesize</h3><h4 id="❓-synthesize-vs-dynamic" tabindex="-1"><a class="header-anchor" href="#❓-synthesize-vs-dynamic" aria-hidden="true">#</a> ❓<code>@synthesize</code> vs <code>@dynamic</code></h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>@synthesize</code>：告诉编译器编译时自动创建 <code>getter/setter</code> 方法，如果没有手动实现的话。</p></li><li><p><code>@dynamic</code>：这个关键字告诉编译器,属性的 <code>getter</code> 和 <code>setter</code> 方法由自己实现，不自动生成（当然对于<code>@dynamic var</code>，编译器还是会默认添加其成员变量<code>_var</code>），如果没有手动实现 <code>setter/getter</code>，那么程序运行时，编译器发现找不到 <code>setter/getter</code> 就会崩溃。好处是可以在运行时动态绑定对象。</p></li></ul><p>注：<code>@dynamic</code> 常常和运行时函数 <code>class_addMethod</code> 配合使用，可以动态添加方法。最常见的应用是在 <code>Core Data</code> 的 <code>NSManagedObject</code> 子类中，<code>Core Data</code> 的属性经常被设置为 <code>@dynamic</code>，<code>NSManagedObject</code> 利用 <code>Core Data</code> 的底层存储访问这些属性，以便将对象的实际数据保存在磁盘上，而不是内存中。</p></details><h4 id="❓-synthesize-合成实例变量的规则是什么" tabindex="-1"><a class="header-anchor" href="#❓-synthesize-合成实例变量的规则是什么" aria-hidden="true">#</a> ❓<code>@synthesize</code> 合成实例变量的规则是什么</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>@synthesize</code> 指令在实现(<code>.m</code>)文件中用于让编译器自动生成与属性相对应的实例变量，并且会为这个实例变量生成 <code>getter</code> 和 <code>setter</code> 方法。</p></blockquote><p><code>@synthesize</code> 基本规则：</p><ul><li><p>当在 <code>.h</code> 文件中用 <code>@property propertyName;</code> 声明一个属性时，编译器会自动生成一个名为 <code>_propertyName</code> 的实例变量，这个实例变量的名称是属性名前加一个下划线。如果无法访问这个实例变量，那么可能就需要用到 <code>@synthesize</code> 来让编译器生成这个实例变量。</p></li><li><p><code>@synthesize</code> 后面可以跟上一个或者两个变量名。如果跟上一个变量名如 <code>@synthesize propertyName;</code> 那么编译器会仅为 <code>propertyName</code> 这个属性生成一个名为 <code>propertyName</code>（没有下划线）的实例变量。如果跟上两个变量名，如 <code>@synthesize propertyName = _propertyName;</code> 编译器会为 <code>propertyName</code> 这个属性生成一个名为 <code>_propertyName</code> 的实例变量。</p></li><li><p>如果一个属性被 <code>@synthesize</code> 指定了一个实例变量，那么这个属性的 <code>getter</code> 和 <code>setter</code> 方法就会和这个实例变量绑定，即通过 <code>getter</code> 和 <code>setter</code> 方法操作的就是这个实例变量。</p></li></ul><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@implementation</span> MyClass
<span class="token keyword">@synthesize</span> foo <span class="token operator">=</span> myFoo<span class="token punctuation">;</span> <span class="token comment">// 创建一个名为 myFoo 的实例变量</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><blockquote><p>❓如果属性名为 <code>foo</code>，存在一个名为 <code>_foo</code> 的实例变量，还会自动合成新变量么</p></blockquote><details class="hint-container details"><summary>💡</summary><p>属性名为 <code>foo</code>，同时已经实现了一个名为 <code>_foo</code> 的实例变量，那么在默认情况下（也就是没有使用 <code>@synthesize</code> 关键字来手动指定成员变量名），编译器不会再合成新的实例变量。因为编译器默认的命名规则是在属性名前加 <code>_</code> 作为成员变量名，如果已经存在这样的成员变量，编译器就不会再创建新的变量。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> strong<span class="token punctuation">)</span> id foo<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> MyClass <span class="token punctuation">{</span>
    id _foo<span class="token punctuation">;</span> <span class="token comment">// 不会再合成新的实例变量</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓-synthesize-的使用场景" tabindex="-1"><a class="header-anchor" href="#❓-synthesize-的使用场景" aria-hidden="true">#</a> ❓<code>@synthesize</code> 的使用场景</h4><details class="hint-container details"><summary>💡</summary><ul><li><p>设置实例变量名。</p><blockquote><p><code>@synthesize</code> 关键字允许手动改变自动生成的实例变量的名称。</p></blockquote></li><li><p>协议属性自动合成。</p><blockquote><p>实现协议时，为了避免重复编写代码，可以使用 <code>@synthesize</code> 来自动合成协议中定义的属性。</p></blockquote></li><li><p>不生成实例变量。</p><blockquote><p>当在类扩展 (<code>@interface MyClass()</code>) 或者父类中已经声明了属性的实例变量 <code>_foo</code>，此时在子类中如果 <code>@synthesize foo;</code>，则不会生成 <code>foo</code> 对应的实例变量。</p></blockquote></li><li><p>生成实例变量。</p><blockquote><p>当在头文件或者类扩展（<code>@interface MyClass()</code>）中声明 <code>@property</code> 属性但没有 <code>@synthesize foo;</code>，则会自动生成 <code>_foo</code> 实例变量。如果在 <code>.m</code> 文件中声明 <code>@property</code> 属性但没有 <code>@synthesize foo;</code>，则不会自动合成属性的实例变量。</p></blockquote></li></ul><p>注：在 <code>Xcode 4.4</code> 及以上版本中，默认已经开启了 <code>@synthesize</code> 自动生成，已经不需要显式地使用 <code>@synthesize</code>，除非在某些特殊情况下需要手动指定实例变量的名字。</p></details><hr><h2 id="拷贝" tabindex="-1"><a class="header-anchor" href="#拷贝" aria-hidden="true">#</a> 拷贝</h2><h3 id="❓copy-vs-mutablecopy" tabindex="-1"><a class="header-anchor" href="#❓copy-vs-mutablecopy" aria-hidden="true">#</a> ❓<code>copy</code> vs <code>mutableCopy</code></h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>copy</code> 和 <code>mutableCopy</code> 是 <code>NSObject</code> 类中定义的方法，被用来创建对象的拷贝。</p></blockquote><ul><li><p><code>copy</code>：执行的是浅拷贝（也叫做指针拷贝），拷贝后的对象类型是不可变的。</p><blockquote><p><code>copy</code> 并没有开辟新的内存空间，而是拷贝对象持有的指针。因此原始对象和拷贝对象指向相同的内存地址。</p></blockquote></li><li><p><code>mutableCopy</code>：执行的是深拷贝（也叫做内容拷贝），拷贝后的对象类型是可变的。</p><blockquote><p><code>mutableCopy</code> 会开辟一块新的内存空间，拷贝对象的值到这块新的内存空间。</p></blockquote></li></ul><p>注：浅拷贝并不意味着拷贝出来的对象一定是不可变的，具体还需要看拷贝的源对象是什么类型。如果拷贝的源对象是不可变的，那么 <code>copy</code> 方法拷贝出来的对象也是不可变的；如果拷贝的源对象是可变的，那么 <code>copy</code> 方法拷贝出来的对象就是可变的。</p></details><blockquote><p>❓<code>copy</code> 修饰的属性，拷贝发生的时机</p></blockquote><details class="hint-container details"><summary>💡</summary><p><code>copy</code>关键字修饰的属性，拷贝动作发生在 <code>setter</code> 方法被调用时。具体发生在属性被赋值的过程之中，新的对象通过 <code>copy</code> 方法生成，然后被赋值给属性。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> copy<span class="token punctuation">)</span> NSString <span class="token operator">*</span>name<span class="token punctuation">;</span>

<span class="token comment">// 属性赋值</span>
person<span class="token punctuation">.</span>name <span class="token operator">=</span> <span class="token string">@&quot;John&quot;</span><span class="token punctuation">;</span>
<span class="token comment">// 等价于</span>
<span class="token punctuation">[</span>person setName<span class="token punctuation">:</span><span class="token punctuation">[</span><span class="token string">@&quot;John&quot;</span> copy<span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓copy-修饰的属性的-setter-方法如何重写" tabindex="-1"><a class="header-anchor" href="#❓copy-修饰的属性的-setter-方法如何重写" aria-hidden="true">#</a> ❓<code>copy</code> 修饰的属性的 <code>setter</code> 方法如何重写</h3><details class="hint-container details"><summary>💡</summary><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> copy<span class="token punctuation">)</span> NSString <span class="token operator">*</span>myString<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> MyClass
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>setMyString<span class="token punctuation">:</span><span class="token punctuation">(</span>NSString <span class="token operator">*</span><span class="token punctuation">)</span>newString <span class="token punctuation">{</span>
    <span class="token comment">// 先检查新值与旧值是否相等</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>_myString <span class="token operator">!=</span> newString<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        _myString <span class="token operator">=</span> <span class="token punctuation">[</span>newString copy<span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// 使用 copy 进行赋值</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓copy-一般用于不可变对象-为什么" tabindex="-1"><a class="header-anchor" href="#❓copy-一般用于不可变对象-为什么" aria-hidden="true">#</a> ❓<code>copy</code> 一般用于不可变对象，为什么</h3><details class="hint-container details"><summary>💡</summary><ul><li><p>使用 <code>copy</code> 可以防止不可变对象在外部被意外修改。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> Person <span class="token punctuation">:</span> NSObject
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> copy<span class="token punctuation">)</span> NSString <span class="token operator">*</span>name<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

NSMutableString <span class="token operator">*</span>mutableString <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableString stringWithString<span class="token punctuation">:</span><span class="token string">@&quot;Hello&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
Person <span class="token operator">*</span>person <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span>Person alloc<span class="token punctuation">]</span> init<span class="token punctuation">]</span><span class="token punctuation">;</span>
person<span class="token punctuation">.</span>name <span class="token operator">=</span> mutableString<span class="token punctuation">;</span>
<span class="token punctuation">[</span>mutableString appendString<span class="token punctuation">:</span><span class="token string">@&quot; World&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> person<span class="token punctuation">.</span>name<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;Hello&quot;，使用 copy，person.name 不会随着 mutableString 的改变而改变</span>
<span class="token comment">// 如果 name 使用 strong 修饰，则会会打印 &quot;Hello World&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p>某些具有父子关系的类，子类对象赋值给用 <code>copy</code> 修饰的父类变量后，父类变量会变为原来的父类特性(里氏替换)。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSMutableString <span class="token operator">*</span>mutableString <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableString stringWithString<span class="token punctuation">:</span><span class="token string">@&quot;Hello&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
NSString <span class="token operator">*</span>copyString <span class="token operator">=</span> <span class="token punctuation">[</span>mutableString copy<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">[</span>mutableString appendString<span class="token punctuation">:</span><span class="token string">@&quot; World&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> copyString<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// &quot;Hello&quot;，copy 后的 copyString 是不可变的，所以它的值不会改变</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul></details><h3 id="❓-property-copy-nsmutablearray-array-这个写法正确吗" tabindex="-1"><a class="header-anchor" href="#❓-property-copy-nsmutablearray-array-这个写法正确吗" aria-hidden="true">#</a> ❓<code>@property (copy) NSMutableArray *array;</code> 这个写法正确吗</h3><details class="hint-container details"><summary>💡</summary><blockquote><p>这个写法可以运行通过，但从代码规范和最佳实践来说，这并不是一个好的写法。</p></blockquote><p>给一个属性添加 <code>copy</code> 标识时，这个属性的 <code>setter</code> 将会使用传入对象的 <code>copy</code> 方法来创建一个新的对象。然而，在 <code>Objective-C</code> 中，<code>copy</code> 方法对 <code>NSMutableArray</code> 实例调用将返回一个 <code>NSArray</code> 实例。这是由于 <code>copy</code> 策略总是返回一个不可变的副本。</p><p>所以，如果用 <code>@property (copy) NSMutableArray *array;</code> 这个写法，实际上得到的 <code>array</code> 是一个不可变数组 <code>NSArray</code>。在后续代码中尝试向这个 “可变数组” (<code>NSMutableArray</code>) 中添加或删除元素时，将会导致运行时错误。</p></details><blockquote><p>❓<code>NSMutableArray</code> 属性如何支持 <code>copy</code></p></blockquote><details class="hint-container details"><summary>💡</summary><p>需要手动实现属性的 <code>getter</code> 和 <code>setter</code> 方法，并在 <code>setter</code> 方法中使用 <code>mutableCopy</code> 进行拷贝。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> strong<span class="token punctuation">)</span> NSMutableArray <span class="token operator">*</span>array<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> MyClass

<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>setArray<span class="token punctuation">:</span><span class="token punctuation">(</span>NSMutableArray <span class="token operator">*</span><span class="token punctuation">)</span>newArray <span class="token punctuation">{</span>
    <span class="token comment">// 这样可以保证每次设置新数组时都创建一个可变副本，而不是指向原始数组。</span>
    _array <span class="token operator">=</span> <span class="token punctuation">[</span>newArray mutableCopy<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓自定义类如何实现-copy-功能" tabindex="-1"><a class="header-anchor" href="#❓自定义类如何实现-copy-功能" aria-hidden="true">#</a> ❓自定义类如何实现 <code>copy</code> 功能</h3><details class="hint-container details"><summary>💡</summary><p>自定义类需要支持拷贝功能，需要遵循并实现 <code>NSCopying</code> 协议，并重写 <code>copyWithZone:</code> 方法。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// 自定义类 Person 实现 copy 功能</span>
<span class="token comment">//  - 1&gt; 遵循 NSCopying 协议</span>
<span class="token keyword">@interface</span> Person <span class="token punctuation">:</span> NSObject <span class="token operator">&lt;</span>NSCopying<span class="token operator">&gt;</span>
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> copy<span class="token punctuation">)</span> NSString <span class="token operator">*</span>name<span class="token punctuation">;</span>
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> assign<span class="token punctuation">)</span> NSUInteger age<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> Person
<span class="token comment">//  - 2&gt; 实现 copyWithZone: 方法</span>
<span class="token operator">-</span> <span class="token punctuation">(</span>id<span class="token punctuation">)</span>copyWithZone<span class="token punctuation">:</span><span class="token punctuation">(</span>NSZone <span class="token operator">*</span><span class="token punctuation">)</span>zone <span class="token punctuation">{</span>
    Person <span class="token operator">*</span>copyPerson <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token keyword">self</span> class<span class="token punctuation">]</span> allocWithZone<span class="token punctuation">:</span>zone<span class="token punctuation">]</span>init<span class="token punctuation">]</span><span class="token punctuation">;</span>
    copyPerson<span class="token punctuation">.</span>name <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token keyword">self</span><span class="token punctuation">.</span>name copyWithZone<span class="token punctuation">:</span>zone<span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// 对象类型属性需要调用 copyWithZone 完成拷贝</span>
    copyPerson<span class="token punctuation">.</span>age <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">.</span>age<span class="token punctuation">;</span>
    <span class="token keyword">return</span> copyPerson<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>

<span class="token comment">// 使用</span>
Person <span class="token operator">*</span>person <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span>Person alloc<span class="token punctuation">]</span> init<span class="token punctuation">]</span><span class="token punctuation">;</span>
Person <span class="token operator">*</span>newPerson <span class="token operator">=</span> <span class="token punctuation">[</span>person copy<span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// 完成拷贝</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓自定义类如何实现-mutablecopy-功能" tabindex="-1"><a class="header-anchor" href="#❓自定义类如何实现-mutablecopy-功能" aria-hidden="true">#</a> ❓自定义类如何实现 <code>mutableCopy</code> 功能</h3><details class="hint-container details"><summary>💡</summary><p>自定义类型实现 <code>mutableCopy</code>，需要遵循 <code>NSMutableCopying</code> 协议，并实现 <code>mutableCopyWithZone:</code> 方法。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> Person <span class="token punctuation">:</span> NSObject <span class="token operator">&lt;</span>NSMutableCopying<span class="token operator">&gt;</span>
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> copy<span class="token punctuation">)</span> NSString <span class="token operator">*</span>name<span class="token punctuation">;</span>
<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> assign<span class="token punctuation">)</span> NSUInteger age<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> Person
<span class="token operator">-</span> <span class="token punctuation">(</span>id<span class="token punctuation">)</span>mutableCopyWithZone<span class="token punctuation">:</span><span class="token punctuation">(</span>NSZone <span class="token operator">*</span><span class="token punctuation">)</span>zone <span class="token punctuation">{</span>
    Person <span class="token operator">*</span>copyPerson <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token punctuation">[</span><span class="token keyword">self</span> class<span class="token punctuation">]</span> allocWithZone<span class="token punctuation">:</span>zone<span class="token punctuation">]</span>init<span class="token punctuation">]</span><span class="token punctuation">;</span>
    copyPerson<span class="token punctuation">.</span>name <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token keyword">self</span><span class="token punctuation">.</span>name mutableCopyWithZone<span class="token punctuation">:</span>zone<span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// 对象类型，使用 mutableCopyWithZone 获取可变副本</span>
    copyPerson<span class="token punctuation">.</span>age <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">.</span>age<span class="token punctuation">;</span> <span class="token comment">// 基本数据类型，直接赋值 </span>
    <span class="token keyword">return</span> copyPerson<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="❓-object-copy-是浅拷贝还是深拷贝" tabindex="-1"><a class="header-anchor" href="#❓-object-copy-是浅拷贝还是深拷贝" aria-hidden="true">#</a> ❓<code>[object copy]</code> 是浅拷贝还是深拷贝</h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>[object copy]</code>方法的行为取决于对象支持哪种拷贝。</p></blockquote><ul><li><p>默认情况下，<code>[object copy]</code>执行的是浅拷贝，也叫做指针拷贝，即创建一个新对象，新对象的内容指向原对象内存。这种情况下，修改新复制出来的对象会影响到原对象。</p></li><li><p>如果要实现深拷贝，需要对象遵守 <code>NSCopying</code> 或 <code>NSMutableCopying</code> 协议，并实现 <code>copyWithZone:</code> 或 <code>mutableCopyWithZone:</code> 方法，在这两个方法中执行实际的复制操作。在这种情况下，<code>[object copy]</code> 执行的就是深拷贝，即完全复制出一个新对象，新对象的内容和原对象是独立的。修改新复制出来的对象不会影响到原对象。</p></li></ul><p>🌰 <code>NSArray</code> 和 <code>NSString</code> 等不可变对象的 <code>copy</code> 方法执行的是浅拷贝，而 <code>mutableCopy</code> 方法执行的是深拷贝。而对于它们各自的可变版本，即 <code>NSMutableArray</code> 和 <code>NSMutableString</code> 等， <code>copy</code> 和 <code>mutableCopy</code> 方法都执行的是深拷贝。</p><p>总结：<code>[object copy]</code> 是浅拷贝还是深拷贝，需要根据该对象的具体实现来判断。</p></details><hr><h2 id="block" tabindex="-1"><a class="header-anchor" href="#block" aria-hidden="true">#</a> Block</h2><h3 id="❓block-vs-delegate" tabindex="-1"><a class="header-anchor" href="#❓block-vs-delegate" aria-hidden="true">#</a> ❓<code>block</code> vs <code>delegate</code></h3><details class="hint-container details"><summary>💡</summary><ul><li><p>定义方式：</p><ul><li><code>block</code> 是一种特殊的数据类型，可以将一段代码封装到一个变量中，然后在需要的时候调用这个变量。</li><li><code>delegate</code> 代理是一种设计模式，是通过 <code>protocol</code> 定义了一系列的方法，然后由其他类去实现这些方法。</li></ul></li><li><p>使用场景：</p><ul><li><code>block</code> 适合一些简单的逻辑处理，如动画回调，数组遍历等。</li><li><code>delegate</code> 代理通常用于处理复杂的交互，如 <code>TableView</code> 的数据源及代理方法，网络请求的回调等。</li></ul></li><li><p>执行流程：</p><ul><li><code>block</code> 在调用时是同一个函数或方法内部的，逻辑关系相对清晰，容易理解。</li><li><code>delegate</code> 代理方法可能分散在代码的不同地方，使得调试和阅读代码稍微困难一些。</li></ul></li><li><p>生命周期：</p><ul><li><code>block</code> 有可能导致循环引用的问题。</li><li><code>delegate</code> 由于是 <code>weak</code> 引用，不会出现循环引用的问题。</li></ul></li><li><p>排序：</p><ul><li><code>block</code> 可以有多个且同时存在，顺序可以自由改变。</li><li><code>delegate</code> 通常只有一个，如果有多个，通常需要其他方式进行排序。</li></ul></li></ul></details><h3 id="❓block-vs-函数指针" tabindex="-1"><a class="header-anchor" href="#❓block-vs-函数指针" aria-hidden="true">#</a> ❓<code>block</code> vs 函数指针</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><code>block</code>：一种特殊的数据类型，可以存储一段代码，并且这段代码可以在任何时候被执行。可以访问在声明他的那个作用域中定义的变量，这被称为“变量捕获”。</p></li><li><p>函数指针：函数指针是存储着函数地址的指针变量，本质上是一个地址值，也就是函数在内存中的起始地址。函数指针不能像 <code>block</code> 那样访问变量，只能通过传参的方式去使用外部的数据。</p></li></ul></details><h3 id="❓block-底层实现" tabindex="-1"><a class="header-anchor" href="#❓block-底层实现" aria-hidden="true">#</a> ❓<code>block</code> 底层实现</h3>`,141),y={class:"hint-container details"},g=c(`<summary>💡</summary><p><code>block</code> 是一个 <code>Objective-C</code> 对象，它的定义中包含了一个结构体和函数指针。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// 简化版 block 结构体</span>
<span class="token keyword">struct</span> Block_literal <span class="token punctuation">{</span>
    <span class="token keyword">void</span> <span class="token operator">*</span>isa<span class="token punctuation">;</span>
    <span class="token keyword">int</span> flags<span class="token punctuation">;</span>
    <span class="token keyword">int</span> reserved<span class="token punctuation">;</span>
    <span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">*</span>invoke<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span> <span class="token operator">*</span><span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">struct</span> block_descriptor <span class="token operator">*</span>descriptor<span class="token punctuation">;</span>
    <span class="token comment">/* captured variables... */</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>isa</code>：指向对象的类型，所有 <code>Objective-C</code> 对象都有该变量。</li><li><code>flags</code> 和 <code>reserved</code>：提供关于 <code>block</code> 的一些信息，如 <code>block</code> 类型等。</li><li><code>invoke</code>：一个函数指针，指向进行调用的函数。</li><li><code>descriptor</code>：一个指向 <code>Block_descriptor</code> 结构的指针，其中包含相关信息。</li></ul><p><code>block</code> 的内存管理：</p><ul><li><p>当 <code>block</code> 没有捕获任何外部变量，或者仅使用了全局变量和静态变量时，它是一个 <code>global block</code>，存储在程序的全局数据区，其生命周期从创建到程序结束。</p></li><li><p>当 <code>block</code> 使用了外部的自动变量（包括 <code>ObjC</code> 对象和基本数据类型），在未进行 <code>copy</code> 操作的情况下，它是一个 <code>stack block</code>，存储在栈内存上，超出作用域即被销毁。</p></li><li><p>如果对 <code>block</code> 进行了 <code>copy</code> 操作，或者是用 <code>__block</code> 修饰的自动变量，那么这个 <code>block</code> 是一个 <code>malloc block</code>，存储在堆内存上，需要手动进行内存管理。</p></li></ul>`,6),S={href:"https://www.mikeash.com/pyblog/friday-qa-2009-08-14-practical-blocks.html",target:"_blank",rel:"noopener noreferrer"},f=c(`<h3 id="block-1" tabindex="-1"><a class="header-anchor" href="#block-1" aria-hidden="true">#</a> __block</h3><h4 id="❓block-内如何修改外部变量" tabindex="-1"><a class="header-anchor" href="#❓block-内如何修改外部变量" aria-hidden="true">#</a> ❓<code>block</code> 内如何修改外部变量</h4><details class="hint-container details"><summary>💡</summary><p>使用 <code>__block</code> 修饰符</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__block <span class="token keyword">int</span> num <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    num <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>  
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;Num: %d&quot;</span><span class="token punctuation">,</span> num<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 这里输出 &quot;Num: 1&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓block-在捕获自动变量-局部静态变量-全局变量-全局静态变量有什么区别" tabindex="-1"><a class="header-anchor" href="#❓block-在捕获自动变量-局部静态变量-全局变量-全局静态变量有什么区别" aria-hidden="true">#</a> ❓<code>block</code> 在捕获自动变量，局部静态变量，全局变量，全局静态变量有什么区别</h4><details class="hint-container details"><summary>💡</summary><ul><li><p>自动变量（<code>auto variables</code>）：这是在方法或函数中常见的局部变量，如 <code>int num = 10</code>。这种变量在 <code>block</code> 中默认以值传递形式捕获，即 <code>block</code> 捕获的是值，而不是变量本身。如果要在 <code>block</code> 中修改局部变量的值，必须在定义变量时加上 <code>__block</code> 修饰符。</p></li><li><p>局部静态变量、全局变量、全局静态变量：这些变量在 <code>block</code> 中以指针形式捕获，即 <code>block</code> 捕获的是对这些变量的指针。这意味着，可以在 <code>block</code> 中直接修改这些变量的值，不需要加 <code>__block</code> 修饰符。</p></li></ul><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">int</span> globalVar <span class="token operator">=</span> <span class="token number">4</span><span class="token punctuation">;</span>
<span class="token keyword">static</span> <span class="token keyword">int</span> staticGlobalVar <span class="token operator">=</span> <span class="token number">5</span><span class="token punctuation">;</span>

<span class="token keyword">void</span> <span class="token function">Test</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">int</span> localVar <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
    <span class="token keyword">static</span> <span class="token keyword">int</span> staticVar <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>
    __block <span class="token keyword">int</span> blockVar <span class="token operator">=</span> <span class="token number">3</span><span class="token punctuation">;</span>

    <span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;localVar: %d&quot;</span><span class="token punctuation">,</span> localVar<span class="token punctuation">)</span><span class="token punctuation">;</span>          <span class="token comment">// 1</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;staticVar: %d&quot;</span><span class="token punctuation">,</span> staticVar<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 2</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;blockVar: %d&quot;</span><span class="token punctuation">,</span> blockVar<span class="token punctuation">)</span><span class="token punctuation">;</span>          <span class="token comment">// 3</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;globalVar: %d&quot;</span><span class="token punctuation">,</span> globalVar<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 4</span>
        <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;staticGlobalVar: %d&quot;</span><span class="token punctuation">,</span> staticGlobalVar<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 5</span>

        <span class="token comment">// localVar++;              // Error! &quot;Variable is not assignable (missing __block type specifier)&quot;</span>
        staticVar<span class="token operator">++</span><span class="token punctuation">;</span>
        blockVar<span class="token operator">++</span><span class="token punctuation">;</span>
        globalVar<span class="token operator">++</span><span class="token punctuation">;</span>
        staticGlobalVar<span class="token operator">++</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span><span class="token punctuation">;</span>

    <span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;staticVar after block: %d&quot;</span><span class="token punctuation">,</span> staticVar<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 3</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;blockVar after block: %d&quot;</span><span class="token punctuation">,</span> blockVar<span class="token punctuation">)</span><span class="token punctuation">;</span>          <span class="token comment">// 4</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;globalVar after block: %d&quot;</span><span class="token punctuation">,</span> globalVar<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 5</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;staticGlobalVar after block: %d&quot;</span><span class="token punctuation">,</span> staticGlobalVar<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 6</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓block-捕获外部局部变量实际上发生了什么" tabindex="-1"><a class="header-anchor" href="#❓block-捕获外部局部变量实际上发生了什么" aria-hidden="true">#</a> ❓<code>block</code> 捕获外部局部变量实际上发生了什么</h4><details class="hint-container details"><summary>💡</summary><p><code>block</code> 可以捕获其外部的局部变量，使得这些外部变量可以在 <code>block</code> 的执行体内部被访问到。捕获的过程其实是在 <code>block</code> 被创建时发生的。<code>block</code> 会创建对应的变量的 &quot;拷贝&quot;。</p><ul><li><p>对于基本数据类型变量，比如 <code>int、float，block</code> 会复制其值，也就是在 <code>heap</code>(堆) 应用一段内存，将外部的变量值复制一份到这个内存上。</p></li><li><p>对于对象类型变量，<code>block</code> 会保留该对象，并复制对象指针。这个过程也被称为引用捕获。这样 <code>block</code> 对象可以在稍后的时间里随时引用到这个变量。</p></li></ul><p><code>__block</code> 关键字可以用于解决 <code>block</code> 内部无法修改外部变量的问题。当捕获一个 <code>__block</code> 变量时，<code>block</code> 内部会保留这个 <code>__block</code> 变量。在堆上创建的 <code>block</code> 会在 <code>heap</code> 中维护一个 <code>__block</code> 变量的结构体，这个结构体中包含了 <code>__block</code> 变量的地址和其值，这样 <code>block</code> 就可以通过操作这个结构体来修改 <code>__block</code> 变量的值。</p></details><blockquote><p>❓如果 使用 <code>__block</code> 捕获了局部变量，这个 <code>block</code> 作为返回值 <code>return</code> 了，这个局部变量释放了，怎么办</p></blockquote><details class="hint-container details"><summary>💡</summary><p>在使用 <code>__block</code> 捕获局部变量的同时，如果这个 <code>block</code> 作为返回值返回，那么这个局部变量就会被 <code>block</code> 捕获到，并且它的生命周期就延长到了 <code>block</code> 生命周期结束为止。也就是说，这个局部变量不会在离开定义它的作用域时释放，而是将在 <code>block</code> 被释放时才释放。</p><p>正常情况下 <code>block</code> 是分配在栈上的，栈上的 <code>block</code> 在离开作用域后就会被销毁。但是如果这个 <code>block</code> 被赋值给了一个强引用的变量，或者被返回出去的话，就需要将栈上的 <code>block</code> 复制到堆上，这样 <code>block</code> 的生命周期就会得到延长，直到没有强引用指向它为止。</p><p>对于使用 <code>__block</code> 修饰的局部变量，这个变量的存储地址会从栈复制到堆上。所以，返回的 <code>block</code> 中捕获的是移动到堆上的 <code>__block</code> 变量的地址，这也确保了返回后，能够正确地访问和修改这个 <code>__block</code> 变量。</p></details><h4 id="❓block-修改捕获变量除了用-block-还可以怎么做" tabindex="-1"><a class="header-anchor" href="#❓block-修改捕获变量除了用-block-还可以怎么做" aria-hidden="true">#</a> ❓<code>block</code> 修改捕获变量除了用 <code>__block</code> 还可以怎么做</h4><details class="hint-container details"><summary>💡</summary><p>可以使用 <code>NS(Mutable)Object/Dictionary/Array</code> 等，这些类型的实例在被捕获后仍然可以修改内容。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSMutableArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    <span class="token punctuation">[</span>array addObject<span class="token punctuation">:</span><span class="token string">@&quot;Hello&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> array<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出: [&quot;Hello&quot;]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓block-中修改-nsmutablearray-需不需要添加-block" tabindex="-1"><a class="header-anchor" href="#❓block-中修改-nsmutablearray-需不需要添加-block" aria-hidden="true">#</a> ❓<code>block</code> 中修改 <code>NSMutableArray</code> 需不需要添加 <code>__block</code></h4><details class="hint-container details"><summary>💡</summary><blockquote><p>不需要。在 <code>Objective-C</code> 中，<code>NSMutableArray</code> 是一个引用类型的对象，即使不使用 <code>__block</code>，也可以在 <code>block</code> 内部添加、删除或修改 <code>NSMutableArray</code> 的元素。因为修改的是 <code>NSMutableArray</code> 对象的内容，而不是 <code>NSMutableArray</code> 变量本身。</p></blockquote><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>NSMutableArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    <span class="token comment">// 向 array 添加了一个字符串</span>
    <span class="token punctuation">[</span>array addObject<span class="token punctuation">:</span><span class="token string">@&quot;Hello, World&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> array<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 输出：[&quot;Hello, World&quot;]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>⚠️ 如果希望在 <code>block</code> 中修改 <code>array</code> 这个变量（比如使其指向一个全新的 <code>NSMutableArray</code> 对象），那就需要使用 <code>__block</code> 了。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__block NSMutableArray <span class="token operator">*</span>array <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token keyword">void</span> <span class="token punctuation">(</span><span class="token operator">^</span>myBlock<span class="token punctuation">)</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    array <span class="token operator">=</span> <span class="token punctuation">[</span>NSMutableArray array<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token punctuation">[</span>array addObject<span class="token punctuation">:</span><span class="token string">@&quot;Hello, World&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token function">myBlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> array<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 输出：[&quot;Hello, World&quot;]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h3 id="内存管理-1" tabindex="-1"><a class="header-anchor" href="#内存管理-1" aria-hidden="true">#</a> 内存管理</h3><h4 id="❓block-如何进行内存管理的" tabindex="-1"><a class="header-anchor" href="#❓block-如何进行内存管理的" aria-hidden="true">#</a> ❓<code>block</code> 如何进行内存管理的</h4><details class="hint-container details"><summary>💡</summary><p><code>block</code> 的内存管理默认分为三个级别：<code>_NSConcreteGlobalBlock</code>、<code>_NSConcreteStackBlock</code>、<code>_NSConcreteMallocBlock</code>。</p><ul><li><p><code>__NSConcreteGlobalBlock__</code>：属于全局区，类似于全局变量，生命周期从应用开始直到应用结束。</p></li><li><p><code>__NSConcreteStackBlock__</code>：存放在栈内存中，当超出其作用域后就会被自动销毁。</p></li><li><p><code>__NSConcreteMallocBlock__</code>：存放在堆内存中，需要手动管理其内存，可以通过 <code>retain</code> 来增加其引用计数，通过 <code>release</code> 来减少其引用计数，当引用计数为 0 时，<code>block</code> 会被销毁。</p></li></ul><p>在使用 <code>ARC</code> (Automatic Reference Counting) 时，对 <code>block</code> 的内存管理会简化许多。当 <code>block</code> 中引用到了外部变量时，<code>block</code> 将会自动从栈复制到堆中（有复制行为的 <code>block</code> 都会进入堆区），并且在引用计数为 0 后会自动销毁，不需要手动管理。</p><p>注意：对于 <code>block</code> 内部应用到的对象，如果不加特殊处理，是会形成强引用的，因此可能会导致引用循环并引发内存泄漏。为了防止这种情况，经常使用 <code>__weak</code> 来修饰引用的对象，避免产生循环引用。</p></details><h4 id="❓-block-vs-weak" tabindex="-1"><a class="header-anchor" href="#❓-block-vs-weak" aria-hidden="true">#</a> ❓<code>__block</code> vs <code>__weak</code></h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>__block</code>：它用于允许在 <code>block</code> 中改变变量的值。</p><blockquote><p><code>__block</code> 是存储类型说明符，用于修饰一个变量，让该变量能在 <code>block</code> 中被读取和写入。如果没有 <code>__block</code>，那么 <code>block</code> 就不能修改该变量的值。</p></blockquote></li><li><p><code>__weak</code>：它用于创建一个弱引用。</p><blockquote><p>弱引用是一种特殊类型的引用，它不会增加被引用对象的引用计数。这样可以解决对象互相引用，形成引用循环的问题。在处理 <code>block</code> 对象引用 <code>self</code> 时，通常会先创建一个弱引用的 <code>self</code>，然后在 <code>block</code> 中使用这个弱引用，从而避免引用循环。</p></blockquote></li></ul></details><h4 id="❓-block-在-arc-和非-arc-有区别吗" tabindex="-1"><a class="header-anchor" href="#❓-block-在-arc-和非-arc-有区别吗" aria-hidden="true">#</a> ❓<code>__block</code> 在 <code>ARC</code> 和非 <code>ARC</code> 有区别吗</h4><details class="hint-container details"><summary>💡</summary><p>有区别</p><ul><li><p>在非 <code>ARC</code> 环境下，<code>__block</code> 不会改变对象的所有权。也就是说，如果有一个 <code>__block</code> 修饰的对象，<code>block</code> 不会为这个对象增加引用计数。因此，如果在 <code>block</code> 执行后，原来的对象被释放了，那么 <code>block</code> 内的这个对象就变成了悬浮指针。</p></li><li><p>在 <code>ARC</code> 环境下，<code>__block</code> 有另外一种行为。它会保留 <code>block</code> 中使用到的对象。也就是说，如果有一个 <code>__block</code> 修饰的对象，当 <code>block</code> 执行时，就会增加这个对象的引用计数。因此，在 <code>ARC</code> 下，可以安全地在 <code>block</code> 中引用 <code>__block</code> 修饰的对象，而不必担心它会被提前释放。</p></li></ul><p>一般会选择 <code>__weak</code> 代替 <code>__block</code> 来避免循环引用，特别是在引用 <code>self</code> 或者其成员变量的时候。</p></details><h4 id="❓block-什么情况会发生循环引用" tabindex="-1"><a class="header-anchor" href="#❓block-什么情况会发生循环引用" aria-hidden="true">#</a> ❓<code>block</code> 什么情况会发生循环引用</h4><details class="hint-container details"><summary>💡</summary><p>当在一个 <code>Objective-C</code> 对象的方法（或者属性）中引用了一个 <code>block</code>，而这个 <code>block</code> 中又引用了该对象（不论是直接引用还是间接引用）时，就会形成一个循环引用。这是由于 <code>block</code> 在捕获对象时默认为强引用，而对象本身又强引用了 <code>block</code>，形成了循环。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// 循环引用</span>
__strong <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> strongSelf <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token keyword">self</span><span class="token punctuation">.</span>myBlock <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    <span class="token punctuation">[</span>strongSelf doSomething<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token comment">// 打破循环引用</span>
__weak <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> weakSelf <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token keyword">self</span><span class="token punctuation">.</span>myBlock <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>   
    <span class="token comment">// 临时创建了一个对 weakSelf 的强引用，确保在 block 执行过程中不会出现 weakSelf 被意外释放的情况。</span>
    __strong <span class="token keyword">typeof</span><span class="token punctuation">(</span>weakSelf<span class="token punctuation">)</span> strongSelf <span class="token operator">=</span> weakSelf<span class="token punctuation">;</span>
    <span class="token punctuation">[</span>strongSelf doSomething<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓uiview-动画函数-block-版本是否需要处理循环引用" tabindex="-1"><a class="header-anchor" href="#❓uiview-动画函数-block-版本是否需要处理循环引用" aria-hidden="true">#</a> ❓<code>UIView</code> 动画函数 <code>block</code> 版本是否需要处理循环引用</h4><details class="hint-container details"><summary>💡</summary><p>在 <code>UIView</code> 的动画函数 <code>block</code> 版本中使用 <code>self</code>，实际上不会造成引用循环。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// self 不持有 动画 block</span>
<span class="token punctuation">[</span>UIView animateWithDuration<span class="token punctuation">:</span><span class="token number">1.0</span> animations<span class="token punctuation">:</span><span class="token operator">^</span><span class="token punctuation">{</span>
    <span class="token keyword">self</span><span class="token punctuation">.</span>myView<span class="token punctuation">.</span>alpha <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓block-一般使用-copy-修饰-可以使用-strong-吗" tabindex="-1"><a class="header-anchor" href="#❓block-一般使用-copy-修饰-可以使用-strong-吗" aria-hidden="true">#</a> ❓<code>block</code> 一般使用 <code>copy</code> 修饰，可以使用 <code>strong</code> 吗</h4><details class="hint-container details"><summary>💡</summary><p><code>block</code> 使用 <code>copy</code> 修饰是为了保持 <code>block</code> 的引用计数的正确。<code>block</code> 本身是在堆栈上分配的，<code>copy</code> 操作将其复制到堆上，可以保证 <code>block</code> 在方法返回后不被销毁，保证其正确执行。</p><p>虽然也可以使用 <code>strong</code> 来修饰 <code>block</code>，但是这样做并不会将 <code>block</code> 从栈复制到堆。因此，如果 <code>block</code> 捕获了方法内部的变量，当方法返回后，这些变量都已经被释放，可能会导致程序崩溃。</p></details><h4 id="❓为什么在-block-外部使用-weak-修饰的同时需要在内部使用-strong-修饰" tabindex="-1"><a class="header-anchor" href="#❓为什么在-block-外部使用-weak-修饰的同时需要在内部使用-strong-修饰" aria-hidden="true">#</a> ❓为什么在 <code>block</code> 外部使用 <code>__weak</code> 修饰的同时需要在内部使用 <code>__strong</code> 修饰</h4><details class="hint-container details"><summary>💡</summary><p>使用 <code>__weak</code> 修饰符是为了防止循环引用。由于 <code>block</code> 会捕获其内部使用的变量，如果 <code>block</code> 内部访问了 <code>self</code> 或其成员变量，那么会形成循环引用，导致内存泄漏。而使用 <code>__weak</code> 修饰符可以将 <code>self</code> 或成员变量变成弱引用，从而避免循环引用。</p><p><code>__weak</code> 的不足之处：在 <code>block</code> 执行期间，若弱引用的对象被释放了，那么 <code>block</code> 内部对该对象的访问就会失效。为了避免这种情况，就需要在 <code>block</code> 内部使用 <code>__strong</code> 修饰符将弱引用变量提升为强引用。</p><p>这样做可以保证在 <code>block</code> 执行期间，这个被提升的强引用变量所指向的对象不会被释放，保证了 <code>block</code> 执行过程中对象的访问安全。而在 <code>block</code> 执行完毕后，强引用也会随之释放，和在 <code>block</code> 外部保持指向对象的弱引用并不存在循环引用问题。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__weak <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> weakSelf <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token punctuation">[</span><span class="token keyword">self</span> doSomethingWithBlock<span class="token punctuation">:</span><span class="token operator">^</span><span class="token punctuation">{</span>
    __strong <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> strongSelf <span class="token operator">=</span> weakSelf<span class="token punctuation">;</span>
    <span class="token punctuation">[</span>strongSelf doSomethingElse<span class="token punctuation">]</span><span class="token punctuation">;</span>  
    <span class="token comment">// 在此处，若 self 在 block 执行期间释放，由于存在 strongSelf 的强引用，self 所指向的对象不会被释放。</span>
<span class="token punctuation">}</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓block-访问对象类型的-auto-变量时-在-arc-和-mrc-下有什么区别" tabindex="-1"><a class="header-anchor" href="#❓block-访问对象类型的-auto-变量时-在-arc-和-mrc-下有什么区别" aria-hidden="true">#</a> ❓<code>block</code> 访问对象类型的 <code>auto</code> 变量时，在 <code>ARC</code> 和 <code>MRC</code> 下有什么区别</h4><details class="hint-container details"><summary>💡</summary><ul><li><p>在 <code>MRC</code> 下，<code>block</code> 只是简单地进行指针拷贝，不会对对象进行 <code>retain</code> 操作，这会导致可能的对象释放错误。</p></li><li><p>在 <code>ARC</code> 下，编译器会自动进行内存管理，在 <code>block</code> 被创建时，会对它所捕获的对象发送一次 <code>retain</code> 消息，同时在 <code>block</code> 被释放时，发送一次 <code>release</code> 消息。</p></li></ul></details><h4 id="❓block-中可以使用-kvc-来访问或修改实例变量吗" tabindex="-1"><a class="header-anchor" href="#❓block-中可以使用-kvc-来访问或修改实例变量吗" aria-hidden="true">#</a> ❓<code>block</code> 中可以使用 <code>KVC</code> 来访问或修改实例变量吗</h4><details class="hint-container details"><summary>💡</summary><p>在 <code>block</code> 中可以使用 <code>KVC</code>(Key-Value Coding) 访问和修改实例变量。</p><ul><li><p>被访问的实例变量是否存在对应的访问器方法(<code>getter</code> 和 <code>setter</code>)。</p></li><li><p>被访问的实例变量在类的接口中是否有定义，或者有无 <code>@property</code> 声明。</p></li></ul><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>__weak <span class="token keyword">typeof</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">)</span> weakSelf <span class="token operator">=</span> <span class="token keyword">self</span><span class="token punctuation">;</span>
<span class="token keyword">self</span><span class="token punctuation">.</span>myBlock <span class="token operator">=</span> <span class="token operator">^</span><span class="token punctuation">{</span>
    <span class="token comment">// 使用 KVC 访问实例变量</span>
    NSInteger value <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span>weakSelf valueForKey<span class="token punctuation">:</span><span class="token string">@&quot;someValue&quot;</span><span class="token punctuation">]</span> integerValue<span class="token punctuation">]</span><span class="token punctuation">;</span>
    
    <span class="token comment">// 使用 KVC 修改实例变量</span>
    <span class="token punctuation">[</span>weakSelf setValue<span class="token punctuation">:</span><span class="token operator">@</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span> forKey<span class="token punctuation">:</span><span class="token string">@&quot;someValue&quot;</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓gcd-中的-block-是在堆上还是栈上" tabindex="-1"><a class="header-anchor" href="#❓gcd-中的-block-是在堆上还是栈上" aria-hidden="true">#</a> ❓<code>GCD</code> 中的 <code>block</code> 是在堆上还是栈上</h4><details class="hint-container details"><summary>💡</summary><p><code>block</code> 初始创建时是在栈上。但是，当调用 <code>block</code> 的 <code>copy</code> 方法或者把 <code>block</code> 传递给需要异步执行的 <code>GCD</code> 方法时，<code>block</code> 会被复制到堆上。</p><p><code>GCD</code> 是 <code>Apple</code> 提供的一种多核编程的解决方案，它允许将任务封装到 <code>block</code> 里面，然后放入 <code>Dispatch Queue</code>（调度队列）中进行异步执行。为了避免 <code>block</code> 在执行时由于变量被释放而产生错误，<code>GCD</code> 会手动对 <code>block</code> 进行复制操作，把 <code>block</code> 从栈复制到堆上，这样即便 <code>block</code> 的创建环境已经不存在，<code>block</code> 中也仍然能够访问到创建环境的变量值。</p></details><hr><h2 id="分类" tabindex="-1"><a class="header-anchor" href="#分类" aria-hidden="true">#</a> 分类</h2><h3 id="❓category-vs-extension" tabindex="-1"><a class="header-anchor" href="#❓category-vs-extension" aria-hidden="true">#</a> ❓<code>Category</code> vs <code>Extension</code></h3><details class="hint-container details"><summary>💡</summary><blockquote><p><code>Category</code>(类别)：<code>Objective-C</code> 的一个特性，提供了一种给已存在类添加方法的机制.</p></blockquote><ul><li><p><code>Category</code> : 只能添加方法，不能添加实例变量(除非通过关联对象)。分类中的方法是运行时添加到方法列表中的。</p></li><li><p><code>Extension</code> : 可以添加方法和计算属性，也可以添加构造方法，也可以实现以一个协议。编译时已经确定，不支持动态添加方法。</p></li></ul></details><h3 id="❓nsobject-添加-extension-会怎么样" tabindex="-1"><a class="header-anchor" href="#❓nsobject-添加-extension-会怎么样" aria-hidden="true">#</a> ❓<code>NSObject</code> 添加 <code>Extension</code> 会怎么样</h3><details class="hint-container details"><summary>💡</summary><p>向 <code>NSObject</code> 添加 <code>Extension</code>(类别)会使 <code>Extension</code> 中的方法对继承自 <code>NSObject</code> 的所有对象可见和可用。这会对项目产生全局性的影响。</p><p><code>Extension</code> 可以用来添加新的方法到现有的类中，但它没有自己的实例变量，所以不能存储新的状态。在使用 <code>Extension</code> 时，需要注意以下几点：</p><ul><li>如果覆盖已有的方法，所有继承自 <code>NSObject</code> 的对象都会受到影响，可能出现无法预知的结果。</li><li>如果添加的新方法与其他库或者框架定义的方法发生冲突，同样可能导致问题。</li></ul></details><h3 id="加载顺序" tabindex="-1"><a class="header-anchor" href="#加载顺序" aria-hidden="true">#</a> 加载顺序</h3><h4 id="❓如果分类中添加与类相同的方法签名会发生什么" tabindex="-1"><a class="header-anchor" href="#❓如果分类中添加与类相同的方法签名会发生什么" aria-hidden="true">#</a> ❓如果分类中添加与类相同的方法签名会发生什么</h4><details class="hint-container details"><summary>💡</summary><p>当一个 <code>OC</code> 的类别（<code>Category</code>）和主类都定义了相同签名的方法时，运行时系统会优先调用类别的方法，而忽略主类中的同名方法。这是因为在运行时的方法解析过程中，分类的方法会被先于主类方法加入到方法列表中。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">@interface</span> MyClass <span class="token punctuation">:</span> NSObject
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>print<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> MyClass
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>print <span class="token punctuation">{</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;print in MyClass&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>

<span class="token keyword">@interface</span> <span class="token function">MyClass</span> <span class="token punctuation">(</span>MyCategory<span class="token punctuation">)</span>
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>print<span class="token punctuation">;</span>
<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> <span class="token function">MyClass</span> <span class="token punctuation">(</span>MyCategory<span class="token punctuation">)</span>
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>print <span class="token punctuation">{</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;print in MyCategory&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>

<span class="token comment">// 调用时</span>
MyClass <span class="token operator">*</span>myClass <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">[</span>MyClass alloc<span class="token punctuation">]</span> init<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">[</span>myClass print<span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// 输出：print in MyCategory</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓两个分类的-load-方法的加载顺序" tabindex="-1"><a class="header-anchor" href="#❓两个分类的-load-方法的加载顺序" aria-hidden="true">#</a> ❓两个分类的 <code>load</code> 方法的加载顺序</h4><details class="hint-container details"><summary>💡</summary><p>类和分类中的 <code>+load</code> 方法会在类或分类被加载到运行时时调用。对于同一个类的不同分类，各自的 <code>+load</code> 方法的调用顺序与编译顺序有关，最后编译的分类的 <code>+load</code> 方法会被先调用。</p><p>注意：虽然编译顺序是影响 <code>+load</code> 方法调用顺序的因素之一，但不是唯一的决定因素。在一个复杂的项目中，编译顺序可能会受到 <code>Xcode</code> 编译设置、文件名字母序、<code>Xcode</code> 版本、甚至 <code>macOS</code> 系统版本的影响。因此，关于这个问题，<code>Apple</code> 官方文档建议不要依赖于 <code>+load</code> 方法的调用顺序。如果项目中多个 <code>+load</code> 方法之间有依赖关系，那么可能需要仔细设计代码，以避免因 <code>+load</code> 方法调用顺序引起的问题。</p></details><h4 id="❓一个类中有个多个分类都实现了相同的方法-会调用哪个方法" tabindex="-1"><a class="header-anchor" href="#❓一个类中有个多个分类都实现了相同的方法-会调用哪个方法" aria-hidden="true">#</a> ❓一个类中有个多个分类都实现了相同的方法，会调用哪个方法</h4><details class="hint-container details"><summary>💡</summary><p>当一个类有多个分类（<code>Category</code>），并且这些分类都实现了相同的方法时，会优先调用最后编译的分类中的方法。编译器按照 <code>.m</code> 文件编译顺序来加载分类，如果存在多个分类都实现了同一方法，那么顺序是&quot;编译顺序的逆序&quot;，也就是说最后编译的分类会首先被加载，同名方法就会被最后编译的那个分类所覆盖。</p><p>但是具体调用哪个方法并不是确定的，因为分类的编译顺序可能会因为项目配置的改变而改变，比如编译选项的修改、文件名的修改等。所以，实际开发中并不建议在不同的分类中实现相同的方法，这可能会引起不可预见的 <code>bug</code>。如果需要在分类中添加方法，应确保新添加的方法与主类和其他分类的方法不冲突。</p></details><h4 id="❓怎么保证自己的类一定能调用到自己写的方法" tabindex="-1"><a class="header-anchor" href="#❓怎么保证自己的类一定能调用到自己写的方法" aria-hidden="true">#</a> ❓怎么保证自己的类一定能调用到自己写的方法</h4><details class="hint-container details"><summary>💡</summary><p>分类（<code>Category</code>）是一种可以为已存在的类添加新方法的方式。同时，分类中的方法会替换掉原来类中同名的方法。此特性在一些情况下可能引起不确定的行为，比如可能无意间覆盖掉你写的方法。</p><p>预防方法被分类覆盖的方法：</p><ul><li><p><strong>方法命名规范：</strong> 尽可能为方法命名时，添加前缀。这样可以避免和系统默认的方法或他人写的分类中的方法冲突。</p></li><li><p><strong>使用子类化代替分类：</strong> 如果你控制的类可能被其他开发者使用，那么建议使用子类代替分类。这样，即使有人创建了同名的分类，也不会对原类产生影响。</p></li><li><p><strong>在源代码级别进行检查：</strong> 在项目编译的时候，通过检查操作来警告和暴露任何重定义的方法。</p></li></ul><p>注意：以上的方式并不能 <code>100%</code> 的避免在运行时被覆盖的情况，因为 <code>Objective-C</code> 是一种动态类型的语言，它允许在运行时添加或替换方法。这种动态性是 <code>Objective-C</code> 的一大特性，但同时也可能带来风险。所以编程时应该遵循良好的开发习惯，避免不必要的麻烦。</p></details><h3 id="底层实现" tabindex="-1"><a class="header-anchor" href="#底层实现" aria-hidden="true">#</a> 底层实现</h3><h4 id="❓分类是如何实现的-为什么会覆盖掉原来的方法" tabindex="-1"><a class="header-anchor" href="#❓分类是如何实现的-为什么会覆盖掉原来的方法" aria-hidden="true">#</a> ❓分类是如何实现的，为什么会覆盖掉原来的方法</h4><details class="hint-container details"><summary>💡</summary><p><code>Objective-C</code> 中的分类 (<code>Category</code>) 实质上是对一个存在的类添加额外的方法，而不改变它的声明和定义。分类是在运行时通过 <code>Objective-C</code> 的动态性质合并到类上的，这使得可以随意地向任何类添加方法。</p><p>底层实现：是一个 <code>objc_category</code> 结构体，它包含了类名(<code>name</code>)、类(<code>cls</code>)、类别中所有方法的列表(<code>method_list</code>)、类别的协议列表(<code>protocol_list</code>)、类别中所有的属性列表(<code>property_list</code>)。</p><p>加载过程：在程序运行时的 <code>load</code> 阶段进行的，所有类和分类的 <code>+load</code> 方法（如果有）被调用。在调用 <code>+load</code> 方法之前，<code>runtime</code> 会先将分类的方法、协议、属性合并到类的数据结构中。这个过程中，分类的方法被放在了原来类方法列表的前面，所以在类和分类中有相同方法名的方法的时候，分类的方法会被优先调用，原类的方法被覆盖。</p><p>分类的本质：在编译阶段，每一个分类会被编译成一个特殊的结构体（如 <code>_category_t</code>），其中包含了分类名，分类所在类的名字，以及分类中所有添加的方法的列表。在程序启动的时候，这个结构有一个初始化函数会被自动调用。在这个函数中，分类中的方法会被添加到类的方法列表中。因此可以在分类中添加新的方法。</p><p>分类中的方法会覆盖掉原来的方法，主要是因为分类的方法被添加到方法列表的顺序优先级更高。在 <code>Objective-C</code> 查找方法实现的时候，是按照方法在列表中的顺序，从前往后查找的，当找到第一个匹配的方法时，就停止查找，并且调用这个方法。因此，如果分类中的方法与原类中的方法同名，分类中的方法就会被优先调用，从而覆盖掉原来的方法。</p></details><h4 id="❓分类中有与主类相同的方法-如果想要调用主类中的方法如何处理" tabindex="-1"><a class="header-anchor" href="#❓分类中有与主类相同的方法-如果想要调用主类中的方法如何处理" aria-hidden="true">#</a> ❓分类中有与主类相同的方法，如果想要调用主类中的方法如何处理</h4><details class="hint-container details"><summary>💡</summary><p>如果分类中的方法与主类的方法名称相同，由于运行时会优先调用分类的方法，所以通常无法直接调用到主类中的方法。不过有一种间接的办法可以实现。可以在分类方法内部通过 <code>NSInvocation</code> 或 <code>methodSignatureForSelector:</code> 和 <code>forwardInvocation:</code> 的方式调用主类的原方法。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">import</span> <span class="token string">&quot;MyClass.h&quot;</span></span>

<span class="token keyword">@implementation</span> <span class="token function">MyClass</span> <span class="token punctuation">(</span>MyCategory<span class="token punctuation">)</span>
<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>print <span class="token punctuation">{</span>
    <span class="token comment">// 获取主类原方法</span>
    SEL origSel <span class="token operator">=</span> <span class="token keyword">@selector</span><span class="token punctuation">(</span>print<span class="token punctuation">)</span><span class="token punctuation">;</span>
    Method origMethod <span class="token operator">=</span> <span class="token function">class_getInstanceMethod</span><span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token keyword">self</span> class<span class="token punctuation">]</span><span class="token punctuation">,</span> origSel<span class="token punctuation">)</span><span class="token punctuation">;</span>
    
    <span class="token comment">// 构建 NSInvocation</span>
    NSInvocation <span class="token operator">*</span>inv <span class="token operator">=</span> <span class="token punctuation">[</span>NSInvocation invocationWithMethodSignature<span class="token punctuation">:</span><span class="token punctuation">[</span><span class="token keyword">self</span> methodSignatureForSelector<span class="token punctuation">:</span>origSel<span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token punctuation">[</span>inv setSelector<span class="token punctuation">:</span>origSel<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token punctuation">[</span>inv setTarget<span class="token punctuation">:</span><span class="token keyword">self</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
    
    <span class="token comment">// 调用 NSInvocation</span>
    <span class="token punctuation">[</span>inv invoke<span class="token punctuation">]</span><span class="token punctuation">;</span>
    
    <span class="token comment">// 获取返回值</span>
    NSString <span class="token operator">*</span>returnValue<span class="token punctuation">;</span>
    <span class="token punctuation">[</span>inv getReturnValue<span class="token punctuation">:</span><span class="token operator">&amp;</span>returnValue<span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> returnValue<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></details><h4 id="❓分类中为什么不能添加-property" tabindex="-1"><a class="header-anchor" href="#❓分类中为什么不能添加-property" aria-hidden="true">#</a> ❓分类中为什么不能添加 <code>@property</code></h4><details class="hint-container details"><summary>💡</summary><p><code>@property</code>会告诉编译器我们在类中声明了一个属性。默认情况下，编译器会生成一个后备存储（实例变量），并且为属性生成 <code>getter</code> 和 <code>setter</code> 方法。然而，对于分类（<code>Category</code>），一旦对象在运行时被注册，就不能再动态地添加实例变量。</p><p>从 <code>runtime</code> 角度来看， 这是由于类型的数据布局已经在编译阶段确定并且注册到了 <code>Objective-C</code> 的 <code>runtime</code> 系统中，而分类是在运行时被加载，所以无法更改基类的数据布局，也就无法添加实例变量。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token comment">// 类结构体，中的 \`ivar_list\`（实例变量列表）是确定</span>
<span class="token keyword">struct</span> objc_class <span class="token punctuation">{</span>
    Class isa<span class="token punctuation">;</span>
    Class super_class<span class="token punctuation">;</span>
    <span class="token keyword">const</span> <span class="token keyword">char</span> <span class="token operator">*</span>name<span class="token punctuation">;</span>
    <span class="token keyword">long</span> version<span class="token punctuation">;</span>
    <span class="token keyword">long</span> info<span class="token punctuation">;</span>
    <span class="token keyword">long</span> instance_size<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> objc_ivar_list <span class="token operator">*</span>ivars<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token comment">// 分类结构体，只有方法列表，协议列表和属性列表，并没有实例变量列表</span>
<span class="token keyword">struct</span> category_t <span class="token punctuation">{</span>
    <span class="token keyword">const</span> <span class="token keyword">char</span> <span class="token operator">*</span>name<span class="token punctuation">;</span>
    classref_t cls<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> method_list_t <span class="token operator">*</span>instanceMethods<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> method_list_t <span class="token operator">*</span>classMethods<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> protocol_list_t <span class="token operator">*</span>protocols<span class="token punctuation">;</span>
    <span class="token keyword">struct</span> property_list_t <span class="token operator">*</span>instanceProperties<span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>Objective-C</code> 分类中不能添加实例变量，也就不能添加带有自动生成的 <code>getter</code> 和 <code>setter</code> 的 <code>@property</code>。但是可以在分类中声明属性，并通过对已有的实例变量手动编写 <code>getter</code> 和 <code>setter</code>，或者使用关联对象(<code>Associated Objects</code>)来模拟实例变量。</p><p>总结：之所以不能在分类中添加 <code>@property</code>，是因为在编译器层面和 <code>runtime</code> 层面上，<code>Objective-C</code> 的设计都不允许动态地向类中添加实例变量。</p></details><h4 id="❓分类怎么实现一个-weak-属性" tabindex="-1"><a class="header-anchor" href="#❓分类怎么实现一个-weak-属性" aria-hidden="true">#</a> ❓分类怎么实现一个 <code>weak</code> 属性</h4><details class="hint-container details"><summary>💡</summary><blockquote><p>分类（<code>Category</code>）主要用于给现有的类添加方法，但是它不能直接添加属性。如果你想在分类中添加一个 <code>weak</code> 属性，需要使用的关联对象（<code>Associated Objects</code>）来实现。</p></blockquote><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">import</span> <span class="token expression"><span class="token operator">&lt;</span>objc<span class="token operator">/</span>runtime<span class="token punctuation">.</span>h<span class="token operator">&gt;</span></span></span>

<span class="token keyword">@interface</span> <span class="token function">NSString</span> <span class="token punctuation">(</span>WeakProperty<span class="token punctuation">)</span>

<span class="token keyword">@property</span> <span class="token punctuation">(</span>nonatomic<span class="token punctuation">,</span> weak<span class="token punctuation">)</span> id weakProperty<span class="token punctuation">;</span>

<span class="token keyword">@end</span>

<span class="token keyword">@implementation</span> <span class="token function">NSString</span> <span class="token punctuation">(</span>WeakProperty<span class="token punctuation">)</span>

<span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>setWeakProperty<span class="token punctuation">:</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>weakProperty <span class="token punctuation">{</span>
    <span class="token comment">// 使用 OBJC_ASSOCIATION_ASSIGN 它表示一个弱引用，但不像 weak 那样会自动置 nil，需要在适当的时候置 nil</span>
    <span class="token function">objc_setAssociatedObject</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">,</span> <span class="token keyword">@selector</span><span class="token punctuation">(</span>weakProperty<span class="token punctuation">)</span><span class="token punctuation">,</span> weakProperty<span class="token punctuation">,</span> OBJC_ASSOCIATION_ASSIGN<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token operator">-</span> <span class="token punctuation">(</span>id<span class="token punctuation">)</span>weakProperty <span class="token punctuation">{</span>
    <span class="token keyword">return</span> <span class="token function">objc_getAssociatedObject</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">,</span> <span class="token keyword">@selector</span><span class="token punctuation">(</span>weakProperty<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">@end</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>注：这种方式并不是真正意义上在分类中添加属性，只是把一个对象关联到了分类的实例对象上，并没有改变类的结构。在内存分配上，主类并不知道新增的这个关联对象。这种方案产生的影响仅限于对象在生命周期内，一旦对象销毁，关联的对象也就随之销毁。</p></details><hr><h2 id="消息发送" tabindex="-1"><a class="header-anchor" href="#消息发送" aria-hidden="true">#</a> 消息发送</h2><h3 id="❓oc-消息发送-vs-c-调用虚方法" tabindex="-1"><a class="header-anchor" href="#❓oc-消息发送-vs-c-调用虚方法" aria-hidden="true">#</a> ❓<code>OC</code> 消息发送 vs <code>C++</code> 调用虚方法</h3><details class="hint-container details"><summary>💡</summary><p>概念知识：</p><ul><li><p><code>Objective-C</code> 消息发送：是 <code>Objective-C</code> 进行方法调用的方式。</p><blockquote><p>它会在运行时动态地确定应该调用对象的哪个方法。当给 <code>Objective-C</code> 对象发送消息时，运行时系统会搜索接收者的类以及所有父类的方法列表，找到第一个可以响应该消息的方法并调用。</p></blockquote></li><li><p><code>C++</code> 虚方法：是 <code>C++</code> 支持多态性的一种方式。</p><blockquote><p>在 <code>C++</code> 中，如果一个父类指针指向派生类对象，并通过该指针调用虚函数，那么调用的将是派生类中的虚函数实现。这是通过 <code>C++</code> 的虚函数表（<code>vtable</code>）来实现的，虚函数表是在编译期确定的。</p></blockquote></li></ul><p>两者区别：</p><ul><li><p><strong>动态性</strong>：<code>Objective-C</code> 的消息发送更具有动态性，因为它在运行时确定方法调用，而 <code>C++</code> 的虚函数在编译时已经确定。</p></li><li><p><strong>灵活性</strong>：<code>Objective-C</code> 的消息发送机制允许在运行时为对象动态添加方法，这使得 <code>Objective-C </code>具有更高的灵活性。</p></li><li><p><strong>性能</strong>：由于 <code>C++</code> 的虚函数调用是在编译期就已经确定，因此它的性能上优于 <code>Objective-C</code> 的动态消息发送。</p></li><li><p><strong>安全性</strong>：<code>C++</code> 的类型检查更为严格，因此调用虚方法要比 <code>Objective-C</code> 发送消息更安全，后者如果发送了对象无法响应的消息就会在运行时崩溃。</p></li></ul></details><h3 id="❓消息转发机制和其他语言的消息机制优劣对比" tabindex="-1"><a class="header-anchor" href="#❓消息转发机制和其他语言的消息机制优劣对比" aria-hidden="true">#</a> ❓消息转发机制和其他语言的消息机制优劣对比</h3><details class="hint-container details"><summary>💡</summary><ul><li><p><strong>动态性</strong>：消息转发机制可以在运行时动态地处理未知的消息。而其他一些语言，如 <code>C++</code>, <code>Java</code>, <code>C#</code> 等，在编译期间就已经确定了方法的调用，并不能在运行时动态地处理未知的消息。</p></li><li><p><strong>灵活性</strong>：消息转发可以不仅仅是转发给其他对象，甚至可以动态地为类生成新的方法。这在很多其他语言中是无法做到的。</p></li><li><p><strong>性能</strong>：消息转发机制相对于其他语言的消息机制，如 <code>C++</code> 的虚函数，性能上会稍差一些。因为 <code>Objective-C</code> 是在运行时通过查找类的方法列表来确定方法调用，而不是在编译时就确定。</p></li><li><p><strong>安全性</strong>：消息转发机制如果使用不当，可能会产生一些难以发现的错误。例如，如果错误地转发了消息，可能会导致运行时崩溃。而其他一些语言，如 <code>Java</code>, <code>C#</code>, 是在编译期就检查了方法的调用，相对来说更加安全。</p></li></ul></details><h3 id="❓oc-是否支持方法重载" tabindex="-1"><a class="header-anchor" href="#❓oc-是否支持方法重载" aria-hidden="true">#</a> ❓<code>OC</code> 是否支持方法重载</h3><details class="hint-container details"><summary>💡</summary><p><code>OC</code> 不支持方法重载。方法重载指的是在同一个类里面，方法名字相同，但参数不同的多个方法。</p><p><code>OC</code> 是一种动态类型语言，其方法的调用不是在编译期决定的，而是在运行时决定的。<code>OC</code> 使用消息派发机制，方法调用更依赖于方法的唯一性，如果有多个方法名字完全相同（即使参数类型不同），编译器和运行时系统就无法准确找到你要调用的方法。</p></details><h3 id="❓静态方法是否能被重写" tabindex="-1"><a class="header-anchor" href="#❓静态方法是否能被重写" aria-hidden="true">#</a> ❓静态方法是否能被重写</h3><details class="hint-container details"><summary>💡</summary><p>在 <code>Objective-C</code> 中，通常所说的 &quot;静态方法&quot; 实际上是指&quot;类方法&quot;，用 &quot;+&quot; 符号来声明。这种方法是可以被子类重写的。也就是说，如果在子类中重写了一个已经在父类中定义的类方法，那么当你用子类来调用这个类方法的时候，实际上执行的是子类中的版本，这就是方法重写。所以，&quot;静态方法&quot;是可以被子类重写的。</p><p>然而这和其他一些语言如 <code>Java</code> 或 <code>C++</code> 中的静态方法是不一样的。在 <code>Java</code> 或<code> C++</code> 中，声明为 <code>static</code> 的方法是不能被子类重写的。</p></details><h3 id="方法查找" tabindex="-1"><a class="header-anchor" href="#方法查找" aria-hidden="true">#</a> 方法查找</h3><h4 id="❓sel、-cmd、imp-是什么" tabindex="-1"><a class="header-anchor" href="#❓sel、-cmd、imp-是什么" aria-hidden="true">#</a> ❓<code>SEL</code>、<code>_cmd</code>、<code>IMP</code> 是什么</h4><details class="hint-container details"><summary>💡</summary><ul><li><p><code>SEL</code>：是一个选择器的类型。选择器是方法选择器的指定类型，可以想象它就像方法或函数的名字。<code>SEL</code>类型的变量就是用来保存这些方法名的。实际上是一个字符串，不同的方法对应不同的 <code>SEL</code>。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code>SEL methodSEL <span class="token operator">=</span> <span class="token keyword">@selector</span><span class="token punctuation">(</span>methodName<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p><code>_cmd</code>：是一个隐藏的参数，表示当前调用的方法的 <code>SEL</code>。每个方法的实现的第二个参数<code>_cmd</code>，它是一个 <code>SEL</code> 类型的，即当前方法的选择器。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token operator">-</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>someMethod <span class="token punctuation">{</span>
    <span class="token function">NSLog</span><span class="token punctuation">(</span><span class="token string">@&quot;%@&quot;</span><span class="token punctuation">,</span> <span class="token function">NSStringFromSelector</span><span class="token punctuation">(</span>_cmd<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出 &quot;someMethod&quot;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><code>IMP</code>：是一个函数指针，这个函数会处理发送到类实例的消息。<code>IMP</code> 类型的函数实际上就是对象方法的实现，一个 <code>IMP</code> 函数会接收两个隐藏的参数 <code>self</code> 和 <code>_cmd</code>。</p></li></ul></details><h4 id="❓oc-中是如何通过-sel-找到对应的-imp-地址" tabindex="-1"><a class="header-anchor" href="#❓oc-中是如何通过-sel-找到对应的-imp-地址" aria-hidden="true">#</a> ❓<code>OC</code> 中是如何通过 <code>SEL</code> 找到对应的 <code>IMP</code> 地址</h4><details class="hint-container details"><summary>💡</summary><ul><li><p><strong>查找类方法列表</strong>：当向一个对象发送一条消息，运行时系统首先会在该对象的类的方法列表中查找与这个 <code>selector</code> 相对应的方法。如果找到，那么就直接调用这个方法的实现。在这个步骤中，<code>selector</code> 会被映射到其对应方法的 <code>IMP</code>。</p></li><li><p><strong>在继承链中查找</strong>：如果在对象的类的方法列表中没有找到对应的方法，那么运行时系统会在该类的父类中查找，如果在父类中找到，同样直接调用对应的 <code>IMP</code>。这个过程会一直沿着类的继承链查找，直到 <code>NSObject</code>。</p></li><li><p><strong>调用动态方法解析</strong>：如果在整个继承链上都没有找到对应的方法，那么运行时系统会给一个“补救”的机会，即会调用该类的 <code>+resolveInstanceMethod:</code> 或 <code>+resolveClassMethod:</code> 方法（取决于是实例方法还是类方法）。可以在此方法中为对应的 <code>selector</code> 动态添加实现。</p></li><li><p><strong>消息转发</strong>：如果 <code>+resolveInstanceMethod:</code> 或 <code>+resolveClassMethod:</code> 返回 <code>NO</code> 或者没有被重写，那么运行时就会启动完整消息转发，此时会调用 <code>- (void)forwardInvocation:(NSInvocation *)anInvocation;</code> 方法。可以在这个方法内将消息转发给能响应此消息的其他对象。</p></li><li><p><strong>抛出异常</strong>：如果以上步骤都没有找到并执行相应的方法，那么运行时系统就会抛出 <code>unrecognized selector</code> 异常。</p></li></ul></details><h4 id="❓unrecognized-selector-异常是如何发生-应该如何避免" tabindex="-1"><a class="header-anchor" href="#❓unrecognized-selector-异常是如何发生-应该如何避免" aria-hidden="true">#</a> ❓<code>unrecognized selector</code> 异常是如何发生，应该如何避免</h4><details class="hint-container details"><summary>💡</summary><p><code>unrecognized selector</code> 异常是一个常见运行时错误，当向一个对象发送它不能响应的消息时，就会抛出这个异常。</p><p>当调用一个对象的方法时，实际上是在向这个对象发送一条消息。运行时系统会在对象的类及其所有的父类中寻找与这个消息对应的方法。如果找到这个方法，就会调用它。如果在最底层的父类（通常是 <code>NSObject</code>）中也找不到这个方法，那么就会抛出 <code>unrecognized selector</code> 异常。</p><p>避免 <code>unrecognized selector</code> 异常：</p><ul><li><p><strong>检查对象是否能响应此消息</strong>：在向对象发送消息之前，使用 <code>respondsToSelector:</code> 方法检查这个对象是否能响应这个消息。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token punctuation">[</span>obj respondsToSelector<span class="token punctuation">:</span><span class="token keyword">@selector</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token punctuation">[</span>obj foo<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><p><strong>使用异常处理</strong>：<code>Objective-C</code> 提供了异常处理语句 <code>@try、@catch、@finally</code>。可以将可能抛出异常的代码放在 <code>@try</code> 块中，将处理异常的代码放在 <code>@catch</code> 块中。</p></li><li><p><strong>消息转发</strong>：如果一个对象无法识别引发异常的方法，那么在引发异常之前，会先调用这个对象的 <code>- (void)forwardInvocation:(NSInvocation *)anInvocation;</code> 方法，可以在这个方法里面向其他能响应此消息的对象发送该消息。这就相当于将此消息转发给其他对象。</p></li></ul></details><h3 id="类方法" tabindex="-1"><a class="header-anchor" href="#类方法" aria-hidden="true">#</a> 类方法</h3><h4 id="❓oc-中的类方法和实例方法有什么本质区别和联系" tabindex="-1"><a class="header-anchor" href="#❓oc-中的类方法和实例方法有什么本质区别和联系" aria-hidden="true">#</a> ❓<code>OC</code> 中的类方法和实例方法有什么本质区别和联系</h4><details class="hint-container details"><summary>💡</summary><p>本质区别：</p><ul><li><p>存储的位置：</p><ul><li>实例方法列表存储在类对象（<code>Class</code>）中。</li><li>类方法列表存储在元类对象（<code>Meta-Class</code>）中。</li></ul></li><li><p>调用者：</p><ul><li>实例方法由实例对象调用，即 <code>[instanceObject exampleMethod];</code>，其中 <code>exampleMethod</code> 是实例对象的方法。</li><li>类方法由类对象调用，即 <code>[ClassObject exampleMethod];</code>，其中 <code>exampleMethod</code> 是类方法。</li></ul></li><li><p>方法查找过程：</p><ul><li>调用实例方法时，<code>runtime</code> 系统会在类的方法列表中查找这个方法。</li><li>调用类方法时，<code>runtime</code> 系统会在元类的方法列表中查找这个方法。</li></ul></li><li><p><code>self</code> 指针：</p><ul><li>在实例方法中，<code>self</code> 指向调用这个方法的实例对象。</li><li>在类方法中，<code>self</code> 指向调用这个方法的类对象。</li></ul></li><li><p>能访问的变量：</p><ul><li>实例方法可以访问实例变量和静态变量。</li><li>类方法只能访问静态变量。</li></ul></li></ul></details><h4 id="❓在类方法中是否可以使用-self" tabindex="-1"><a class="header-anchor" href="#❓在类方法中是否可以使用-self" aria-hidden="true">#</a> ❓在类方法中是否可以使用 <code>self</code></h4><details class="hint-container details"><summary>💡</summary><p>可以在类方法中使用 <code>self</code>。但在类方法中，<code>self</code> 表示的是类对象本身，而不是类的实例。这意味着不能用 <code>self</code> 来访问实例变量，只能用它来调用其他类方法。</p><div class="language-objc line-numbers-mode" data-ext="objc"><pre class="language-objc"><code><span class="token operator">+</span> <span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span>aClassMethod <span class="token punctuation">{</span>
    <span class="token comment">// 这里的 self 是类对象本身</span>
    <span class="token punctuation">[</span><span class="token keyword">self</span> anotherClassMethod<span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>总结：<code>self</code> 在类方法中指的是类对象本身，用来调用其他类方法；在实例方法中指的是调用该方法的实例对象，用来访问实例变量或调用实例方法。</p></details><h3 id="消息发送-1" tabindex="-1"><a class="header-anchor" href="#消息发送-1" aria-hidden="true">#</a> 消息发送</h3><h4 id="❓oc-中对象消息发送的大致流程" tabindex="-1"><a class="header-anchor" href="#❓oc-中对象消息发送的大致流程" aria-hidden="true">#</a> ❓<code>OC</code> 中对象消息发送的大致流程</h4><details class="hint-container details"><summary>💡</summary><ul><li><p><strong>消息发送者</strong>：当调用一个 <code>Objective-C</code> 对象的方法时，实际上是向这个对象发送了一条消息。</p><blockquote><p>例如，<code>[object methodWithParameter:parameter];</code>，这里的 <code>object</code> 是消息的接受者，<code>methodWithParameter:</code> 是消息的选择器（<code>SEL</code>，<code>Selector</code>）。</p></blockquote></li><li><p><strong>动态绑定</strong>：不是在编译的时候决定函数地址，而是在运行时根据接收者和选择器找到对应的函数实现。</p></li><li><p><strong>寻找接受者</strong>：明确了消息的接收者后，会在对应的类以及其父类中查找这个方法的实现。如果在消息接收者的类中找不到响应的方法，则继续往它的父类中寻找。</p></li><li><p><strong>消息转发</strong>：如果到了根类（<code>NSObject</code>）都没有找到方法实现，那么就会进入对象的 <code>doesNotRecognizeSelector:</code> 方法，进入此方法就算出错了，未对此方法做处理的话程序会 <code>crash</code>。不过，<code>NSObject</code> 提供了最后挽救的机会，走进消息转发机制，可以指定一个新的接受者来响应这条消息。如果消息转发也没有处理，就真的会 <code>crash</code> 了。</p></li></ul></details><h4 id="❓在方法调用的时候-方法查询-动态解析-消息转发-之前做了什么" tabindex="-1"><a class="header-anchor" href="#❓在方法调用的时候-方法查询-动态解析-消息转发-之前做了什么" aria-hidden="true">#</a> ❓在方法调用的时候，<code>方法查询-&gt; 动态解析-&gt; 消息转发</code> 之前做了什么</h4><details class="hint-container details"><summary>💡</summary><p>仅当通过发送消息调用方法失败的时候，才会启动方法查找（<code>method lookup</code>）、动态解析（<code>dynamic resolution</code>）和消息转发（<code>message forwarding</code>）这些步骤。</p><p>之前操作：</p><ul><li><p><strong>消息发送</strong>：实际上是调用了运行时库中的 <code>objc_msgSend</code> 函数。方法的调用者（即消息的接收者）和方法选择器（<code>selector</code>）都作为 <code>objc_msgSend</code> 的参数。</p></li><li><p><strong>查找对象的类</strong>：<code>objc_msgSend</code> 需要知道对象的类信息，以便在类的方法列表里查找可以响应该消息的方法。对象内部有一个指针指向它的类，<code>objc_msgSend</code> 可以通过这个指针获取对象的类。</p></li><li><p><strong>缓存查找</strong>：<code>Objective-C</code> 运行时系统为了优化性能，已经将一部分方法的调用结果缓存起来，所以 <code>objc_msgSend</code> 会先从缓存中查找 <code>selector</code> 对应的方法实现。如果缓存中有，则直接调用该方法结束查找流程；如果没有，进入下一步。</p></li><li><p><strong>方法列表查找</strong>：<code>objc_msgSend</code> 会在对象的类的方法列表中搜索 <code>selector</code> 对应的方法。如果找到，则调用该方法，将方法结果添加到缓存中，并结束查找流程。</p></li></ul></details><h4 id="❓向一个-nil-对象发送消息将会发生什么" tabindex="-1"><a class="header-anchor" href="#❓向一个-nil-对象发送消息将会发生什么" aria-hidden="true">#</a> ❓向一个 <code>nil</code> 对象发送消息将会发生什么</h4><details class="hint-container details"><summary>💡</summary><p>向 <code>nil</code> 对象发送消息是安全的，不会导致程序崩溃。这一特性是 <code>Objective-C</code> 语言中的一项重要特性，使其在处理对象的空态时更加灵活。</p><p>对一个 <code>nil</code> 对象发送消息：</p><ul><li><p><strong>方法返回非对象类型</strong>：如果该消息是一个返回非对象类型的方法（例如 <code>int</code>，<code>float</code>，<code>struct</code> 等），会返回零值（0，0.0，相应类型的零值等）。</p></li><li><p><strong>方法返回对象类型</strong>：如果该消息是一个返回对象的方法，那么将会返回 <code>nil</code>。</p></li><li><p><strong>方法不返回值</strong>：如果该消息是一个不返回值的方法，那么什么都不会发生。</p></li></ul><p>这种特性使得可以在不检查一个对象是否为 <code>nil</code> 的情况下向它发送消息，这通常能使代码更加简洁。反过来，如果在其他语言（例如 <code>Java</code> 或 <code>C#</code>）那样，在向对象发送消息之前总是先检查它是否为 <code>nil</code>，那么可能会编写出很多不必要的代码。然而，注意这并不意味着可以忽略 <code>nil</code> 检查，在某些情况下，<code>nil</code> 检查仍然是必要的。</p></details><h4 id="❓一个对象向一个不存在的方法发送消息-消息是如何转发的" tabindex="-1"><a class="header-anchor" href="#❓一个对象向一个不存在的方法发送消息-消息是如何转发的" aria-hidden="true">#</a> ❓一个对象向一个不存在的方法发送消息，消息是如何转发的</h4><details class="hint-container details"><summary>💡</summary><ul><li><p>动态方法解析（<code>Dynamic Method Resolution</code>）</p><blockquote><p>运行时系统会调用 <code>+resolveInstanceMethod:</code>（对于类方法是 <code>+resolveClassMethod:</code>）。在这个阶段，可以为这个类动态添加一个方法。如果此方法返回 <code>YES</code>，那么运行时系统会重新发送消息。</p></blockquote></li><li><p>备援接收者（<code>Backup Receiver</code>）</p><blockquote><p>如果第一步未处理消息（即 <code>+resolveInstanceMethod:</code> 返回 <code>NO</code>），系统会调用 <code>-forwardingTargetForSelector:</code> 方法，可以返回一个替代的对象。这个方法返回了一个实例后，系统会把这条消息发送给这个实例。</p></blockquote></li><li><p>完整消息转发（<code>Complete Message Forwarding</code>）</p><blockquote><p>如果 <code>-forwardingTargetForSelector:</code> 返回了 <code>nil</code>，运行时就会进入完整的消息转发阶段。首先会发送 <code>-methodSignatureForSelector:</code> 消息来获取方法的返回和参数类型。然后会发送 <code>-forwardInvocation:</code> 消息。可以在 <code>-forwardInvocation:</code> 中自行处理消息，或者转发给其他对象处理。</p></blockquote></li><li><p>消息转发失败</p><blockquote><p>如果在 <code>-forwardInvocation:</code> 中未处理消息，那么运行时会调用 <code>-doesNotRecognizeSelector:</code> 方法， 默认会抛出异常。</p></blockquote></li></ul></details><h4 id="❓向一个对象发送消息-obj-foo-和-objc-msgsend-函数之间有什么关系" tabindex="-1"><a class="header-anchor" href="#❓向一个对象发送消息-obj-foo-和-objc-msgsend-函数之间有什么关系" aria-hidden="true">#</a> ❓向一个对象发送消息 <code>[obj foo]</code> 和 <code>objc_msgSend()</code> 函数之间有什么关系</h4><details class="hint-container details"><summary>💡</summary><p><code>[obj foo]</code> 这种语义实际上是在运行时通过调用 <code>objc_msgSend(obj, @selector(foo))</code> 来实现的。</p><p>方法(<code>methods</code>)的调用被称为向一个对象发送消息。当使用方括号语法向对象发送消息时，如 <code>[obj foo]</code>，实际上运行时系统会将其转化为 <code>objc_msgSend()</code> 函数的调用。</p><p><code>objc_msgSend()</code> 是 <code>runtime</code> 库中的函数，接受两个以上的参数。第一个参数(<code>receiver</code>)是要发送消息的对象，第二个参数(<code>selector</code>)是要发送的消息(<code>method</code>)。例如，<code>objc_msgSend(obj, @selector(foo))</code> 就是向 <code>obj</code> 对象发送名为 <code>foo</code> 的消息。</p></details><h4 id="❓-objc-msgforward-函数是做什么的" tabindex="-1"><a class="header-anchor" href="#❓-objc-msgforward-函数是做什么的" aria-hidden="true">#</a> ❓<code>_objc_msgForward</code> 函数是做什么的</h4><details class="hint-container details"><summary>💡</summary><blockquote><p><code>_objc_msgForward</code> 是运行时系统中用于对消息进行转发（<code>Message Forwarding</code>）的函数。</p></blockquote><p>当向一个对象发送一条消息，运行时系统会在该对象的类以及其父类链中查找是否有可以响应这条消息的方法。如果没有找到的话，运行时系统会调用 <code>_objc_msgForward</code> 函数进行消息转发。</p></details><blockquote><p>❓直接调用它将会发生什么</p></blockquote><details class="hint-container details"><summary>💡</summary><p>一般不会直接调用 <code>_objc_msgForward</code> 函数。因为这个函数是由运行时系统在需要进行消息转发时自动调用的。</p><p>如果直接调用 <code>_objc_msgForward</code>，那么一般而言，这将会引起 <code>unrecognized selector</code> 异常，因为这个函数的作用就是处理不能识别的选择器（即方法）。如果手动调用它，系统会在尝试进行消息转发时找不到能响应此消息的对象或方法，从而引发异常。基本上，直接调用 <code>_objc_msgForward</code> 而没有适当的转发目标和处理机制，是没有意义的。</p></details><h4 id="❓怎么在汇编实现中计算方法调用时间" tabindex="-1"><a class="header-anchor" href="#❓怎么在汇编实现中计算方法调用时间" aria-hidden="true">#</a> ❓怎么在汇编实现中计算方法调用时间</h4><details class="hint-container details"><summary>💡</summary><blockquote><p>计算一个方法调用的时间，通常的方案是在调用方法之前和之后各取一次系统时间，并计算时间差。不过在汇编语言中，这部分逻辑将变得更加复杂，主要源于汇编语言的低阶性质，以及 <code>Objective-C</code> 方法调用的动态性。</p></blockquote><p>若要在函数入口和返回处插入计时的代码，理论上需要修改编译出来的二进制文件，这在实际操作中较为困难。</p><p>一种更为可行的方法是 <code>hook</code> 函数的调用，在 <code>Objective-C</code> 中，对象方法调用最后实际上会通过 <code>objc_msgSend</code> 函数的机制实现，可以创建自定义版本的 <code>objc_msgSend</code>，并在这个函数中计算方法的执行时间。</p><p>大致步骤：</p><ul><li><p>在程序启动时，获取原始的 <code>objc_msgSend</code> 的函数指针，并把它保存下来。</p></li><li><p>编写一个新的函数，替代 <code>objc_msgSend</code>。在这个新函数中：</p><ul><li>使用当前时间减去开始时间，计算出时间差，也就是方法的执行时间。</li><li>执行原本的 <code>objc_msgSend</code> 函数。</li></ul></li><li><p>使用 <code>Objective-C</code> 的方法交换机制，将新的函数与 <code>objc_msgSend</code> 函数进行交换。</p></li></ul><p>注意：因为涉及到汇编语言，必须具备相应的 <code>ARM</code> 汇编知识，而且要考虑到指令集的不同，针对 32 位和 64 位可能需要分别处理。</p></details>`,97);function N(w,j){const s=d("ExternalLinkIcon");return i(),p("div",null,[o(" more "),r,o(" block "),n("ul",null,[n("li",null,[n("a",u,[a("2013-11-27 objc 中的 block"),e(s)])]),n("li",null,[n("a",k,[a("2013-05-29 对 Objective-C 中 Block 的追探"),e(s)])])]),o(" NSCache "),n("ul",null,[n("li",null,[n("a",m,[a("2015-12-09 NSCache 源码分析"),e(s)])]),n("li",null,[n("a",b,[a("2015-02-11 Foundation: NSCache"),e(s)])]),n("li",null,[n("a",v,[a("2012-07-14 NSCache"),e(s)])])]),h,n("details",y,[g,n("p",null,[n("a",S,[a("👉🏻"),e(s)])])]),f])}const q=t(l,[["render",N],["__file","0x01.objective-c.html.vue"]]);export{q as default};
